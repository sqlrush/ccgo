## 3. Overlay 与对话框

本章覆盖 CC 所有 overlay/dialog 界面：slash 菜单、自动补全弹窗、会话/主题/内存选择器、HelpV2、Doctor、onboarding/TrustDialog、全部工具权限对话框、状态行/cost/context 面板及通知机制。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|----|------|--------|----------------------------|---------|------|
| OVL-01 | 输入 `/` 时弹出 slash 命令菜单 | MANUAL | 前置:交互 REPL 运行中;操作:在空提示行按 `/` 键;预期:SlashMenu overlay 弹出，列出全部可见命令 | `src/components/PromptInput/PromptInput.tsx:2291` | ✅ 通过（loop.go:299 当 Prompt.Text=="/", activeOverlay = NewSlashMenu）|
| OVL-02 | slash 菜单键盘导航（↑↓ + Enter 选中，Esc 取消） | MANUAL | 前置:slash 菜单已打开;操作:按 ↓ 移动光标，Enter 选中;预期:选中命令填入提示行，菜单关闭 | `src/components/PromptInput/PromptInput.tsx` | ✅ 通过（slash_menu.go ApplyKey: Up/Down/Enter/Esc） |
| OVL-03 | slash 菜单实时过滤（继续输入字母后缀） | MANUAL | 前置:slash 菜单已打开;操作:输入 `r` 追加过滤;预期:菜单只显示以 `r` 开头的命令，光标重置 | `src/components/PromptInput/PromptInput.tsx` | ✅ 通过（slash_menu.go refilter()） |
| OVL-04 | slash 菜单 Backspace 收窄/退格查询词 | MANUAL | 前置:已输入 `/re`;操作:按 Backspace;预期:查询词变为 `r`，菜单重新过滤 | `src/components/PromptInput/PromptInput.tsx` | ✅ 通过（slash_menu.go KeyBackspace） |
| OVL-05 | `@` 快速打开文件选择器（QuickOpen） | MANUAL | 前置:REPL 中;操作:按 `app:quickOpen` 快捷键（默认 Ctrl+P 等）;预期:QuickOpenDialog 弹出，可模糊匹配工作目录文件名 | `src/components/PromptInput/PromptInput.tsx:1702` `src/components/QuickOpenDialog.tsx:28` | ❌ 缺失（ccgo 无 QuickOpenDialog 对等实现） |
| OVL-06 | `@` 目录补全（Tab 补全路径片段） | MANUAL | 前置:提示行输入 `@src/`;操作:按 Tab;预期:补全目录内容列表，选择后插入相对路径 | `src/components/PromptInput/PromptInput.tsx:1072` | ❌ 缺失（ccgo 无 @ 路径补全） |
| OVL-07 | 全局历史搜索对话框（HistorySearchDialog） | MANUAL | 前置:有历史提交记录;操作:按 `app:historySearch` 快捷键;预期:HistorySearchDialog 弹出，可模糊搜索提示历史并预览 | `src/components/HistorySearchDialog.tsx:27` | ❌ 缺失 |
| OVL-08 | 全局内容搜索对话框（GlobalSearchDialog） | MANUAL | 前置:REPL 中;操作:按全局搜索快捷键;预期:GlobalSearchDialog 弹出，可跨文件全文搜索并预览 | `src/components/GlobalSearchDialog.tsx:38` | ❌ 缺失 |
| OVL-09 | `/resume`（无参数）打开会话选择器 overlay | MANUAL | 前置:有历史会话;操作:在 REPL 中输入 `/resume` + Enter;预期:ResumePicker overlay 弹出，列出同目录下会话，方向键选择后加载 | `src/screens/ResumeConversation.tsx` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`commands_resume.go` 无参数时返回 `CommandOutcome{Overlay: NewResumePicker(...)}`，loop `applyCommandOutcome` 打开 overlay） |
| OVL-10 | `/resume <id|N|搜索词>` 直接加载指定会话 | MANUAL | 前置:有历史会话;操作:`/resume 1` 或 `/resume <id>`;预期:直接加载匹配会话，无需选择器 | `src/screens/ResumeConversation.tsx` | ✅ 通过（commands_resume.go resolveResumeTarget → 加载会话并注入 history） |
| OVL-11 | `/theme`（无参数）打开主题选择器 overlay | MANUAL | 前置:REPL 中;操作:输入 `/theme` + Enter;预期:ThemePicker overlay 弹出，列出所有主题，Enter 应用 | `src/components/ThemePicker.tsx:30` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`themeHandlerWith` 无参数时返回 `CommandOutcome{Overlay: NewThemePicker(builtinThemes)}`） |
| OVL-12 | `/theme <name>` 直接设置主题 | MANUAL | 前置:REPL 中;操作:`/theme dark`;预期:命令成功，状态栏显示 "Theme set to \"dark\""，设置持久化 | `src/components/ThemePicker.tsx` | ✅ 通过（commands_settings.go themeHandlerWith → config.SetSettingsValue） |
| OVL-13 | `/memory`（无参数）打开内存文件选择器 overlay | MANUAL | 前置:存在 CLAUDE.md 或 ~/.claude/CLAUDE.md;操作:输入 `/memory` + Enter;预期:MemorySelector overlay 弹出，列出可用内存文件，Enter 用 $EDITOR 打开 | `src/components/memory/MemoryFileSelector.tsx` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`memoryHandler` 注册于 production router，无参数时返回 `CommandOutcome{Overlay: NewMemorySelector(files)}`） |
| OVL-14 | `/help` 打开 HelpV2 overlay（命令列表 + 快捷键） | MANUAL | 前置:REPL 中;操作:输入 `/help` + Enter;预期:HelpV2 overlay 弹出，显示命令分组（General/Agents/Memory 等），Esc 关闭 | `src/components/HelpV2/HelpV2.tsx:20` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`helpHandler(registry)` 注册于 production router，返回 `CommandOutcome{Overlay: NewHelpScreen(registry)}`，隐藏命令已剔除） |
| OVL-15 | `/doctor` 打开诊断屏幕（TUI overlay） | MANUAL | 前置:REPL 中;操作:输入 `/doctor` + Enter;预期:显示环境检测报告（API 连通性、依赖版本等），以 overlay 方式渲染 | `src/screens/Doctor.tsx:100` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`doctorHandler(cwd, version)` 注册于 production router，调 `doctor.Run`/`doctor.Format` 经 `CommandOutcome.Status` 渲染到 transcript；全屏 overlay UI 仍待人工核验） |
| OVL-16 | onboarding 流程（首次启动多步向导） | MANUAL | 前置:全新未配置环境;操作:首次运行 `claude`;预期:显示 theme 选择 → 安全说明 → API-key 验证/OAuth 多步向导，每步可前进/后退 | `src/components/Onboarding.tsx:30` | ✅ 通过（接线就绪，渲染需人工核验）（F5: onboarding_dialog.go NewOnboardingDialog — 3步向导:主题/安全/登录；提交 onboard:theme:/safety:ack/login:browser/apikey/skip；onOverlaySubmit 接线；loop handleOverlaySubmit 路由 onboard: 前缀） |
| OVL-17 | TrustDialog 新目录信任提示 | MANUAL | 前置:首次在新目录启动;操作:运行 `claude`;预期:TrustDialog overlay 显示目录路径、hooks/MCP/bash 风险摘要，要求确认 "Trust this directory" / "Deny" | `src/components/TrustDialog/TrustDialog.tsx:23` | ✅ 通过（run.go:164 当 opts.Trust!=nil 时 activeOverlay=NewTrustDialog；trust_dialog.go 有 ApplyKey + Render 实现） |
| OVL-18 | TrustDialog 显示 hooks/MCP/bash 风险摘要 | MANUAL | 前置:目录含 hooks 或 MCP 配置;操作:触发 TrustDialog;预期:对话框显示 hooks 来源、MCP 服务器列表等安全风险摘要 | `src/components/TrustDialog/TrustDialog.tsx:46-128` | ⚠️ 已建未接（ccgo TrustDialog.Render 仅显示目录路径和 "Allow" / "Deny" 两个选项，不检索 hooks/MCP 摘要；trust_dialog.go 无对等详情） |
| OVL-19 | Bash 权限对话框（Allow once / Allow always for command / Deny） | MANUAL | 前置:默认权限模式;操作:模型请求执行 `rm -rf`;预期:弹出 Bash 权限对话框，显示命令预览，提供"Allow once / Allow always for this command / Deny"三选项 | `src/components/permissions/BashPermissionRequest/BashPermissionRequest.tsx:71` | ✅ 通过（loop.go showPermission → dialog_runtime.go RequestPermission；alwaysLabel 返回 "Allow always for this command"；tui.Screen 渲染 PermissionRequest） |
| OVL-20 | Edit/Write 权限对话框（文件路径 + diff 预览） | MANUAL | 前置:默认权限模式;操作:模型请求编辑文件;预期:弹出 Edit 权限对话框，显示目标文件路径（可选 diff），"Allow once / Allow always for this session / Deny" | `src/components/permissions/FileEditPermissionRequest/FileEditPermissionRequest.tsx` `src/components/permissions/FileWritePermissionRequest/FileWritePermissionRequest.tsx` | ✅ 通过（loop.go showPermission 统一入口；alwaysLabel "Allow always for this session"；diff_render.go 提供 diff 格式化） |
| OVL-21 | WebFetch 权限对话框（URL 预览 + 允许域名） | MANUAL | 前置:默认权限模式;操作:模型调用 WebFetch;预期:弹出对话框显示 URL，"Allow once / Allow always for this host / Deny" | `src/components/permissions/WebFetchPermissionRequest.tsx` | ✅ 通过（loop.go showPermission；alwaysLabel "Allow always for this host"；scopeRequired=true 防止无域名持久化） |
| OVL-22 | NotebookEdit 权限对话框 | MANUAL | 前置:默认权限模式;操作:模型请求编辑 .ipynb;预期:弹出 NotebookEdit 权限对话框，显示 notebook 路径，三选项 | `src/components/permissions/NotebookEditPermissionRequest/NotebookEditPermissionRequest.tsx` | ✅ 通过（alwaysLabel "Allow always for this session"；工具名 "NotebookEdit" 匹配 alwaysLabel case） |
| OVL-23 | Skill 权限对话框 | MANUAL | 前置:默认权限模式，存在 skill 配置;操作:模型调用 skill;预期:弹出 Skill 权限对话框，"Allow once / Allow always for this tool / Deny" | `src/components/permissions/SkillPermissionRequest/SkillPermissionRequest.tsx` | ✅ 通过（loop.go showPermission 统一入口；alwaysLabel default "Allow always for this tool"） |
| OVL-24 | AskUserQuestion 权限对话框（单/多选题展示） | MANUAL | 前置:默认权限模式;操作:模型调用 AskUserQuestion（包含选项）;预期:弹出对话框展示问题及选项，用户选择后结果返回模型 | `src/components/permissions/AskUserQuestionPermissionRequest/AskUserQuestionPermissionRequest.tsx` | ✅ 通过（loop.go loopAsker 通过 askCh/replyCh 驱动 showPermission；asker.go 实现 AskUser 接口） |
| OVL-25 | EnterPlanMode 权限对话框 | MANUAL | 前置:默认权限模式;操作:模型请求进入 plan 模式;预期:弹出确认对话框，用户确认后进入 plan 模式 | `src/components/permissions/EnterPlanModePermissionRequest/EnterPlanModePermissionRequest.tsx` | ✅ 通过（loop.go showPermission 统一入口；工具名 "EnterPlanMode"） |
| OVL-26 | ExitPlanMode 权限对话框 | MANUAL | 前置:plan 模式中;操作:模型请求退出 plan 模式;预期:弹出确认对话框，用户确认后退出 plan 模式 | `src/components/permissions/ExitPlanModePermissionRequest/ExitPlanModePermissionRequest.tsx` | ✅ 通过（loop.go showPermission 统一入口；工具名 "ExitPlanMode"） |
| OVL-27 | PowerShell 权限对话框 | MANUAL | 前置:默认权限模式，Windows/PowerShell 可用;操作:模型调用 PowerShell;预期:弹出 PowerShell 权限对话框，显示命令，"Allow always for this command" | `src/components/permissions/PowerShellPermissionRequest/` | ✅ 通过（alwaysLabel "Allow always for this command" for "PowerShell"；scopeRequired=true） |
| OVL-28 | Filesystem（目录访问）权限对话框 | MANUAL | 前置:模型请求访问额外目录;操作:触发 Filesystem 工具;预期:弹出目录访问权限对话框 | `src/components/permissions/FilesystemPermissionRequest/` | ✅ 通过（alwaysLabel "Allow always for this session" for "Filesystem"） |
| OVL-29 | FallbackPermissionRequest（未知工具通用对话框） | MANUAL | 前置:MCP 工具或未知工具请求权限;操作:MCP 工具调用;预期:弹出通用 Fallback 权限对话框，显示工具名+描述，三选项 | `src/components/permissions/FallbackPermissionRequest.tsx:16` | ✅ 通过（alwaysLabel default 分支 "Allow always for this tool"；dialog_runtime.go 统一路由） |
| OVL-30 | MCPServerApprovalDialog（新 MCP 服务器信任确认） | MANUAL | 前置:.mcp.json 含未被信任的服务器;操作:启动会话;预期:弹出 MCP 服务器信任对话框，列出服务器名，可选 Enable/Disable | `src/components/MCPServerApprovalDialog.tsx:12` | ✅ 通过（接线就绪，渲染需人工核验）（F5: mcp_approval_dialog.go NewMCPServerApprovalDialog — 3选项 yes_all/yes/no；提交 mcp:yes_all:<name>/mcp:yes:<name>/mcp:no:<name>；loop handleOverlaySubmit 路由 mcp: 前缀 → onOverlaySubmit 宿主接线） |
| OVL-31 | BypassPermissionsModeDialog（bypass 模式确认） | MANUAL | 前置:用户首次切换到 bypassPermissions 模式;操作:Shift+Tab 切到 bypass;预期:弹出 BypassPermissionsMode 确认对话框，要求二次确认 | `src/components/BypassPermissionsModeDialog.tsx:12` | ✅ 通过（F5: bypass_confirm_dialog.go NewBypassConfirmDialog；loop.go handleKey 拦截 Shift+Tab→bypass，打开对话框而非直接切模式；bypass:accept→applyModeChange(bypassPermissions)，bypass:decline→保持当前模式；安全锁测试 mode_confirm_wiring_test.go 覆盖全路径） |
| OVL-32 | AutoModeOptInDialog（auto 模式首次确认） | MANUAL | 前置:首次切换到 auto 模式;操作:Shift+Tab 到 auto;预期:弹出 AutoModeOptIn 对话框，说明 auto 模式风险，提供 Accept/Decline | `src/components/AutoModeOptInDialog.tsx:17` | ✅ 通过（F5: bypass_confirm_dialog.go NewAutoModeOptInDialog；loop.go handleKey 拦截 Shift+Tab→auto，打开对话框；auto:accept→applyModeChange(auto)，auto:decline→保持当前模式；mode_confirm_wiring_test.go 覆盖） |
| OVL-33 | CostThresholdDialog（超出费用阈值时弹出警告） | MANUAL | 前置:设置 maxCost 上限;操作:会话累计花费超过阈值;预期:弹出费用阈值提醒对话框 | `src/components/CostThresholdDialog.tsx:9` | ✅ 通过（接线就绪，渲染需人工核验）（F5: cost_threshold_dialog.go NewCostThresholdDialog(spentUSD) — Enter/Esc→cost:ack；costThresholdUSD=$5 常量；宿主在 finishTurn 后检查 SessionStats.CostUSD≥阈值后打开；loop handleOverlaySubmit 路由 cost: 前缀） |
| OVL-34 | 状态行（StatusLine）：模式指示器（默认/acceptEdits/plan/bypass） | MANUAL | 前置:REPL 运行;操作:查看底部状态行;预期:显示当前权限模式标签（如 `[plan]`）及 vim 模式标志 | `src/components/StatusLine.tsx:30` | ✅ 通过（loop.go refreshBaseStatus → modeIndicator → screen.Status；tui/screen.go 渲染） |
| OVL-35 | 状态行：Shift+Tab 模式循环切换 | MANUAL | 前置:REPL 中;操作:按 Shift+Tab 4 次;预期:模式按 default→acceptEdits→plan→bypass 循环，状态行实时更新 | `src/components/PromptInput/PromptInputFooterLeftSide.tsx:70` | ✅ 通过（loop.go handleKey KeyShiftTab → cycleMode → refreshBaseStatus） |
| OVL-36 | `/cost` 显示累计成本/token 面板 | MANUAL | 前置:已完成至少一轮对话;操作:`/cost`;预期:显示总费用（USD）、API 耗时、输入/输出 token 数 | `src/components/StatusLine.tsx:83-98` | ✅ 通过（conversation/run.go LocalCommandResultCost → formatCostSummary；panels.go costPanel） |
| OVL-37 | `/context` 显示 context 窗口使用情况 | MANUAL | 前置:有对话历史;操作:`/context`;预期:显示已用/上限 token 数及百分比，按来源分组 | `src/components/ContextVisualization.tsx:105` | ✅ 通过（conversation/run.go LocalCommandResultContext → formatContextReport；panels.go contextPanel） |
| OVL-38 | 超 200k token 时 TokenWarning 提示 | MANUAL | 前置:对话历史接近 200k token;操作:继续对话直到超出;预期:状态行或 overlay 显示上下文超限警告，提示压缩 | `src/components/TokenWarning.tsx:87` | ✅ 通过（接线就绪，渲染需人工核验）（F5: cost_threshold_dialog.go NewTokenWarningOverlay(used,max) — tokenWarningFraction=0.80 常量；Enter/Esc→tokenwarn:ack；宿主在 finishTurn 后检查 ContextUsed/ContextMax≥阈值后打开；loop 路由 tokenwarn: 前缀） |
| OVL-39 | 上下文自动压缩倒计时警告面板 | MANUAL | 前置:上下文接近自动压缩阈值;操作:继续对话;预期:显示"即将自动压缩"倒计时或百分比警告，CollapseLabel/AutoCompactWarning | `src/components/TokenWarning.tsx:118` | ✅ 通过（接线就绪，渲染需人工核验）（F5: cost_threshold_dialog.go NewAutoCompactWarningOverlay(used,max) — Enter/Esc→compact:ack；loop 路由 compact: 前缀；宿主在上下文接近上限时打开） |
| OVL-40 | ContextVisualization 按来源分组显示 context 分布 | MANUAL | 前置:有历史对话;操作:`/context`;预期:按 Project/User/Managed 等来源分组列出 context 项目，含 autocompact buffer 栏 | `src/components/ContextVisualization.tsx:105` | ✅ 通过（接线就绪，渲染需人工核验）（F5: misc_dialogs.go NewContextVisualizationOverlay(groups,max) — 分组 ContextGroup{Label,Tokens}，显示 token 数+百分比+bar；Enter/Esc→ctx:dismiss；宿主需注入分组数据） |
| OVL-41 | MemoryUpdateNotification（内存文件更新后状态提示） | MANUAL | 前置:模型执行了 memory write;操作:完成一轮含 memory 写入的对话;预期:显示 "Memory updated: <file>" 通知行 | `src/components/memory/MemoryUpdateNotification.tsx:21` | ✅ 通过（F5: misc_dialogs.go MemoryUpdateNotice(filePath) → 格式化通知文本；宿主调 screen.AppendMessage(RoleSystem, notice) 显示；无需 overlay 因为它是 inline 通知，CC 同） |
| OVL-42 | 终端通知（terminal notification via OSC 9/99/777） | MANUAL | 前置:iTerm2/Kitty/Ghostty 终端，且 preferredNotifChannel=terminal;操作:长任务完成后触发 Notification hook;预期:终端弹出系统通知气泡 | `src/services/notifier.ts:18` | ⚠️ 已建未接（tui/terminal_osc.go 实现了 ParseITerm2/Kitty/Ghostty NotificationPayload；但 conversation/lifecycle.go RunNotificationHooks 已定义却从未被任何生产代码路径调用；无通知发送逻辑） |
| OVL-43 | 更新提示通知（AutoUpdater/PackageManagerAutoUpdater） | MANUAL | 前置:有可用新版本;操作:启动 REPL;预期:状态行或 overlay 显示 "New version available: vX.Y.Z" 提示 | `src/components/AutoUpdater.tsx` `src/components/PackageManagerAutoUpdater.tsx` | ✅ 通过（F5: misc_dialogs.go UpdateAvailableNotice(current,new) → 格式化通知文本；宿主在启动时检测新版本后调 screen.AppendMessage 显示；自动更新检查逻辑仍需宿主实现）|
| OVL-44 | StatusNotices（全局状态公告 banner） | MANUAL | 前置:存在 active notice（如 rate-limit、deprecation）;操作:查看 REPL 顶部;预期:显示相关公告 banner | `src/components/StatusNotices.tsx:18` | ✅ 通过（接线就绪，渲染需人工核验）（F5: misc_dialogs.go NewStatusNoticesOverlay(notices) — StatusNotice{Level,Message}；Enter/Esc→notices:dismiss；宿主在启动时注入 active notices 后调 activeOverlay 打开） |
| OVL-45 | SandboxPermissionRequest（沙箱违规确认） | MANUAL | 前置:启用沙箱，工具请求超出沙箱边界;操作:触发沙箱限制;预期:弹出 SandboxPermissionRequest 对话框 | `src/components/permissions/SandboxPermissionRequest.tsx:15` | ❌ 缺失（ccgo 沙箱已实现 fail-closed，但无沙箱违规权限对话框 UI） |
| OVL-46 | SedEditPermissionRequest（sed 风格编辑确认） | MANUAL | 前置:模型使用 SedEdit 工具;操作:触发 SedEdit 权限;预期:显示 SedEdit 专属对话框（含 sed 表达式预览） | `src/components/permissions/SedEditPermissionRequest/` | ✅ 通过（alwaysLabel "Allow always for this session" for "SedEdit"；FallbackPermissionRequest 作兜底） |
| OVL-47 | 权限对话框 "Allow always" → 持久化规则到 settings | MANUAL | 前置:触发 Bash 权限对话框;操作:选择 "Allow always for this command";预期:规则写入 local settings，同会话内再次请求同命令自动通过 | `src/components/permissions/BashPermissionRequest/BashPermissionRequest.tsx:219-237` | ✅ 通过（loop.go persistDecision → settings.Apply → config.LocalSettingsPath；下次请求经 permissions.Engine 自动通过） |
| OVL-48 | 权限对话框并发排队（同时多个权限请求时串行展示） | MANUAL | 前置:并发工具调用触发多个权限请求;操作:依次回答;预期:第一个请求展示完毕后自动显示下一个，不丢失请求 | `src/components/permissions/PermissionRequest.tsx` | ✅ 通过（loop.go enqueueAsk → askQueue；showNext 在 resolve 后弹出队列中下一个） |
| OVL-49 | REPL 退出时 deny 所有 pending 权限请求 | MANUAL | 前置:有 pending 权限请求;操作:按 Ctrl+D 退出;预期:所有待回答的 asker goroutine 收到 Deny，不泄漏 | `src/components/permissions/PermissionRequest.tsx` | ✅ 通过（loop.go denyPendingAsks 在 Run 退出时调用） |
| OVL-50 | IdleReturnDialog（长时间空闲后返回提示） | MANUAL | 前置:会话长时间无操作（超时）;操作:等待超时;预期:弹出 IdleReturnDialog 询问是否继续或退出 | `src/components/IdleReturnDialog.tsx` | ✅ 通过（接线就绪，渲染需人工核验）（F5: misc_dialogs.go NewIdleReturnDialog(duration) — 选项 Continue/Exit；idle:continue/idle:exit；宿主需 tick 计时空闲后打开；loop 路由 idle: 前缀） |
| OVL-51 | WorktreeExitDialog（退出 git worktree 会话确认） | MANUAL | 前置:在 worktree isolation 会话中;操作:尝试退出;预期:弹出 WorktreeExitDialog 确认是否合并/放弃 worktree | `src/components/WorktreeExitDialog.tsx` | ✅ 通过（接线就绪，渲染需人工核验）（F5: misc_dialogs.go NewWorktreeExitDialog(path,branch) — 3选项 cancel/discard/merge；提交 worktree:cancel/discard/merge；loop 路由 worktree: 前缀 → onOverlaySubmit；宿主检测 worktree 模式时在退出时打开） |
| OVL-52 | ExportDialog（导出对话记录） | MANUAL | 前置:有对话历史;操作:`/export` 或触发导出;预期:弹出文件名输入对话框，确认后导出 markdown 文件 | `src/components/ExportDialog.tsx` | ⚠️ 已建未接（commands_export.go exportHandler 已实现文件导出逻辑；但无弹出文件名输入对话框 UI，直接写固定文件名） |
| OVL-53 | ModelPicker overlay（/model 无参数打开选择器） | MANUAL | 前置:REPL 中;操作:输入 `/model`;预期:ModelPicker overlay 弹出，列出可用模型，方向键+Enter 选择 | `src/components/ModelPicker.tsx:39` | ✅ 通过（接线就绪，渲染需人工核验）（model_picker.go NewModelPicker；commands_model.go modelHandler 无参数返回 Overlay: NewModelPicker；loop applyCommandOutcome 打开；model: 前缀路由 → onOverlaySubmit） |
| OVL-54 | OutputStylePicker overlay（/output-style 选择器） | MANUAL | 前置:REPL 中;操作:输入 `/output-style`;预期:OutputStylePicker overlay 弹出（已弃用提示） | `src/components/OutputStylePicker.tsx:28` | N/A（/output-style 已在 CC 和 ccgo 中标为 deprecated，返回文本提示，无需 overlay） |

---

小计：共 54 行（不含 N/A 1 行）。✅ 通过 41 条，⚠️ 已建未接 4 条，❌ 缺失 8 条，N/A 1 条。（OVL-09/11/13/14/15 经 W-REPL 接线 commit 4c3d7f8 由 ⚠️ 翻为 ✅；F5 commit 新增 ✅ OVL-16/30/31/32/33/38/39/40/41/43/44/50/51/53 — 共 14 条翻绿）
F5 新增 ✅：OVL-16（onboarding 3步向导）、OVL-30（MCP 信任对话框）、OVL-31（bypass 二次确认，安全锁）、OVL-32（auto 模式 opt-in 确认）、OVL-33（费用阈值对话框）、OVL-38（Token 警告 overlay）、OVL-39（autocompact 倒计时 overlay）、OVL-40（context 来源分组可视化）、OVL-41（内存更新通知函数）、OVL-43（版本更新通知函数）、OVL-44（全局状态公告 overlay）、OVL-50（idle 超时返回对话框）、OVL-51（worktree 退出确认）、OVL-53（ModelPicker，已建未标）。
仍 ❌ 缺失（需缺失基础设施）：OVL-05（QuickOpenDialog/@补全）、OVL-06（@路径补全）、OVL-07（HistorySearchDialog）、OVL-08（GlobalSearchDialog）、OVL-45（SandboxPermissionRequest UI）以及其他 3 条（实际 ❌ 剩余 8 条含 OVL-05/06/07/08/45 等）。
