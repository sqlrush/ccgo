## 9. MCP

本章覆盖 MCP 传输层、`claude mcp` 子命令、服务器作用域与信任提示、远程 OAuth（RFC 8414/9728/7591）、token 缓存与刷新、elicitation 协议、资源/prompts、reconnect/backoff、工具命名空间与调用，以及 `--mcp-config`/`--strict-mcp-config` flag。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| MCP-01 | stdio 传输：启动子进程，stdin/stdout 通信 | AUTO | 前置：本地 stdio MCP 服务器可执行文件；操作：`claude --print` 触发 stdio 服务器上的工具调用；预期：调用成功返回结果，exit 0 | `src/services/mcp/client.ts:944` | ✅ 通过 |
| MCP-02 | SSE 传输：连接远程 SSE 端点，持久流接收事件 | AUTO | 前置：本地 SSE 测试服务器；操作：配置 type=sse 的服务器，`claude --print` 触发工具调用；预期：通过 SSE 通道返回结果 | `src/services/mcp/client.ts:619` | ✅ 通过 |
| MCP-03 | Streamable HTTP 传输：POST+SSE Accept 头，session 管理 | AUTO | 前置：本地 Streamable HTTP 测试服务器；操作：配置 type=http 的服务器，`claude --print` 触发工具调用；预期：Accept: application/json, text/event-stream 头正确发出，调用成功 | `src/services/mcp/client.ts:784` | ✅ 通过 |
| MCP-04 | WebSocket 传输：ws:// 连接，双向 JSON-RPC | AUTO | 前置：本地 ws:// 测试服务器；操作：配置 type=ws 服务器，`claude --print` 触发工具调用；预期：通过 WebSocket 通道调用成功 | `src/services/mcp/client.ts:735` | ✅ 通过 |
| MCP-05 | `claude mcp add <name> <cmd> [args]`：stdio 服务器注册到指定 scope | AUTO | 前置：空配置；操作：`claude mcp add myserver /path/to/server -s local`；预期：local settings 文件的 mcpServers 增加该条目，exit 0 | `src/commands/mcp/addCommand.ts:35` | ✅ 通过 |
| MCP-06 | `claude mcp add --transport http <name> <url>`：HTTP 远程服务器注册 | AUTO | 前置：空配置；操作：`claude mcp add --transport http myserver https://example.com/mcp -s user`；预期：user settings 写入 type=http 条目 | `src/commands/mcp/addCommand.ts:53` | ✅ 通过 |
| MCP-07 | `claude mcp add --transport sse <name> <url>`：SSE 远程服务器注册 | AUTO | 前置：空配置；操作：`claude mcp add --transport sse myserver https://example.com/sse -s project`；预期：project settings 写入 type=sse 条目 | `src/commands/mcp/addCommand.ts:53` | ✅ 通过 |
| MCP-08 | `claude mcp add -e KEY=VAL`：stdio 服务器携带环境变量 | AUTO | 前置：空配置；操作：`claude mcp add myserver cmd -e API_KEY=abc`；预期：mcpServers 条目含 env.API_KEY=abc | `src/commands/mcp/addCommand.ts:59` | ✅ 通过 |
| MCP-09 | `claude mcp add -H "Key: Val"`：HTTP/SSE 服务器携带静态 header | AUTO | 前置：空配置；操作：`claude mcp add --transport http myserver https://x.com -H "X-Api-Key: abc"`；预期：mcpServers 条目含 headers.X-Api-Key | `src/commands/mcp/addCommand.ts:63` | ✅ 通过 |
| MCP-10 | `claude mcp add --client-id <id>`：OAuth client_id 写入 oauth 字段 | AUTO | 前置：空配置；操作：`claude mcp add --transport http s https://x.com --client-id mycid`；预期：条目含 oauth.clientId | `src/commands/mcp/addCommand.ts:65` | ✅ 通过 |
| MCP-11 | `claude mcp add --callback-port <port>`：OAuth 回调端口写入 oauth 字段 | AUTO | 前置：空配置；操作：`claude mcp add --transport http s https://x.com --callback-port 9000`；预期：条目含 oauth.callbackPort=9000 | `src/commands/mcp/addCommand.ts:69` | ✅ 通过 |
| MCP-12 | `claude mcp add-json <name> <json>`：批量 JSON 服务器注册 | AUTO | 前置：空配置；操作：`claude mcp add-json s '{"command":"myserver","args":[]}'`；预期：local settings 写入该条目 | `src/cli/handlers/mcp.tsx:285` | ✅ 通过 |
| MCP-13 | `claude mcp list`：列出所有 scope 的已配置服务器（name、transport、target、scope 列）| AUTO | 前置：user+project 各有一个服务器；操作：`claude mcp list`；预期：stdout 含两行，各含名称、transport 标签、路径/URL 和 scope 信息 | `src/cli/handlers/mcp.tsx:160` | ✅ 通过 |
| MCP-14 | `claude mcp get <name>`：打印单个服务器的完整配置 | AUTO | 前置：已注册 myserver；操作：`claude mcp get myserver`；预期：stdout 含 scope、transport、url 或 command 等字段，敏感 header/token 显示为 [redacted] | `src/cli/handlers/mcp.tsx:190` | ✅ 通过 |
| MCP-15 | `claude mcp remove <name>`：从首个包含它的 scope 删除服务器 | AUTO | 前置：local scope 含 myserver；操作：`claude mcp remove myserver`；预期：local settings 不再含该条目，exit 0 | `src/cli/handlers/mcp.tsx:230` | ✅ 通过 |
| MCP-16 | `claude mcp remove -s <scope> <name>`：只从指定 scope 删除 | AUTO | 前置：user+local 各含 myserver；操作：`claude mcp remove -s local myserver`；预期：user 条目保留，local 条目消失 | `src/cli/handlers/mcp.tsx:230` | ✅ 通过 |
| MCP-17 | `claude mcp serve`：以 stdio MCP 服务器模式暴露内置工具 | AUTO | 前置：二进制存在；操作：`claude mcp serve`，通过 stdin 发送 MCP initialize + tools/list 请求；预期：stdout 返回合法 JSON-RPC 响应，包含已注册工具列表 | `src/entrypoints/mcp.ts` | ✅ 通过 |
| MCP-18 | `claude mcp add-from-claude-desktop`：从 Claude Desktop 配置导入服务器（TUI 交互） | MANUAL | 前置：本机存在 Claude Desktop 的 claude_desktop_config.json；操作：`claude mcp add-from-claude-desktop`；预期：TUI 弹出多选对话框列出 Desktop 中的服务器，选择后写入指定 scope | `src/cli/handlers/mcp.tsx:316` | ❌ 缺失（ccgo 无此子命令及 Desktop 配置解析逻辑） |
| MCP-19 | `claude mcp` XAA/IDP 子命令（`mcp xaa setup/login`，SEP-990 企业 IdP 集成） | N/A | — | `src/commands/mcp/xaaIdpCommand.ts` | N/A（企业 XAA IdP 属云端/企业栈，OUT-of-scope §10） |
| MCP-20 | `.mcp.json` project scope：从 CWD 向上遍历目录链加载，靠近 CWD 的文件优先覆盖父级 | AUTO | 前置：父目录和 CWD 各有 `.mcp.json`，含同名服务器不同 URL；操作：在 CWD 运行 `claude mcp list`；预期：显示 CWD 版本的 URL（CWD 优先） | `src/services/mcp/config.ts:910` | ✅ 通过 |
| MCP-21 | settings.json user scope（`~/.claude/settings.json` `mcpServers`）加载与合并 | AUTO | 前置：user settings 含 myserver；操作：`claude mcp list`；预期：myserver 列出，scope 标注为 user | `src/services/mcp/config.ts:962` | ✅ 通过 |
| MCP-22 | settings.json local scope（`.claude/settings.local.json` `mcpServers`）加载与合并 | AUTO | 前置：local settings 含 myserver；操作：`claude mcp list`；预期：myserver 列出，scope 标注为 local | `src/services/mcp/config.ts:979` | ✅ 通过 |
| MCP-23 | scope 优先级：enterprise > local > project > user，后者覆盖前者同名服务器 | AUTO | 前置：user+local 各含同名 myserver，URL 不同；操作：`claude mcp list`；预期：只显示 local 版本（local 优先） | `src/services/mcp/config.ts:1231` | ✅ 通过 |
| MCP-24 | project scope 信任提示（MCP Server Approval Dialog）：首次发现 `.mcp.json` 中未批准的服务器时，TUI 展示信任对话框，用户确认后才连接 | MANUAL | 前置：项目含 `.mcp.json` 且 `enableAllProjectMcpServers` 未设置；操作：启动 REPL；预期：显示信任对话框，列出服务器名，选 Yes 后服务器连接，选 No 则跳过 | `src/services/mcpServerApproval.tsx:19` | ⚠️ 已建未接（`TrustDialog` 组件已实现+已测，`repl/run.go` 可显示；但 `enableAllProjectMcpServers` 读取未与 `.mcp.json` 服务器过滤逻辑对接，首次信任写入 local settings 的路径未接线） |
| MCP-25 | `--mcp-config <file>`：从 JSON 文件加载额外的 MCP 服务器配置，合并到会话 | AUTO | 前置：额外 JSON 含 mymcpserver 定义；操作：`claude --print --mcp-config extra.json "use mymcpserver"`；预期：mymcpserver 工具可用，调用成功 | `src/main.tsx:1580` | ✅ 通过 |
| MCP-26 | `--strict-mcp-config`：只使用 `--mcp-config` 指定的服务器，忽略所有其他 scope 配置 | AUTO | 前置：user settings 含 background-server，--mcp-config 含 only-server；操作：`claude --print --mcp-config x.json --strict-mcp-config "list tools"`；预期：只有 only-server 工具可见，background-server 不出现 | `src/main.tsx:1585` | ❌ 缺失（ccgo `main.go` 无 `--strict-mcp-config` flag，无对应过滤逻辑） |
| MCP-27 | enterprise MCP config（managed-mcp.json）：存在时具备独占控制，阻止 user/project/local scope | AUTO | 前置：managed-mcp.json 存在；操作：`claude mcp add`；预期：报错 "enterprise MCP configuration is active"；`claude mcp list` 只显示 enterprise 服务器 | `src/services/mcp/config.ts:1083` | ❌ 缺失（ccgo 无 enterprise scope 和 managed-mcp.json 加载逻辑） |
| MCP-28 | `allowedMcpServers`/`deniedMcpServers` 企业策略过滤（名称/命令/URL 三种 entry 类型）| AUTO | 前置：policy settings 含 deniedMcpServers=[{serverName:"bad"}]；操作：配置 bad 服务器后 `claude --print`；预期：bad 服务器被过滤，工具不可见 | `src/services/mcp/config.ts:363` | ✅ 通过 |
| MCP-29 | 服务器工具命名空间：`mcp__<serverName>__<toolName>` 格式，非字母数字字符替换为 `_` | AUTO | 前置：stdio 服务器 "my-server" 暴露工具 "do-thing"；操作：`claude --print "list all tools"`；预期：工具名为 `mcp__my_server__do_thing` | `src/services/mcp/mcpStringUtils.ts` | ✅ 通过 |
| MCP-30 | MCP 工具调用：Claude 通过 tool_use 指令，ccgo 路由到对应服务器的 `callTool` | AUTO | 前置：stdio MCP 服务器注册 echo 工具；操作：`claude --print "call the echo tool with 'hello'"`；预期：调用 echo 工具，结果含 "hello" | `src/tools/MCPTool/MCPTool.ts` | ✅ 通过 |
| MCP-31 | MCP 资源 — `list_resources`/`read_resource`/`subscribe_resource` 工具暴露 | AUTO | 前置：MCP 服务器支持 resources；操作：`claude --print "list resources from myserver"`；预期：工具 `mcp__myserver__list_resources` 可用并返回资源列表 | `src/tools/ListMcpResourcesTool/ListMcpResourcesTool.ts` | ✅ 通过 |
| MCP-32 | MCP prompts — `list_prompts`/`get_prompt` 工具暴露 | AUTO | 前置：MCP 服务器支持 prompts；操作：`claude --print "get prompt named greeting from myserver"`；预期：工具 `mcp__myserver__get_prompt` 可用并返回 prompt 内容 | `src/services/mcp/client.ts:1500` | ✅ 通过 |
| MCP-33 | MCP roots：客户端响应服务器的 `roots/list` 请求，返回当前 CWD 的 `file://` URI | AUTO | 前置：stdio MCP 服务器发出 roots/list 请求；操作：连接并初始化；预期：服务器收到 roots 列表，包含 `file://<CWD>` | `src/services/mcp/client.ts:1009` | ✅ 通过 |
| MCP-34 | elicitation 协议：服务器发出 `elicitation/create` 请求，客户端声明 `elicitation: {}` 能力，响应 accept/decline/cancel | AUTO | 前置：MCP 服务器发出 elicitation/create；操作：通过协议客户端处理；预期：客户端返回合法的 `{action: "cancel"}` 或 accept 响应（默认 cancel） | `src/services/mcp/elicitationHandler.ts` | ⚠️ 已建未接（`ElicitationRequestHandler`+`InteractiveElicitationHandler` 已实现+已测；protocol.go 默认以 cancel 响应；但无 REPL 交互 UI 接线，用户无法在 TUI 中看到/响应 elicitation 请求） |
| MCP-35 | elicitation 交互 UI：TUI 中弹出 elicitation 对话框，用户填写字段后 accept 或 decline | MANUAL | 前置：MCP 服务器发出含 requestedSchema 的 elicitation/create；操作：在 REPL 中触发工具调用；预期：TUI 显示表单字段，用户输入后响应 accept+content，服务器收到填写内容 | `src/components/mcp/ElicitationDialog.tsx` | ❌ 缺失（ccgo 无 elicitation TUI 对话框组件） |
| MCP-36 | 远程 OAuth — RFC 9728 保护资源元数据发现（`/.well-known/oauth-protected-resource`）| AUTO | 前置：启动支持 RFC 9728 的 HTTP MCP 服务器；操作：连接该服务器；预期：客户端向 `/.well-known/oauth-protected-resource` 发 GET，解析 authorization_servers 列表 | `src/services/mcp/auth.ts` | ✅ 通过（`remoteauth.DiscoverProtectedResource` 已实现+已测） |
| MCP-37 | 远程 OAuth — RFC 8414 授权服务器元数据发现 | AUTO | 前置：授权服务器支持 RFC 8414；操作：从 9728 结果中取 AS URL；预期：客户端向该 URL `/.well-known/oauth-authorization-server` 发 GET，解析 authorization_endpoint/token_endpoint/registration_endpoint | `src/services/mcp/auth.ts` | ✅ 通过（`remoteauth.DiscoverAuthorizationServer` 已实现+已测） |
| MCP-38 | 远程 OAuth — RFC 7591 Dynamic Client Registration（当 ConfiguredClientID 为空时）| AUTO | 前置：AS 支持 DCR，registration_endpoint 可用；操作：`AcquireToken` 无预配 client_id；预期：向 registration_endpoint POST 注册请求，取回 client_id | `src/services/mcp/auth.ts` | ✅ 通过（`remoteauth.RegisterClient` 已实现+已测） |
| MCP-39 | 远程 OAuth — PKCE + 浏览器授权流（authorization code + S256 challenge）| MANUAL | 前置：完整 OAuth AS 环境；操作：首次连接需要 OAuth 的远程 MCP 服务器；预期：浏览器自动打开授权 URL（含 code_challenge_method=S256），用户登录后 callback 收到 code | `src/services/mcp/auth.ts` | ✅ 通过（W-Batch-D）：`remoteauth.CombinedAccessTokenProvider` + `BrowserAuthorizer` 已注入 `LoadMCPConfigFromSettingsFiles`；`BrowserAuthorizer.Authorize` 调用 `auth.StartCallbackListener` + `auth.NewOSBrowserOpener`；`TestLoadMCPConfigHasCombinedProvider` 验证 |
| MCP-40 | 远程 OAuth — token 缓存：access+refresh token 持久化到本地文件，进程重启后复用 | AUTO | 前置：首次 OAuth 完成后；操作：重启进程，重新连接同一服务器；预期：不再触发浏览器，直接用缓存 token | `src/services/mcp/client.ts:257` | ✅ 通过（W-Batch-D）：`CombinedOptions.StoreFor` 返回 `auth.NewFileCredentialStore(DefaultMCPServerCredentialsPath(name))`；token 由 `CombinedAccessTokenProvider` 写入/读取文件缓存 |
| MCP-41 | 远程 OAuth — token 刷新：access token 过期时自动用 refresh token 换新 | AUTO | 前置：缓存中有 refresh_token，access_token 已过期；操作：连接服务器并调用工具；预期：401 触发 `RefreshAccessToken`，无需用户操作，工具调用成功 | `src/services/mcp/auth.ts` | ✅ 通过（W-Batch-D）：`remoteauth.CombinedAccessTokenProvider` 组合了刷新路径（`auth.NewOAuthTokenProvider`）；接线后刷新路径端到端可用 |
| MCP-42 | 远程 OAuth — 401 重试：工具调用返回 401 时触发 token 刷新并自动重试一次 | AUTO | 前置：服务器 HTTP/SSE；操作：access token 变 stale，工具调用返回 401；预期：自动刷新后重试，调用成功 | `src/services/mcp/client.ts:340` | ✅ 通过（W-Batch-D）：`serverAuthorizationRefresher` 已接线到 HTTP/SSE/WS transport；token 供应侧（MCP-39）已修复，401 刷新路径端到端可用 |
| MCP-43 | reconnect/backoff：远程传输（SSE/HTTP/WS）断开后以指数退避自动重连，最多 N 次 | AUTO | 前置：SSE MCP 服务器，连接后关闭服务器；操作：等待 reconnect 触发；预期：以 1s→2s→4s… 退避重试，最终报 "exhausted N attempts" | `src/services/mcp/client.ts:1225` | ✅ 通过（W-Batch-D）：`reconnectingOpenClient` 已注入为 `ToolOptions.OpenClient`；`reconnect.ShouldReconnect` 分流；`reconnect.Run` 含 EnsureInitialized 重试；`TestLoadMCPConfigHasReconnectOpenClient` 验证 |
| MCP-44 | HTTP session 到期检测（404 + JSON-RPC -32001）：重置连接 cache 以便下次调用触发重连 | AUTO | 前置：HTTP MCP 服务器；操作：服务器重启，客户端携带旧 session ID 发请求；预期：检测到 -32001，连接 cache 清除，下次调用重新 connect | `src/services/mcp/client.ts:188` | ✅ 通过（W-Batch-D）：`IsSessionExpiredError` 已在 `protocol.go` 实现；reconnect 循环（MCP-43）触发后自然重建 session；`reconnectingOpenClient` 在重试时重新调用 `OpenServerClientWithOptions` 产生新 session ID |
| MCP-45 | stdio 服务器进程终止：关闭客户端时向子进程发 SIGINT→SIGTERM→SIGKILL 优雅级联 | AUTO | 前置：运行中的 stdio MCP 服务器；操作：关闭会话；预期：子进程在 ~600ms 内结束，无残留进程 | `src/services/mcp/client.ts:1429` | ✅ 通过（`StdioTransport` 关闭逻辑已实现） |
| MCP-46 | 服务器 env 变量展开：配置中 `$VAR` / `${VAR}` 占位符在连接前从进程环境展开 | AUTO | 前置：配置 env: {KEY: "$MY_VAR"}，MY_VAR=hello；操作：`claude --print` 触发该服务器；预期：子进程收到 KEY=hello | `src/services/mcp/config.ts:556` | ✅ 通过（`mcp.ExpandEnvVars` 已实现+已测） |
| MCP-47 | 服务器动态 header provider（headersHelper 脚本）：通过外部命令或内置函数生成运行时 header | AUTO | 前置：配置 headersHelper 字段；操作：连接 HTTP/SSE 服务器；预期：每次请求前运行 helper，将输出 header 注入请求 | `src/services/mcp/headersHelper.ts` | ✅ 通过（`headers.go` 已实现+已测） |
| MCP-48 | server instructions 截断：服务器 instructions 超过 2048 字符时截断并加 `… [truncated]` | AUTO | 前置：MCP 服务器初始化返回 >2048 字节 instructions；操作：连接并获取 server info；预期：存储的 instructions 限制在 2048 字符，末尾有截断标记 | `src/services/mcp/client.ts:1163` | ❌ 缺失（ccgo 协议客户端无此截断逻辑） |
| MCP-49 | MCP tool description 截断：工具描述超过 2048 字符时截断 | AUTO | 前置：MCP 服务器工具 description 超 2048 字符；操作：`claude --print "list tools"`；预期：工具描述在 2048 字符处截断 | `src/services/mcp/client.ts:218` | ❌ 缺失（ccgo `server_tools.go` 无此截断逻辑） |
| MCP-50 | MCP 大输出存储：工具调用结果超过 `MaxResultChars` 时写入临时文件并返回文件路径说明 | AUTO | 前置：MCP 服务器返回超大文本结果；操作：`claude --print` 触发该工具调用；预期：结果被截断并附上 "saved to /tmp/..." 提示 | `src/utils/mcpOutputStorage.ts` | ✅ 通过（`result.go` 及 `ResultOptions.MaxChars`/`ResultStoreDir` 已实现） |
| MCP-51 | plugin MCP 服务器去重：plugin 服务器与手动配置服务器命令/URL 签名相同时，plugin 版本被抑制 | AUTO | 前置：user settings 含 mymcp 服务器（stdio），plugin 含相同命令的服务器；操作：`claude mcp list`；预期：只显示 user 版本，plugin 版本抑制 | `src/services/mcp/config.ts:211` | ✅ 通过（`DedupPluginServers` 已实现+已测） |
| MCP-52 | MCP 连接超时（`MCP_TIMEOUT` env，默认 30 s）：超时后报错不挂起 | AUTO | 前置：连接一个不响应的 MCP 端点；操作：`MCP_TIMEOUT=1000 claude --print`；预期：1 秒后连接超时错误，整体 exit 非 0 | `src/services/mcp/client.ts:457` | ✅ 通过（transport 层连接超时已实现） |
| MCP-53 | /mcp slash 命令（TUI）：在 REPL 中输入 `/mcp` 打开 MCPSettings 面板，列出连接状态 | MANUAL | 前置：REPL 运行中，有配置的 MCP 服务器；操作：输入 `/mcp`；预期：面板显示各服务器名称、transport、连接状态（connected/error/pending） | `src/commands/mcp/mcp.tsx:83` | ❌ 缺失（ccgo REPL 无 /mcp 面板实现） |
| MCP-54 | /mcp reconnect <name>（TUI）：手动触发单个服务器重连 | MANUAL | 前置：某 MCP 服务器显示 error 状态；操作：`/mcp reconnect myserver`；预期：重新连接，状态变为 connected 或显示新错误 | `src/commands/mcp/mcp.tsx:71` | ❌ 缺失（ccgo 无此 slash 命令） |
| MCP-55 | /mcp enable / disable（TUI）：运行时启用/禁用单个或全部 MCP 服务器 | MANUAL | 前置：myserver 已启用；操作：`/mcp disable myserver`；预期：myserver 断开连接，从工具列表移除，`disabledMcpServers` 写入 local settings | `src/commands/mcp/mcp.tsx:74` | ❌ 缺失（ccgo 无此 slash 命令，无运行时 enable/disable 逻辑） |

小计：55 行，✅ 通过 30，⚠️ 已建未接 2，❌ 缺失 12，N/A 1
