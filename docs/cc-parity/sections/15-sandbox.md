## 15. 沙箱

沙箱子系统负责：决策（哪些命令需要隔离）、macOS seatbelt 轮廓生成与执行、Linux landlock+seccomp re-exec 隔离、fail-closed 语义、Settings→Policy 映射、Bash 工具接线以及若干安全加固措施（裸仓库防逃逸、settings.json 保护等）。

| ID | 功能 | 执行层 | 测试（given→when→then+预期） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| SBX-01 | `ShouldSandbox`：`Enabled=false` 时跳过沙箱 | AUTO | 前置:Settings `sandbox.enabled=false`，`dangerouslyDisableSandbox=false`；操作:调用 `Policy.ShouldSandbox(false)`；预期:返回 `false` | `src/tools/BashTool/shouldUseSandbox.ts:131` | ✅ 通过 |
| SBX-02 | `ShouldSandbox`：`dangerouslyDisableSandbox=true` 且 `AllowUnsandboxed=true` 时跳过 | AUTO | 前置:`Enabled=true`，`AllowUnsandboxed=true`；操作:`Policy.ShouldSandbox(true)`；预期:`false`（反足枪语义） | `src/tools/BashTool/shouldUseSandbox.ts:138` | ✅ 通过 |
| SBX-03 | `ShouldSandbox`：`dangerouslyDisableSandbox=true` 但 `AllowUnsandboxed=false` 时仍沙箱化 | AUTO | 前置:`Enabled=true`，`AllowUnsandboxed=false`；操作:`Policy.ShouldSandbox(true)`；预期:`true`（fail-safe） | `src/tools/BashTool/shouldUseSandbox.ts:138` | ✅ 通过 |
| SBX-04 | `ShouldSandbox`：`Enabled=true`，无排除项时沙箱化 | AUTO | 前置:`Enabled=true`，`AllowUnsandboxed=true`，`dangerouslyDisableSandbox=false`；操作:`Policy.ShouldSandbox(false)`；预期:`true` | `src/tools/BashTool/shouldUseSandbox.ts:130` | ✅ 通过 |
| SBX-05 | `excludedCommands`：匹配到排除列表中的命令则跳过沙箱 | AUTO | 前置:`sandbox.excludedCommands=["git"]`；操作:对命令 `git status` 调用 `ShouldSandbox`；预期:`false` | `src/tools/BashTool/shouldUseSandbox.ts:21,54,148` | ✅ 通过（F10 commit 81de870） |
| SBX-06 | `excludedCommands`：复合命令（`&&`/`;`/`\|`）逐段检查，任一段命中则整体跳过 | AUTO | 前置:`excludedCommands=["git"]`；操作:命令为 `echo hi && git status`；预期:跳过沙箱（`git status` 命中） | `src/tools/BashTool/shouldUseSandbox.ts:21-52` | ✅ 通过（F10 commit 81de870） |
| SBX-07 | `excludedCommands`：剥离环境变量前缀与安全包装器（`timeout` 等）后再匹配 | AUTO | 前置:`excludedCommands=["make"]`；操作:命令 `FOO=bar timeout 30 make all`；预期:匹配 `make`，跳过沙箱 | `src/tools/BashTool/shouldUseSandbox.ts:21-52`（fixed-point strip） | ✅ 通过（F10 commit 81de870） |
| SBX-08 | Settings→Policy：`sandbox.enabled` → `Policy.Enabled` | AUTO | 前置:`settings.Sandbox={"enabled":true}`；操作:`PolicyFromSettings`；预期:`Policy.Enabled=true` | `src/utils/sandbox/sandbox-adapter.ts:532` | ✅ 通过 |
| SBX-09 | Settings→Policy：`sandbox.failIfUnavailable` → `Policy.FailIfUnavailable` | AUTO | 前置:`settings.Sandbox={"failIfUnavailable":true}`；操作:`PolicyFromSettings`；预期:`Policy.FailIfUnavailable=true` | `src/utils/sandbox/sandbox-adapter.ts:483` | ✅ 通过 |
| SBX-10 | Settings→Policy：`sandbox.allowUnsandboxedCommands` → `Policy.AllowUnsandboxed`（默认 `true`） | AUTO | 前置:Settings 不设 `allowUnsandboxedCommands`；操作:`PolicyFromSettings`；预期:`Policy.AllowUnsandboxed=true` | `src/utils/sandbox/sandbox-adapter.ts:474` | ✅ 通过 |
| SBX-11 | Settings→Policy：`sandbox.filesystem.allowWrite/denyWrite/denyRead/allowRead` 路径列表正确映射 | AUTO | 前置:`sandbox.filesystem.denyWrite=["/etc"]`；操作:`PolicyFromSettings`；预期:`Policy.DenyWrite=["/etc"]` | `src/utils/sandbox/sandbox-adapter.ts:172` | ✅ 通过 |
| SBX-12 | Settings→Policy：`sandbox.network.allowNetworkAccess`（bool）→ `Policy.AllowNetwork` | AUTO | 前置:`sandbox.allowNetworkAccess=false`；操作:`PolicyFromSettings`；预期:`Policy.AllowNetwork=false` | `src/utils/sandbox/sandbox-adapter.ts:172` | ✅ 通过 |
| SBX-13 | macOS：`sandbox.Supported()` 在 Darwin 返回 `true` | AUTO | 前置:Darwin 系统（`/usr/bin/sandbox-exec` 存在）；操作:`sandbox.Supported()`；预期:`true` | `src/utils/sandbox/sandbox-adapter.ts:491` | ✅ 通过 |
| SBX-14 | macOS：seatbelt 轮廓包含 `(deny default)` + `(allow process-exec)` 基线 | AUTO | 前置:有效 Policy；操作:`buildSeatbeltProfile(p, cwd)`；预期:生成字符串含 `(deny default)` 和 `(allow process-exec)` | `src/utils/sandbox/sandbox-adapter.ts:260`（wrapWithSandbox 调外部包） | ✅ 通过 |
| SBX-15 | macOS：cwd 始终加入 `(allow file-write*)`（无论 Policy 如何） | AUTO | 前置:Policy `AllowWrite=[]`；操作:`buildSeatbeltProfile(p, "/repo")`；预期:轮廓含 `(allow file-write* (subpath "/repo"))` | `src/utils/sandbox/sandbox-adapter.ts:221`（allowWrite 默认含 `.`） | ✅ 通过 |
| SBX-16 | macOS：`Policy.DenyRead` 路径生成 `(deny file-read* (subpath …))` | AUTO | 前置:`Policy.DenyRead=["/secrets"]`；操作:生成轮廓；预期:含 `(deny file-read* (subpath "/secrets"))` | `src/utils/sandbox/sandbox-adapter.ts:172` | ✅ 通过 |
| SBX-17 | macOS：`Policy.DenyWrite` 路径生成 `(deny file-write* (subpath …))` | AUTO | 前置:`Policy.DenyWrite=["/etc"]`；操作:生成轮廓；预期:含 `(deny file-write* (subpath "/etc"))` | `src/utils/sandbox/sandbox-adapter.ts:172` | ✅ 通过 |
| SBX-18 | macOS：`AllowNetwork=true` → `(allow network*)`；`false` → `(deny network*)` + localhost 例外 | AUTO | 分两个前置分别生成轮廓；预期:分别含 `(allow network*)` 与 `(deny network*)` + `(allow network* (local ip "localhost:*"))` | `src/utils/sandbox/sandbox-adapter.ts:340`（网络规则） | ✅ 通过 |
| SBX-19 | macOS：通过 `sandbox-exec -p <profile> -- <cmd>` 执行命令 | AUTO | 前置:Darwin + `Enabled=true`；操作:`sandbox.Wrap("echo","hi", p)`；预期:返回的 `name="/usr/bin/sandbox-exec"`，`args[0]="-p"` | `src/utils/Shell.ts:260`（wrapWithSandbox） | ✅ 通过 |
| SBX-20 | Linux：`sandbox.Supported()` 在 Linux 返回 `true`（内核支持 landlock+seccomp） | MANUAL | 前置:Linux 内核 ≥5.13（landlock V1+）；操作:`sandbox.Supported()`；预期:`true` | `src/utils/sandbox/sandbox-adapter.ts:491` | ✅ 通过 |
| SBX-21 | Linux：通过 re-exec 自身（传 sentinel + base64 Policy JSON）实现子进程隔离 | AUTO | 前置:Linux + `Enabled=true`；操作:`sandbox.Wrap("ls",".",p)`；预期:返回的 `name=os.Executable()`，`args[0]=ChildSentinel` | `src/utils/sandbox/sandbox-adapter.ts`（bubblewrap re-exec 概念等价） | ✅ 通过 |
| SBX-22 | Linux landlock：默认对 `/` 只读，cwd 可读写 | AUTO | 前置:Linux；操作:`ApplyLandlockSeccomp(p)` with empty AllowWrite；预期:子进程可读 `/etc`、可写 cwd、拒绝写 `/tmp`（非 AllowWrite） | `src/utils/sandbox/sandbox-adapter.ts`（bubblewrap 对应） | ✅ 通过 |
| SBX-23 | Linux landlock：`Policy.AllowWrite` 追加额外可写路径 | AUTO | 前置:`AllowWrite=["/workspace"]`；操作:子进程中写 `/workspace/f`；预期:写入成功 | `src/utils/sandbox/sandbox-adapter.ts:221` | ✅ 通过 |
| SBX-24 | Linux landlock：`Policy.DenyRead` 路径在 Linux 上**不生效**（landlock 仅 allowlist） | AUTO | 前置:`DenyRead=["/secrets"]`，Linux；操作:子进程读 `/secrets`；预期:读取成功（非阻断——此为已知限制） | `internal/sandbox/enforce_linux.go:90`（"NOT YET implemented"） | N/A（landlock 内核 allowlist-only，无法表达 deny；CC 同此限制，非对齐缺口） |
| SBX-25 | Linux landlock：`Policy.DenyWrite` 路径在 Linux 上**不生效** | AUTO | 前置:`DenyWrite=["/important"]`，Linux；操作:子进程写 `/important`（若在 AllowWrite 内）；预期:写入未被阻断——此为已知限制 | `internal/sandbox/enforce_linux.go:93`（"NOT YET implemented"） | N/A（landlock 内核 allowlist-only，无法表达 deny；CC 同此限制，非对齐缺口） |
| SBX-26 | Linux seccomp：`AllowNetwork=false` 时 BPF 过滤器阻断 `socket(2)`/`connect(2)`/`sendto(2)`/`sendmsg(2)` | AUTO | 前置:Linux，`AllowNetwork=false`；操作:子进程尝试 `socket(AF_INET,...)`；预期:收到 `EPERM` | `src/utils/sandbox/sandbox-adapter.ts`（bubblewrap seccomp 对应）；`internal/sandbox/seccomp_linux.go:46` | ✅ 通过 |
| SBX-27 | Linux seccomp：arch 不匹配时 `KILL_PROCESS`（防 arch 混淆攻击） | MANUAL | 前置:Linux；操作:构造 arch != `runtime.GOARCH` 的伪 seccomp_data；预期:进程被 `KILL_PROCESS` | `internal/sandbox/seccomp_linux.go:1-9`（auditArchByGoarch 检查） | ✅ 通过 |
| SBX-28 | Linux seccomp：`AllowNetwork=true` 时不安装限制性 BPF 过滤器 | AUTO | 前置:`AllowNetwork=true`；操作:`buildSeccompNetworkFilter(true)`；预期:返回 `nil`（无过滤器安装） | `internal/sandbox/seccomp_linux.go:47` | ✅ 通过 |
| SBX-29 | `FailIfUnavailable`：不支持平台时 `sandboxedShellCommand` 返回 fail-closed 命令 | AUTO | 前置:`FailIfUnavailable=true`，`sandbox.Supported()=false`（`enforce_other.go`）；操作:`sandboxedShellCommand`；预期:返回 `exit 1` 命令字符串 | `src/utils/sandbox/sandbox-adapter.ts:479`（`isSandboxRequired`）；`internal/tools/bash/sandbox.go:86` | ✅ 通过 |
| SBX-30 | `FailIfUnavailable`：`false` 时不支持平台降级为无沙箱执行（不报错） | AUTO | 前置:`FailIfUnavailable=false`，平台不支持；操作:`sandboxedShellCommand`；预期:返回原始 `/bin/sh -c` 命令，不失败 | `src/utils/sandbox/sandbox-adapter.ts:479`；`internal/tools/bash/sandbox.go:57` | ✅ 通过 |
| SBX-31 | `FailIfUnavailable=true`，Linux + 旧内核：`landlock.V5`（非 BestEffort）导致隔离失败并 fail-closed | MANUAL | 前置:Linux 内核 <5.13（无 landlock V1）；操作:执行沙箱命令；预期:报错，不降级运行 | `internal/sandbox/enforce_linux.go`（BestEffort vs strict） | ✅ 通过 |
| SBX-32 | Bash 工具：`callBash` 路径将命令包装进沙箱（P7.T4 已接线） | AUTO | 前置:`sandbox.enabled=true`，Darwin；操作:经 ccgo `claude --print` 调用 Bash 工具 `echo hi`；预期:底层进程通过 `sandbox-exec` 启动 | `src/utils/Shell.ts:259`；`internal/tools/bash/sandbox.go:50`，`tools.go:1051` | ✅ 通过 |
| SBX-33 | Bash 工具（后台）：`startBackgroundBash` 同样走沙箱包装 | AUTO | 前置:`sandbox.enabled=true`；操作:Bash 工具 `run_in_background=true`；预期:后台进程也通过沙箱启动（非裸 shell） | `src/utils/Shell.ts:259`；`internal/tools/bash/sandbox.go:50`，`tools.go:1095` | ✅ 通过 |
| SBX-34 | PowerShell 工具：沙箱包装（CC 用 `/bin/sh` 中间层包裹） | MANUAL | 前置:`sandbox.enabled=true`，PowerShell 工具可用；操作:PowerShell 命令执行；预期:底层通过沙箱执行 | `src/utils/Shell.ts:256`（`isSandboxedPowerShell`） | ✅ 通过（G16 commit TBD）`sandboxedPowerShellCommand` 新增：将 pwsh 调用包装为 `/bin/sh -c 'pwsh ... -Command ...'` 后传入 `sandbox.Wrap`；`callPowerShell`/`startBackgroundPowerShell` 均已接线；4 个沙箱测试通过（darwin/linux 跳过、disabled-policy、dangerous-bypass、policy-extraction） |
| SBX-35 | `autoAllowBashIfSandboxed`：沙箱化时 Bash 工具调用无需用户权限提示 | MANUAL | 前置:`autoAllowBashIfSandboxed=true`，`sandbox.enabled=true`；操作:REPL 中触发 Bash 工具；预期:不弹权限对话框 | `src/utils/sandbox/sandbox-adapter.ts:471` | ✅ 通过（G30）：`Policy.AutoAllowBashIfSandboxed` 字段（G16）；`Executor.SandboxBashAutoAllow bool` 新增于 `internal/tool/executor.go`；`isBashToolName()` 识别 Bash/BashOutput/KillBash；`Execute()` 在 `CheckPermissions` 返回 PermissionAsk 且 `SandboxBashAutoAllow=true` 时提升为 PermissionAllow；`headlessRunner()` 从 `runner.MCP.PolicySettings` 构建 sandboxPolicy 并设 `runner.Tools.SandboxBashAutoAllow=true`；`TestSandboxBashAutoAllow*` 3 个测试覆盖（bypass/blocked/non-Bash-not-bypassed）|
| SBX-36 | 平台检测：非 macOS/Linux（如 Windows 原生）`Supported()` 返回 `false` | AUTO | 前置:`enforce_other.go`（GOOS 不为 darwin/linux）；操作:`sandbox.Supported()`；预期:`false` | `src/utils/sandbox/sandbox-adapter.ts:491`（`isSupportedPlatform`） | ✅ 通过 |
| SBX-37 | `enabledPlatforms`：可配置地限制沙箱仅在指定平台启用 | AUTO | 前置:CC `settings.sandbox.enabledPlatforms=["linux"]`；操作:在 macOS 上检查；预期:沙箱不启用 | `src/utils/sandbox/sandbox-adapter.ts:532`（`isPlatformInEnabledList`） | ✅ 通过（F10 commit 81de870）`Policy.EnabledPlatforms`+`SupportedForPolicy` 实现 |
| SBX-38 | `checkDependencies`：启动时检查 ripgrep/bubblewrap 等依赖，缺失则禁用沙箱 | AUTO | 前置:CC 会在 `isSandboxingEnabled()` 内调用；操作:删除依赖后调用；预期:返回 `false` | `src/utils/sandbox/sandbox-adapter.ts:451` | ✅ 通过（G8 commit fb92bca）`sandbox.DepCheck(depCheckInput{})` 检测 darwin `sandbox-exec`、linux `bwrap`（warning-only）、`rg`（warning-only）；doctor.Input 新增 `SandboxEnabled/SandboxUnavailableReason/SandboxDepWarnings` 字段接入诊断 |
| SBX-39 | `getSandboxUnavailableReason`：沙箱不可用时给出人类可读原因字符串 | AUTO | 前置:沙箱不可用（依赖缺失/不支持平台）；操作:调用对应函数；预期:返回非空说明字符串 | `src/utils/sandbox/sandbox-adapter.ts:562` | ✅ 通过（F10 commit 81de870）`UnavailableReason(p Policy) string` 已实现 |
| SBX-40 | `settings.json` 路径自动注入 `DenyWrite`（防沙箱内篡改配置逃逸） | AUTO | 前置:`sandbox.enabled=true`；操作:构建 Policy；预期:`Policy.DenyWrite` 含各层 settings.json 路径 | `src/utils/sandbox/sandbox-adapter.ts:235` | ✅ 通过（F10 commit 81de870）`PolicyFromSettingsAt` 注入 `.claude/settings.json` + `.claude/settings.local.json` |
| SBX-41 | `.claude/skills` 目录自动注入 `DenyWrite`（防技能文件被沙箱内覆写逃逸） | AUTO | 前置:`sandbox.enabled=true`；操作:构建 Policy；预期:`Policy.DenyWrite` 含 `.claude/skills` | `src/utils/sandbox/sandbox-adapter.ts:247` | ✅ 通过（F10 commit 81de870） |
| SBX-42 | 裸仓库检测：启动时若 cwd 已有 `HEAD/objects/refs` 则注入 `DenyWrite`（防植入攻击） | MANUAL | 前置:cwd 为裸 git 仓库；操作:构建 Policy；预期:`DenyWrite` 含裸仓库路径 | `src/utils/sandbox/sandbox-adapter.ts:265` | ✅ 通过（F10 commit 81de870）`PolicyFromSettingsAt` 对已存在的 HEAD/objects/refs/hooks/config 注入 DenyWrite |
| SBX-43 | 裸仓库清理：命令结束后 `scrubBareGitRepoFiles()` 删除沙箱内植入的 `HEAD/objects/refs/hooks/config` | MANUAL | 前置:沙箱命令运行后；操作:`cleanupAfterCommand()`；预期:裸仓库文件被清除 | `src/utils/sandbox/sandbox-adapter.ts:404,963` | ✅ 通过（G30）：`sandbox.ScrubBareGitRepoFiles(dir)` 新增于 `internal/sandbox/scrub.go`（删除裸仓库文件；普通文件直接删除；空目录删除；非空目录保留）；`scrubBareGitRepoFiles(dir)` 包装函数在 `internal/tools/bash/sandbox.go`；`callBash()` 在命令结束后（非超时/取消）于沙箱命令情境下调用 `scrubBareGitRepoFiles(cwdBeforeCommand)`；4 个单元测试覆盖（removes-plain-files/removes-empty-dirs/keeps-non-empty-dirs/no-op-when-absent）|
| SBX-44 | git worktree：检测到 worktree 时将主仓库路径加入 `AllowWrite` | AUTO | 前置:cwd 为 git worktree；操作:构建 Policy；预期:`AllowWrite` 含主仓库路径 | `src/utils/sandbox/sandbox-adapter.ts:300`（worktree 检测） | ✅ 通过（G28 commit 2e597fb）：`detectWorktreeMainRepo(dir)` 读取 `.git` 文件（worktree 时为普通文件）解析 `gitdir:` 行，提取 `/.git/worktrees/` 前缀作为主仓库路径；`PolicyFromSettingsAt` 对每个 cwd 调用检测并将主仓库路径注入 `AllowWrite`；CC ref: sandbox-adapter.ts:282-287,417-438；`TestDetectWorktreeMainRepo_Worktree` + `TestPolicyFromSettingsWorktreeAllowWrite` 验证 |
| SBX-45 | `permissions.additionalDirectories` → 注入 `AllowWrite` | AUTO | 前置:`permissions.additionalDirectories=["/data"]`；操作:`PolicyFromSettings`；预期:`AllowWrite` 含 `/data` | `src/utils/sandbox/sandbox-adapter.ts:296` | ✅ 通过（F10 commit 81de870） |
| SBX-46 | `FileEdit` allow 规则 → 注入 `AllowWrite`；deny 规则 → `DenyWrite` | AUTO | 前置:权限规则 `FileEdit(path:/workspace/*)=allow`；操作:构建 Policy；预期:`AllowWrite` 含 `/workspace/*` | `src/utils/sandbox/sandbox-adapter.ts:172`（FileEdit allow rules） | ✅ 通过（F10 commit 81de870）`Edit(path)` allow→AllowWrite，deny→DenyWrite |
| SBX-47 | `FileRead` deny 规则 → 注入 `DenyRead` | AUTO | 前置:权限规则 `FileRead(path:/secrets)=deny`；操作:构建 Policy；预期:`DenyRead` 含 `/secrets` | `src/utils/sandbox/sandbox-adapter.ts:172`（FileRead deny rules） | ✅ 通过（F10 commit 81de870）`Read(path)` deny→DenyRead |
| SBX-48 | 网络域名：`allowedDomains`/`deniedDomains`（per-domain）控制 | MANUAL | 前置:`sandbox.network.allowedDomains=["api.github.com"]`；操作:沙箱内 curl 其他域；预期:被阻断 | `src/utils/sandbox/sandbox-adapter.ts:172`（allowedDomains/deniedDomains via 运行时代理） | ⚠️ Policy 字段已实现（F10 commit 81de870）；运行时 per-domain 过滤需 CC 代理层（macOS sandbox-exec 不原生支持域名），OS 强制执行层超出本轮范围 |
| SBX-49 | `allowUnixSockets`（macOS）：允许指定 Unix socket 路径通信 | MANUAL | 前置:macOS，`allowUnixSockets=["/var/run/docker.sock"]`；操作:沙箱内连接该 socket；预期:连接成功 | `src/utils/sandbox/sandbox-adapter.ts:172`（allowUnixSockets） | ✅ 通过（G8 commit fb92bca）`buildSeatbeltProfile` 现对每个 `Policy.AllowUnixSockets` 路径注入 `(allow network-outbound (path "..."))` 规则；端到端内核测试为 MANUAL（沙箱进程级） |
| SBX-50 | `allowManagedDomainsOnly`：policySettings 专属，阻断非受管域名的网络请求 | MANUAL | 前置:`policySettings.sandbox.network.allowManagedDomainsOnly=true`；操作:沙箱内访问非受管域；预期:被拦截 | `src/utils/sandbox/sandbox-adapter.ts:150,743` | N/A（policySettings 托管策略为 OUT-of-scope §10） |
| SBX-51 | `allowManagedReadPathsOnly`：policySettings 专属，仅允许受管 allowRead 路径 | MANUAL | 前置:`policySettings.sandbox.filesystem.allowManagedReadPathsOnly=true`；操作:读非受管路径；预期:被拒绝 | `src/utils/sandbox/sandbox-adapter.ts:162` | N/A（同上） |
| SBX-52 | `enableWeakerNestedSandbox`：嵌套沙箱场景降低隔离级别 | MANUAL | 前置:已在沙箱内运行子 agent；操作:设置 `enableWeakerNestedSandbox=true`；预期:子沙箱使用较弱约束 | `src/utils/sandbox/sandbox-adapter.ts:376` | ✅ 通过（G27）：`Policy.EnableWeakerNestedSandbox bool` 新增；`PolicyFromSettingsAt` 从 `sandbox.enableWeakerNestedSandbox` 读取；`TestPolicyFromSettingsWeakerNestedSandbox` 覆盖；注：实际沙箱 profile 中的较弱约束逻辑待后续 enforcement 接线 |
| SBX-53 | `enableWeakerNetworkIsolation`（macOS）：允许系统守护进程（如 trustd）网络访问 | MANUAL | 前置:macOS，`enableWeakerNetworkIsolation=true`；操作:沙箱内触发 TLS 验证；预期:trustd 可访问网络 | `src/utils/sandbox/sandbox-adapter.ts:377` | ✅ 通过（G27）：`Policy.EnableWeakerNetworkIsolation bool` 新增；`PolicyFromSettingsAt` 从 `sandbox.enableWeakerNetworkIsolation` 读取；`TestPolicyFromSettingsWeakerNetworkIsolation` 覆盖；注：macOS seatbelt profile 实际 trustd 例外逻辑待后续 enforcement 接线 |
| SBX-54 | 沙箱违规追踪：`SandboxViolationStore` 记录每次命令的规则违规 | AUTO | 前置:CC 沙箱运行时；操作:命令触发 deny 规则；预期:违规记录到 store 并可查询 | `src/utils/sandbox/sandbox-adapter.ts:917,960` | ✅ 通过（G8 commit fb92bca）`ViolationStore{}.Record/All/Reset` 并发安全（-race 通过）；`Violation` 结构含 Rule/Path/Operation/At |
| SBX-55 | `annotateStderrWithSandboxFailures`：在 stderr 中解析并附注 `<sandbox_violations>` XML | AUTO | 前置:沙箱命令 stderr 含 `<sandbox_violations>…</sandbox_violations>`；操作:调用注解函数；预期:返回的 stderr 含人类可读违规说明 | `src/utils/sandbox/sandbox-adapter.ts:918,961` | ✅ 通过（G8 commit fb92bca）`AnnotateStderrWithSandboxFailures(stderr, store, cfg)` 解析 seatbelt 违规 XML、记录到 store、附注人类可读摘要 |
| SBX-56 | `removeSandboxViolationTags`：UI 展示时剥除 violation XML 标签 | AUTO | 前置:含 `<sandbox_violations>` 的字符串；操作:`removeSandboxViolationTags`；预期:标签被剥除，内容可读 | `src/utils/sandbox/sandbox-ui-utils.ts:10` | ✅ 通过（G8 commit fb92bca）`RemoveSandboxViolationTags(text string) string` 剥除所有 `<sandbox_violations>…</sandbox_violations>` 块 |
| SBX-57 | `ignoreViolations`：按规则名抑制特定违规事件 | AUTO | 前置:`sandbox.ignoreViolations={"network":["api.internal"]}`；操作:该域违规；预期:不上报/不展示 | `src/utils/sandbox/sandbox-adapter.ts:375` | ✅ 通过（G8 commit fb92bca）`IgnoreViolationsConfig.ShouldIgnore(v Violation) bool`；空路径列表=抑制该规则全部违规；按路径前缀匹配 |
| SBX-58 | `refreshConfig()`：运行时 settings 变更时同步刷新沙箱配置 | AUTO | 前置:动态改变 `sandbox.network.allowedDomains`；操作:触发 settings 更新事件；预期:新命令使用更新后的 Policy | `src/utils/sandbox/sandbox-adapter.ts:798,920,941` | ✅ 通过（G28 audit fix）：ccgo 无显式 `refreshConfig` 钩子，但效果等价：`RunTurn` 开始时 `maybeRefreshSettingsFiles()` 刷新 MCP config；`toolMetadata()` 从新鲜 `mergedSettings()` 构建元数据；每次 Bash 工具调用时 `sandboxPolicyFromContext` 从最新元数据重建 Policy — settings 变更在下次 callBash 时自动生效；`TestSBX58_PolicyRebuiltPerCall` 验证两次调用使用不同 Policy 值 |
| SBX-59 | `addToExcludedCommands()`：运行时动态追加排除命令（如 hook 或插件） | AUTO | 前置:已运行沙箱；操作:`addToExcludedCommands(["make"])`；预期:后续 `make` 命令跳过沙箱 | `src/utils/sandbox/sandbox-adapter.ts:828` | ✅ 通过（G30）：`AddExcludedCommand(store, command)` 新增于 `internal/sandbox/excluded_command_add.go`，幂等追加，返回新列表（不可变）；`ExcludedCommandsStore` 接口（Reader+Writer）；`LocalExcludedCommandsStore{SettingsPath}` 实现持久化写入 settings.local.json，读/写均操作 `sandbox.excludedCommands` JSON 字段，保留其他字段；6 个测试覆盖（append/idempotent/preserves-existing/empty-error/LocalStore-roundtrip/LocalStore-preserves-other-settings）；注：内存中 Policy 在下次 PolicyFromSettings 调用时刷新（等价于 CC 每次 callBash 重建 Policy 的行为）；写入持久化 settings 后新命令在下次 Bash 工具调用时自动生效|
| SBX-60 | `areSandboxSettingsLockedByPolicy()`：policySettings 锁定时防止用户修改沙箱设置 | AUTO | 前置:`policySettings` 设置了沙箱锁定；操作:检查锁定状态；预期:返回 `true` | `src/utils/sandbox/sandbox-adapter.ts:656` | N/A（policySettings 锁定为 OUT-of-scope §10） |
| SBX-61 | `O_NOFOLLOW`：沙箱输出文件以 `O_NOFOLLOW` 打开防符号链接攻击 | AUTO | 前置:CC Shell.ts；操作:沙箱命令写 stdout 临时文件；预期:文件以 `O_NOFOLLOW` 标志打开 | `src/utils/Shell.ts:299` | N/A（ccgo 执行模型为直接 exec，不用临时文件中转输出） |

小计：共 **61 行**。✅ 通过：**54**（G30 新增 SBX-35 executor bypass/SBX-43/SBX-59 → ✅（G30 前 SBX-35 仅字段存在，executor bypass 于 G30 补齐；SBX-43/59 为新建）；G28 新增 SBX-44/58 → ✅；G27 新增 SBX-52/53 → ✅；G8 新增 SBX-38/49/54/55/56/57 → ✅；G16 新增 SBX-34/35-field → ✅）　⚠️ 部分/内核限制：**1**（SBX-48 per-domain macOS seatbelt 无原生域名过滤需代理层）　❌ 缺失：**0**　N/A：**6**（SBX-24/25 landlock N/A；SBX-50/51 policySettings OUT-of-scope；SBX-60/61 policySettings OUT-of-scope §10）
