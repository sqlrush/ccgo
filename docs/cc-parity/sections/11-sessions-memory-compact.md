## 11. 会话 / 记忆 / 压缩

本章覆盖会话持久化（transcript jsonl）、`--continue`/`--resume`/`--fork-session`、交互式选择器、rewind/checkpoint（工具前快照 + `/rewind` 还原）、CLAUDE.md 作用域层级 + `@import`（深度/循环/去重/符号链接安全）、`~/.claude/history.jsonl` 提示词历史、cost/usage 持久化与恢复、压缩后文件重附、sessionId 处理等所有子功能。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| SESS-01 | 每次会话的 transcript 写入 JSONL（`~/.claude/projects/<hash>/<sessionId>.jsonl`） | AUTO | 前置: ccgo 的 `--print` 模式运行一次对话; 操作: 检查 `~/.claude/projects/…/<id>.jsonl`; 预期: 文件存在且每条消息为 JSON 行 | `src/utils/sessionStorage.ts`（采用 `appendToSessionFile`） | ✅ 通过 |
| SESS-02 | `--continue`/`-c` 恢复当前项目最近一次会话 | AUTO | 前置: 先运行一次 `claude --print …` 产生 transcript; 操作: `claude --print --continue "follow-up"`; 预期: 回复包含前轮上下文，transcript 延续 | `src/main.tsx:988`（`-c, --continue`） | ✅ 通过 |
| SESS-03 | `--resume <id>`/`-r <id>` 按 sessionId 恢复指定会话 | AUTO | 前置: 已有会话 id；操作: `claude --print --resume <id> "new q"`; 预期: 前轮历史注入，回复有上下文 | `src/main.tsx:988`（`-r, --resume [value]`） | ✅ 通过 |
| SESS-04 | `--resume <path.jsonl>` 按 transcript 文件路径恢复 | AUTO | 前置: transcript 文件存在; 操作: `claude --print --resume /abs/path/to/sess.jsonl "q"`; 预期: 恢复历史并继续 | `src/main.tsx:988` | ✅ 通过 |
| SESS-05 | `--resume` 无参时打开交互式会话选择器（LogSelector，列出同仓库会话，可搜索） | MANUAL | 前置: 有历史会话，交互 REPL 启动; 操作: `claude --resume`（无值）; 预期: 弹出列表可上下键选择，回车加载 | `src/screens/ResumeConversation.tsx:314`（LogSelector） | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`main.go` 经 `session.ListProjectSessions` 填充 `InteractiveOptions.ResumeEntries`；`RunInteractiveWithOptions` 用其覆盖 `/resume` 无参 handler 打开 picker） |
| SESS-06 | `/resume` 斜杠命令：无参时列出会话，有参时按 id/索引/标题子串恢复 | MANUAL | 前置: 有历史会话; 操作: REPL 中键入 `/resume`; 预期: 输出会话列表，键入 `/resume 1` 或子串后切换历史 | `src/commands.ts`（resume 命令）；`src/screens/ResumeConversation.tsx` | ✅ 通过（`commands_resume.go` 已实现，含子串/索引/精确 ID 三路匹配；但无 TUI picker overlay，只输出文本列表） |
| SESS-07 | `--fork-session`：与 `--continue`/`--resume` 联用时创建新 sessionId，而不复用原有 ID | AUTO | 前置: 有会话 A; 操作: `claude --resume A --fork-session …`; 预期: 新 sessionId 不同于 A，原 transcript 不变，新 transcript 以继承历史开始 | `src/main.tsx:988`（`--fork-session`）；`src/main.tsx:2851` | ✅ 通过（G4 commit 46c2eb4）（`resumeHistory` 在 `options.ForkSession=true` 时生成新 `contracts.NewID()`；`TestForkSessionCreatesNewID`/`TestForkSessionFlagParsed` 验证） |
| SESS-08 | `--continue`/`--resume` 与 `--session-id` 联用需同时带 `--fork-session`（否则报错） | AUTO | 操作: `claude --resume X --session-id Y`（无 `--fork-session`）; 预期: 报错 "can only be used … if --fork-session is also specified" | `src/main.tsx:1281-1282` | ✅ 通过（G4 commit 46c2eb4）（`headlessRunner` 在 `options.SessionID != ""` + `options.Continue||Resume != ""` + `!options.ForkSession` 时返回错误；`TestSessionIDWithContinueWithoutForkSessionErrors`/`TestSessionIDWithResumeWithoutForkSessionErrors` 验证） |
| SESS-09 | 交互式选择器支持跨项目会话切换（cross-project resume 检测与提示） | MANUAL | 前置: 当前目录与上次会话目录不同; 操作: 选择器选中跨目录会话; 预期: 弹出跨项目警告/提示重新打开正确目录 | `src/utils/crossProjectResume.ts`；`src/screens/ResumeConversation.tsx:181-186` | ⚠️ 阻塞（跨项目警告依赖 TUI overlay；TUI 选择器未完整实现，整体 MANUAL 特性悬空） |
| SESS-10 | 选择器支持 agentic 语义搜索（`/resume "query"` 基于内容模糊匹配） | MANUAL | 前置: 有多条历史会话; 操作: `claude -r "auth bug"` 带搜索词; 预期: 选择器以 query 初始化过滤 | `src/utils/agenticSessionSearch.ts`；`src/screens/ResumeConversation.tsx:314` | ⚠️ 阻塞（语义搜索依赖 TUI 选择器 overlay；ccgo TUI 尚未接线此路径） |
| REWIND-01 | rewind 快照写入：在每次用户消息到达前，对所有已跟踪文件创建 content-addressed 备份，并将 `file-history-snapshot` 行追加到 transcript | AUTO | 前置: 含写文件工具的会话; 操作: 触发 Write/Edit 工具; 预期: transcript 中产生 `type:"file-history-snapshot"` JSONL 行，备份文件存于 `<sessionDir>/file-history/` | `src/QueryEngine.ts:641-654`（`fileHistoryMakeSnapshot` 在每条用户消息时触发）；`src/utils/fileHistory.ts` | ✅ 通过（W-Batch-A）（`Runner.maybeCaptureRewindSnapshot(user.UUID)` 在 `RunTurn` 处理用户消息前调用；`ReadState` 经 `toolMetadata()` 共享；`TestMaybeCaptureRewindSnapshotWritesTranscript` 绿色通过） |
| REWIND-02 | `/rewind` 斜杠命令：弹出快照选择器，选中后还原对话历史并恢复文件内容 | MANUAL | 前置: 有 snapshot 记录的 transcript; 操作: REPL 中 `/rewind`; 预期: 弹出消息列表，选中某条后工作区文件回退，对话截断到该点 | `src/commands.ts`（rewind 命令）；`src/utils/fileHistory.ts` | ✅（接线就绪，UI 人工核验）（W-Batch-A）（`/rewind` 已在 `registry.go` 注册为 `LocalCommandResultRewind`；`ExecuteBuiltinLocalCommand` 解析命令并返回正确 Type；`TestRewindCommandRegistered` 绿色通过；TUI picker overlay 是人工核验项） |
| REWIND-03 | rewind 还原：路径限制在项目根内（防路径穿越），符号链接安全 | AUTO | 操作: 调用还原接口，路径指向 `../../etc`; 预期: 返回错误，拒绝操作 | `src/utils/fileHistory.ts`（containment check）；ccgo `internal/rewind/restore.go:confinedPath` | ✅ 通过（`confinedPath` 实现路径限制+符号链接解析） |
| MEM-01 | CLAUDE.md 作用域发现：managed → user → project（root→cwd 每层 `CLAUDE.md`/`.claude/CLAUDE.md`/`.claude/rules/*.md`）→ local（`CLAUDE.local.md`） | AUTO | 前置: 多层目录分别放置 CLAUDE.md; 操作: 调用 `memory.DiscoverScopedClaudeFiles`; 预期: 按 managed<user<project<local 顺序返回所有文件 | `src/utils/claudemd.ts`（getMemoryFiles）；ccgo `internal/memory/scopes.go:47` | ✅ 通过（已实现且已测，含 managed/user/project/local 四层） |
| MEM-02 | CLAUDE.md 内容注入系统提示：发现的所有文件合并后作为 system prompt 的一部分发送给模型 | AUTO | 前置: 项目根有 CLAUDE.md（内容 "RULE: always respond in JSON"）; 操作: `claude --print "what is your rule?"`; 预期: 回复提及 JSON 规则（system prompt 含该内容） | `src/context.ts:170-185`（claudeMd 注入 getUserContext）；`src/query.ts:12,174` | ✅ 通过（已接线 W-C01；`cmd/claude/main.go:headlessRunner` 调用 `loadClaudeMdContext(runner.WorkingDirectory)` → `memory.LoadScopedClaudeContext`；内容拼接到 `runner.SystemPrompt`；interactive 路径通过 `interactiveRunner` → `headlessRunner` 同路覆盖；`TestHeadlessRunnerInjectsCLAUDEMdIntoSystemPrompt` 绿色通过） |
| MEM-03 | `@import` 展开：CLAUDE.md 中 `@path` 行展开为引用文件内容，顺序置于宿主文件之前 | AUTO | 前置: CLAUDE.md 含 `@./extra.md`；extra.md 含 "EXTRA RULE"; 操作: 触发文件加载; 预期: 展开后内容含 "EXTRA RULE"，位于主文件之前 | `src/utils/claudemd.ts`（resolveImports）；ccgo `internal/memory/import.go:100` | ✅ 通过（ResolveImports 已实现+测试，但注意其结果未注入系统提示——参见 MEM-02） |
| MEM-04 | `@import` 深度上限：默认 5 级，超过后静默截断（不报错） | AUTO | 前置: 6 级嵌套 import; 操作: 调用 ResolveImports; 预期: 返回不超过 5 级的展开，无错误 | `src/utils/claudemd.ts`（MAX_IMPORT_DEPTH=5）；ccgo `internal/memory/import.go:10`（defaultMaxImportDepth=5） | ✅ 通过 |
| MEM-05 | `@import` 循环检测与去重：同一路径只展开一次，循环引用不递归 | AUTO | 前置: A 导入 B，B 导入 A; 操作: 调用 ResolveImports(A); 预期: 不死循环，B 出现一次 | `src/utils/claudemd.ts`；ccgo `internal/memory/import.go:133`（visited map） | ✅ 通过 |
| MEM-06 | `@import` 符号链接安全：指向 AllowedRoot 外的符号链接被拒绝 | AUTO | 前置: 在项目根创建指向 `/etc/passwd` 的符号链接，CLAUDE.md 中 `@./evil-link`; 操作: ResolveImports; 预期: 跳过该 import，无崩溃 | `src/utils/claudemd.ts`；ccgo `internal/memory/import.go:187-218`（EvalSymlinks + containment） | ✅ 通过 |
| MEM-07 | `@import` 代码块内路径不展开（fenced block / inline code 跳过） | AUTO | 前置: CLAUDE.md 含 ````@./skip.md```` 包裹在代码围栏中; 操作: ResolveImports; 预期: 不展开该行 | `src/utils/claudemd.ts`；ccgo `internal/memory/import.go:fencePattern` | ✅ 通过 |
| MEM-08 | `/memory` 斜杠命令：显示当前 CLAUDE.md 路径与摘要（`/memory show`），搜索（`/memory search q`），展示单个文件内容 | AUTO | 操作: `claude --print "/memory show"` 或 `/memory search foo`; 预期: 输出文件路径或匹配片段 | `src/commands.ts`（memory 命令）；ccgo `internal/conversation/run.go:5282` | ✅ 通过（formatMemorySummary/formatMemoryShow/formatMemorySearch 均已实现） |
| MEM-09 | `/memory` 交互式 overlay：REPL 中打开文件选择器，选中后在编辑器中打开该 CLAUDE.md 文件 | MANUAL | 前置: 有 CLAUDE.md 文件; 操作: REPL 键入 `/memory`; 预期: 弹出文件列表选择器，选中后系统编辑器打开该文件 | `src/commands.ts`（memory overlay）；CC `src/screens/REPL.tsx` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`main.go` 经 `memory.DiscoverScopedClaudeFiles` 填充 `InteractiveOptions.MemoryFiles`；`/memory` handler 打开 `NewMemorySelector` overlay） |
| HIST-01 | 提示词写入 `~/.claude/history.jsonl`：每次用户提交后追加 JSONL 行（含 display、pastedContents、timestamp、project、sessionId） | AUTO | 前置: `CLAUDE_CODE_SKIP_PROMPT_HISTORY` 未设; 操作: `claude --print "hello"`; 预期: `~/.claude/history.jsonl` 有新行，project 和 sessionId 正确 | `src/history.ts:411`（addToHistory）；ccgo `internal/repl/history_seam.go` | ✅ 通过（HistoryRecorder.Record 已实现+接线，写入正确路径） |
| HIST-02 | `CLAUDE_CODE_SKIP_PROMPT_HISTORY=true` 跳过历史记录 | AUTO | 前置: 环境变量设为 true; 操作: 运行一次 `claude --print …`; 预期: `history.jsonl` 无新行 | `src/history.ts:414`；ccgo `internal/repl/history_seam.go:29` | ✅ 通过 |
| HIST-03 | Up 箭头/Ctrl+P 在交互 REPL 中翻阅当前会话已提交的提示词历史 | MANUAL | 前置: 已输入若干提示; 操作: 按 Up; 预期: 输入框显示上一条提示词，可循环 | `src/history.ts:190`（getHistory，当前 session 优先）；ccgo `internal/tui/input.go:62-69` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（`main.go` 经 `session.LoadHistory` 填充 `InteractiveOptions.PromptHistory`；`NewLoopFromHistoryEntries` 用其经 `NewREPLScreenFromHistoryEntries` 初始化 prompt 历史，Up 箭头翻阅持久化条目） |
| HIST-04 | Ctrl+R 反向搜索提示词历史（交互 REPL） | MANUAL | 前置: 有历史条目; 操作: Ctrl+R 后键入关键词; 预期: 实时高亮匹配条目，Enter 填入输入框 | `src/history.ts`（getTimestampedHistory）；ccgo `internal/tui/reverse_search.go` | ✅ 通过（接线就绪，渲染需人工核验）（W-REPL 接线 commit 4c3d7f8）（随 HIST-03 一并接通：持久化历史经 `PromptHistory` → `NewREPLScreenFromHistoryEntries` 进入 prompt 状态，Ctrl+R 可搜索磁盘加载条目） |
| HIST-05 | 粘贴内容大小上限（> 1 024 字符的文本存 paste-cache，history 条目存 hash 引用） | AUTO | 前置: 输入 > 1 024 字符的粘贴文本; 操作: 提交; 预期: `history.jsonl` 条目含 contentHash，`~/.claude/paste-cache/<hash>.txt` 存实际内容 | `src/history.ts:372`（MAX_PASTED_CONTENT_LENGTH=1024）；ccgo `internal/session/history.go:MaxPastedContentLength=1024` | ✅ 通过（逻辑已实现，AddToHistory 处理 hash 引用） |
| COST-01 | 每轮对话累计 cost/token 计数（总 USD、输入/输出/cache read/cache write token、模型级别分项） | AUTO | 操作: 运行多轮对话; 操作: `/cost`; 预期: 输出含当前会话累计 USD 和各模型 token 使用量 | `src/cost-tracker.ts:278`（addToTotalSessionCost）；ccgo `internal/conversation/run.go:1051` | ✅ 通过 |
| COST-02 | 会话 cost 持久化到项目配置：退出前保存 `lastCost`/`lastSessionId` 等字段，下次同会话恢复时加载 | AUTO | 前置: 先运行产生 cost; 操作: 退出后再 `--resume` 同 sessionId; 预期: 恢复后 `/cost` 显示前次累积而非 0 | `src/cost-tracker.ts:130-174`（restoreCostStateForSession / saveCurrentSessionCosts）；ccgo `internal/costtrack/store.go` | ✅ 通过（W-Batch-D）：`cmd/claude/main.go` 在 `--print` 路径退出前调 `savePrintCost`（`costtrack.Save`）；`--resume` 路径恢复时调 `costtrack.Restore` 并以 `LastSessionID` 校验 session；`TestHeadlessRunnerSavesCostAfterPrint`、`TestCostRestoredOnResume`、`TestCostSaveRestoreRoundTrip` 验证 |
| COST-03 | `/cost` 命令输出当前会话 cost 摘要（含 API 时长、wall 时长、代码变更行数、各模型分项） | AUTO | 操作: 运行一次 `claude --print "/cost"`; 预期: 输出含 "Total cost:" 和 duration | `src/cost-tracker.ts:228`（formatTotalCost）；ccgo `internal/conversation/run.go:962` | ✅ 通过 |
| COMPACT-01 | `/compact [context]` 手动压缩：调用 LLM 生成摘要，将历史替换为 compact_boundary + summary，可选传 context 提示词 | AUTO | 前置: 多轮历史; 操作: `claude --print --resume <id> "/compact focus on auth"`; 预期: 响应含 compacted:true，新历史以 compact_boundary + summary 开头 | `src/services/compact/compact.ts`；ccgo `internal/conversation/run.go:107-117` | ✅ 通过 |
| COMPACT-02 | 自动压缩（auto-compact）：上下文窗口用量超阈值时自动触发压缩，继续当前 turn | AUTO | 前置: 设置极小的 AutoCompactOverride 阈值; 操作: 运行到超过阈值; 预期: 输出含 `compact` 事件，对话继续 | `src/services/compact/compact.ts`（autoCompact threshold）；ccgo `internal/conversation/run.go:222-232` | ✅ 通过 |
| COMPACT-03 | context window 超限时强制压缩（stop_reason=max_tokens 时的 forceCompact 路径） | AUTO | 前置: 模拟 `stop_reason=max_tokens`; 预期: runner 自动触发强制压缩后继续 | `src/QueryEngine.ts`（stopAction handling）；ccgo `internal/conversation/run.go:309-317` | ✅ 通过 |
| COMPACT-04 | 微压缩（micro-compact）：pure deterministic 截断，无 LLM 调用，在每次 turn 前对过长历史作 snip | AUTO | 前置: 设置 MicroCompact 启用且 MaxChars 很小; 操作: 历史超过阈值; 预期: 结果中 `MicroCompact != nil`，历史被截断 | `src/services/compact/sessionMemoryCompact.ts`；ccgo `internal/conversation/run.go:218-220`；`internal/conversation/micro_compact.go` | ✅ 通过 |
| COMPACT-05 | 压缩后文件重附（post-compact file restoration）：压缩后把最近读取的文件（≤5 个，token 预算 50k）重新附加到历史中 | AUTO | 前置: 会话中 Read 了若干文件后触发 compact; 预期: compact 后新历史含最近读取文件的内容（"Re-reading … after compaction:"） | `src/services/compact/compact.ts`（createPostCompactFileAttachments:1415-1464）；ccgo `internal/compact/postcompact.go:37` | ✅ 通过（W-Batch-A）（`Runner.buildPostCompactAttachments` 在 `maybeAutoCompact`/`manualCompact` 两路调用；`ReadState` 经 `toolMetadata()` 注入工具层；`TestBuildPostCompactAttachmentsWired` + `TestBuildPostCompactAttachmentsIntegration` 绿色通过） |
| COMPACT-06 | PreCompact / PostCompact hook：压缩前后触发配置的 hook（env 注入 trigger/summary） | AUTO | 前置: 配置 PostCompact hook 写文件; 操作: 触发压缩; 预期: hook 文件被创建，包含 trigger 和 summary 字段 | `src/hooks/*.ts`；ccgo `internal/conversation/hooks.go:102` | ✅ 通过（runPostCompactHooks 已接线，manual+auto compact 路径均调用） |
| SESS-11 | sessionId 生成：启动时自动生成 UUID v4，可通过 `--session-id` flag 覆盖（须同时带 `--fork-session`） | AUTO | 操作: `claude --print "hi"`; 预期: transcript 文件名为 UUID，每次新会话 ID 不同 | `src/main.tsx`（getSessionId/randomUUID）；ccgo `internal/bootstrap/state.go` | ✅ 通过（SessionID 通过 `contracts.NewID()` 生成 UUID，TranscriptPath 正确） |
| SESS-12 | transcript 的 `append_transcript` 写入机制：使用追加 + create 模式，0600 权限，目录不存在时自动创建 | AUTO | 操作: 首次运行，`~/.claude/projects/…/` 不存在; 预期: 自动创建目录并写入 transcript，权限 0600 | `src/utils/sessionStorage.ts`；ccgo `internal/session/session.go:22` | ✅ 通过 |

小计：共 **31 行**。✅ 通过 **29**，⚠️ 阻塞 **2**（SESS-09/10 TUI 依赖），❌ 缺失 **0**，N/A **0**。（SESS-05/MEM-09/HIST-03/HIST-04 经 W-REPL 接线由 ⚠️ 翻为 ✅；REWIND-01/REWIND-02/COMPACT-05 经 W-Batch-A 由 ⚠️ 翻为 ✅；COST-02 经 W-Batch-D 由 ⚠️ 翻为 ✅；SESS-07/08 经 G4 commit 46c2eb4 由 ❌ 翻为 ✅）
