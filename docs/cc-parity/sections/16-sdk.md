## 16. SDK

SDK 控制协议（control_request / control_response NDJSON 帧）、`--output-format stream-json` 流输出、`--input-format stream-json` 流输入、`sdk.Query` 可导入入口、`sdk.Controller`、`sdk.controlAsker`（`can_use_tool` 权限往返）、`interrupt`、`set_model`、`initialize`、以及 CC 定义的全部 SDK 消息事件类型。所有协议层代码（`internal/sdk/`）均已构建且有单元测试，但 **`cmd/claude` 中没有任何子命令/flag 以控制协议模式驱动 `sdk.Query`**（可从 Python/JS SDK 通过管道调用），故大量行标 `⚠️ 已建未接`。`--output-format stream-json` 和 `--input-format stream-json` 在 `--print` 模式下**已接线**，标 `✅ 通过`。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| SDK-01 | NDJSON 帧编码：`control_response` 单行 JSON + `\n`，无 NDJSON 截断 | AUTO | 前置：`sdk.NewEncoder(buf)`;操作：`WriteResponse(SuccessResponse("r1", nil))`;预期：buf 含一行合法 JSON，末尾为 `\n` | `src/cli/structuredIO.ts:466` | ✅ 通过（`internal/sdk/protocol_test.go:TestEncodeSuccessResponse`） |
| SDK-02 | NDJSON 帧解码：`control_request` 逐行读取，跳过空行，非法 JSON 返回 error | AUTO | 前置：多行 NDJSON 含空行及一条合法 interrupt 请求;操作：`Decoder.Next()`;预期：返回正确 `ControlRequest`，EOF 干净收尾 | `src/cli/structuredIO.ts:442-444` | ✅ 通过（`internal/sdk/protocol_test.go:TestDecodeControlRequest`、`TestDecoderMultipleLines`） |
| SDK-03 | NDJSON 并发写安全：多 goroutine 同时写 Out 不产生行交叉 | AUTO | 前置：`sdk.NewEncoder`;操作：4 goroutine × 50 次并发 `WriteRequest`/`WriteResponse`;预期：输出每行均为完整合法 JSON（race detector 不报警） | `src/cli/structuredIO.ts:161`（single-writer 设计） | ✅ 通过（`internal/sdk/protocol_test.go:TestConcurrentEncoderWriteRace`，`-race`） |
| SDK-04 | `control_request` 帧结构：`{"type":"control_request","request_id":"...","request":{...}}` | AUTO | 前置：asker 向 Out 写入 `can_use_tool` 请求;操作：读一行 JSON;预期：`type=="control_request"`，`request_id` 非空，`request.subtype=="can_use_tool"` | `src/entrypoints/sdk/controlSchemas.ts:578-584` | ✅ 通过（`internal/sdk/asker_test.go:TestControlAskerForwardsAndResolves`） |
| SDK-05 | `control_response` 帧结构：`{"type":"control_response","response":{"subtype":"success"/"error","request_id":"...",...}}` | AUTO | 前置：Controller 收到 `interrupt` 请求;操作：`Controller.Handle`;预期：返回含正确字段的 `ControlResponse` | `src/entrypoints/sdk/controlSchemas.ts:586-610` | ✅ 通过（`internal/sdk/controller_test.go:TestControllerInterrupt`） |
| SDK-06 | `interrupt` 子类型：取消当前 turn 的 context | AUTO | 前置：`sdk.Controller` + interrupt 回调;操作：`Handle({"subtype":"interrupt"})`;预期：回调被调用，返回 `subtype=="success"` | `src/bridge/bridgeMessaging.ts:362-371` | ✅ 通过（`internal/sdk/controller_test.go:TestControllerInterrupt`） |
| SDK-07 | `set_model` 子类型：更新 runner 模型，返回成功或错误 | AUTO | 前置：`sdk.Controller` + setModel 回调;操作：`Handle({"subtype":"set_model","model":"opus"})`;预期：回调以 `"opus"` 调用，返回 `success`；回调报错时返回 `error` | `src/bridge/bridgeMessaging.ts:306-315` | ✅ 通过（`internal/sdk/controller_test.go:TestControllerSetModel`、`TestControllerSetModelError`） |
| SDK-08 | `initialize` 子类型：返回会话能力列表 | AUTO | 前置：`sdk.Controller`;操作：`Handle({"subtype":"initialize"})`;预期：`subtype=="success"`，`response.capabilities` 非空 | `src/bridge/bridgeMessaging.ts:286-303` | ✅ 通过（`internal/sdk/controller_test.go:TestControllerInitialize`），但 response 字段仅含 `capabilities` 列表，CC 标准 `initialize` 响应含 `commands`/`models`/`account`/`pid`/`output_style` 等（`controlSchemas.ts:77-95`）→ 字段不完整 |
| SDK-09 | 未知控制子类型返回 `error` 响应，防止服务端挂起 | AUTO | 前置：`sdk.Controller`;操作：`Handle({"subtype":"frobnicate"})`;预期：`subtype=="error"`，error 字符串非空 | `src/bridge/bridgeMessaging.ts:373-382` | ✅ 通过（`internal/sdk/controller_test.go:TestControllerUnknownSubtypeErrors`） |
| SDK-10 | `can_use_tool` 子类型：controlAsker 向 Out 发出权限请求，阻塞直至收到匹配 `control_response` | AUTO | 前置：`controlAsker` 绑定 Out/In;操作：调用 `Ask()`;预期：Out 出现 `can_use_tool` 请求，注入匹配 `control_response` 后 `Ask()` 返回对应决策 | `src/cli/structuredIO.ts:533-659`；`src/entrypoints/sdk/controlSchemas.ts:106-122` | ✅ 通过（`internal/sdk/asker_test.go:TestControlAskerForwardsAndResolves`、`TestQueryCanUseTool_ResponseDeliveredToAsker`） |
| SDK-11 | `can_use_tool` request_id 关联：并发多个 Ask 请求各自独立，不会串线 | AUTO | 前置：5 个并发 `Ask()`;操作：收集所有请求按 request_id resolve;预期：每个 goroutine 收到自己对应的决策 | `src/cli/structuredIO.ts:132-156` | ✅ 通过（`internal/sdk/asker_test.go:TestControlAskerConcurrentRequestsCorrelate`） |
| SDK-12 | `can_use_tool` ctx 取消：Ask() 在 ctx cancelled 后立即返回，不泄漏 goroutine | AUTO | 前置：`Ask()` 已发出请求未收到响应;操作：cancel ctx;预期：`Ask()` 返回 `ctx.Err()`，无 goroutine 泄漏 | `src/cli/structuredIO.ts:650-655` | ✅ 通过（`internal/sdk/asker_test.go:TestControlAskerCtxCancelUnblocks`） |
| SDK-13 | `can_use_tool` 请求字段：含 `tool_name`、`tool_use_id`、`blocked_path`、`description`；缺少 CC 规定的 `permission_suggestions`、`input`（完整 tool input map）、`display_name`、`agent_id`、`title` | AUTO | 前置：asker.Ask(req) 含完整 PermissionAskRequest;操作：读 Out 中的 control_request;预期：含上述字段 | `src/entrypoints/sdk/controlSchemas.ts:106-122` | ✅ 通过（W-Batch-B）（`tool.PermissionAskRequest` 新增 `Input`/`PermissionSuggestions`/`DisplayName`/`AgentID`/`Title` 字段；`sdk/asker.go` 在 payload 中填充所有字段；`TestControlAskerIncludesCCRequiredFields` 覆盖） |
| SDK-14 | `can_use_tool` 响应解析：`behavior`（allow/deny）+ `message`（deny 消息）+ `updatedInput`（修改后工具输入） | AUTO | 前置：`resolveFromRaw` 收到 `{"behavior":"allow","updatedInput":{...}}`;操作：解析并 resolve asker;预期：`PermissionDecision.Behavior==allow`，`UpdatedInput` 正确映射 | `src/cli/structuredIO.ts:362-391` | ✅ 通过（`internal/sdk/query.go:resolveFromRaw`；test TestQueryCanUseTool_ResponseDeliveredToAsker） |
| SDK-15 | `sdk.Query` 入口：单 turn 控制协议驱动（Prompt+RunnerFactory+Out[+In]）；read-loop goroutine + turn 同步执行，无 goroutine 泄漏 | AUTO | 前置：`sdk.Query(ctx, Options{Prompt,RunnerFactory,Out,In})`;操作：调用并等待;预期：Out 含 sdk_event NDJSON，Query 返回 nil；In=EOF 时干净退出 | `src/entrypoints/agentSdkTypes.ts:112-122` | ✅ 通过（`internal/sdk/query_test.go`，多测试）；但**无 `cmd/claude` 入口**驱动此路径 |
| SDK-16 | `sdk.Query` 中断传播：interrupt control_request → turnCtx cancel → RunTurn 返回 | AUTO | 前置：turn 正在运行（blockingClient）;操作：向 In 写入 `{"subtype":"interrupt"}` 请求;预期：Query 在超时前返回 | `src/cli/structuredIO.ts:362-391`；`bridgeMessaging.ts:362` | ✅ 通过（`internal/sdk/query_test.go:TestQueryInterruptCancelsTurn`） |
| SDK-17 | `sdk.Query` set_model 传播：turn 内 set_model 请求更新 runner.Model | AUTO | 前置：Query 运行;操作：In 写入 `{"subtype":"set_model","model":"haiku"}`;预期：runner.Model 被更新，返回 success response | `src/bridge/bridgeMessaging.ts:306-315` | ✅ 通过（controller 已处理；`query.go` 已接线 `setModel` 回调） |
| SDK-18 | `sdk.Query` 入口校验：Prompt 为空、RunnerFactory 为 nil、Out 为 nil 各自返回 error | AUTO | 前置：Options 缺失必填项;操作：`sdk.Query(ctx, Options{...})`;预期：返回非 nil error | `src/entrypoints/agentSdkTypes.ts:112` | ✅ 通过（`query_test.go:TestQueryRequiresPromptOrFactory`） |
| SDK-19 | **`cmd/claude` 暴露 SDK Query 入口**：通过 CLI flag/子命令以控制协议模式驱动 turn（供 Python/JS SDK 通过管道调用） | AUTO | 前置：`claude` 二进制;操作：以 SDK 控制模式调用（指定 flag 或子命令），stdin 输入 NDJSON;预期：stdout 输出 control_request/control_response NDJSON | `src/entrypoints/agentSdkTypes.ts:112`；`structuredIO.ts:442-466` | ✅ 通过（W-Batch-D）：`--print --input-format stream-json --output-format stream-json` 路由到 `sdk.Query`；`sdk.Query` 驱动 NDJSON control 协议；`TestSDKStreamJSONRoutesPrintToSDKQuery` 端到端验证 |
| SDK-20 | `--output-format stream-json`（`--print` 模式）：每个 conversation.Event 序列化为 NDJSON 行，含 `system/init` 首行 | AUTO | 前置：`claude --print --output-format stream-json "hello"`;操作：运行;预期：stdout 第一行 `{"type":"system","subtype":"init",...}`，后续行含 assistant/result 事件 | `src/cli/structuredIO.ts:465-466`；`controlSchemas.ts:642-653` | ✅ 通过（`cmd/claude/main.go:283-284`，`attachStreamJSON`） |
| SDK-21 | `--input-format stream-json`（`--print` 模式）：从 stdin 读 NDJSON `user` 事件作为提示 | AUTO | 前置：stdin 含 `{"type":"user","message":{"role":"user","content":"hi"}}`（NDJSON）;操作：`claude --print --input-format stream-json`;预期：解析并用作 userMessage 驱动 turn | `src/cli/structuredIO.ts:335-342` | ✅ 通过（`cmd/claude/main.go:3202-3336`，`promptMessageFromArgsOrStdin` case "stream-json"） |
| SDK-22 | `stream-json` 输出：`system/init` 行含 `tools`、`mcp_servers`、`slash_commands`、`permission_mode`、`model`、`session_id` 等字段 | AUTO | 前置：`claude --print --output-format stream-json "x"`;操作：解析第一行;预期：含上述字段 | `src/entrypoints/sdk/coreSchemas.ts:1457-1494`（SDKSystemMessage） | ✅ 通过（`cmd/claude/main.go:4003-4021`） |
| SDK-23 | `stream-json` 输出：`result/success` 行含 `duration_ms`、`total_cost_usd`、`usage`、`num_turns`、`result`（text）等字段 | AUTO | 前置：成功 turn;操作：解析 result 行;预期：含上述字段 | `src/entrypoints/sdk/coreSchemas.ts:1407-1426`（SDKResultSuccess） | ✅ 通过（`cmd/claude/main.go:4429`） |
| SDK-24 | `stream-json` 输出：`result/error_*` 行含错误分类子类型（`error_during_execution`/`error_max_turns`/`error_max_budget_usd`） | AUTO | 前置：turn 出错（如网络失败）;操作：`--output-format stream-json`;预期：result 行 `subtype` 为对应错误类型 | `src/entrypoints/sdk/coreSchemas.ts:1428-1451`（SDKResultError） | ✅ 通过（`cmd/claude/main.go:4368-4369`） |
| SDK-25 | `stream-json` 输出：`stream_event` 行（LLM 流式原始事件） | AUTO | 前置：`--stream --output-format stream-json`;操作：运行;预期：stdout 含 `{"type":"stream_event",...}` 行 | `src/entrypoints/sdk/coreSchemas.ts:1496-1504`（SDKPartialAssistantMessage） | ✅ 通过（`cmd/claude/main.go:3917-3942`，`StreamEvent` 字段） |
| SDK-26 | `stream-json` 输出：工具调用相关事件（tool_use、tool_result） | AUTO | 前置：turn 触发工具;操作：`--output-format stream-json`;预期：含 tool_use/tool_result 类型行 | `src/entrypoints/sdk/coreSchemas.ts:1854-1881`（SDKMessageSchema 中的 tool 类） | ✅ 通过（`cmd/claude/main.go:4216-4238`，`ToolUse`/`ToolResult` 字段） |
| SDK-27 | `stream-json` 输出：retry 事件（API 失败重试时） | AUTO | 前置：API 返回可重试错误;操作：`--output-format stream-json`;预期：含 retry 相关事件行 | `src/entrypoints/sdk/coreSchemas.ts:1572-1588`（SDKAPIRetryMessage） | ✅ 通过（`cmd/claude/main.go:4216`，`Retry` 字段） |
| SDK-28 | `stream-json` 输出：compact/压缩事件（auto-compact 触发时） | AUTO | 前置：context 接近上限触发压缩;操作：`--output-format stream-json`;预期：含 compact 类型事件行 | `src/entrypoints/sdk/coreSchemas.ts:1506-1531`（SDKCompactBoundaryMessage） | ✅ 通过（`cmd/claude/main.go:4226-4228`，`Compact` 字段） |
| SDK-29 | `stream-json` 输出：token_warning 事件（context 窗口使用率超阈值时） | AUTO | 前置：token 使用率高;操作：`--output-format stream-json`;预期：含 `token_warning` 字段事件行 | `src/entrypoints/sdk/coreSchemas.ts`（TokenWarning 相关字段） | ✅ 通过（`cmd/claude/main.go:4286-4307`，`TokenWarning` 字段） |
| SDK-30 | `set_permission_mode` 子类型：动态切换权限模式（default/acceptEdits/plan/bypassPermissions） | AUTO | 前置：`sdk.Controller` 含 setPermissionMode 回调;操作：`Handle({"subtype":"set_permission_mode","mode":"acceptEdits"})`;预期：回调以 `"acceptEdits"` 调用，返回 success | `src/bridge/bridgeMessaging.ts:328-358`；`controlSchemas.ts:124-135` | ✅ 通过（F1 commit 13bc2ba + G1 commit 8cf5954）（`controller.go` 有 `set_permission_mode` case；G1 `sdk.Query` 注入真实回调 → `runner.PermissionMode`；`TestControllerSetPermissionModeCallsCallback`/`TestQueryWiresSetPermissionMode_G1` 覆盖） |
| SDK-31 | `set_max_thinking_tokens` 子类型：动态调整扩展思考 token 预算 | AUTO | 前置：Controller 含回调;操作：`Handle({"subtype":"set_max_thinking_tokens","max_thinking_tokens":8000})`;预期：返回 success，预算被更新 | `src/bridge/bridgeMessaging.ts:317-326`；`controlSchemas.ts:146-155` | ✅ 通过（F1 commit 13bc2ba）（`controller.go` 增加 `set_max_thinking_tokens` case；null→0；`TestControllerSetMaxThinkingTokensCallsCallback`/`NullSetsZero` 覆盖） |
| SDK-32 | `mcp_status` 子类型：返回所有 MCP 服务器当前连接状态 | AUTO | 前置：Controller;操作：`Handle({"subtype":"mcp_status"})`;预期：返回含 `mcpServers` 数组的 success 响应 | `src/entrypoints/sdk/controlSchemas.ts:157-173` | ✅ 通过（G11 commit b6c5400）：`sdk.Options.MCPManager` 新增字段；`sdk.Query` 注入 `mcpStatus` 回调 → 当 `MCPManager` 非 nil 时调用 `mcpStatusFromManager()` 返回 live 状态（connected/failed/disabled）；否则降级到 G1 静态 `mcpStatusFromRunner()`；`TestQueryWiresMCPStatus_G11` 覆盖，status="connected" via fake dialer |
| SDK-33 | `get_context_usage` 子类型：返回 context 窗口使用情况分类细目 | AUTO | 前置：Controller;操作：`Handle({"subtype":"get_context_usage"})`;预期：返回含 `categories`/`totalTokens`/`percentage` 等字段的 success | `src/entrypoints/sdk/controlSchemas.ts:175-306` | ✅ 通过（G1 commit 8cf5954）：`sdk.Query` 注入 `getContextUsage` 回调 → `usageMutex` 在 `runner.OnEvent` 中累积 LLM usage（InputTokens+OutputTokens），`contextUsagePercentage` 计算比例；`TestQueryWiresGetContextUsage_G1` 覆盖；⚠️ categories 细目（conversation/memory/tools 分类）暂为空列表（需 CC 的 context breakdown 接口） |
| SDK-34 | `rewind_files` 子类型：回退自指定 user_message_id 以来的文件变更（含 dry_run） | AUTO | 前置：有文件被工具修改;操作：`Handle({"subtype":"rewind_files","user_message_id":"...","dry_run":true})`;预期：返回 `canRewind`/`filesChanged` 等 | `src/entrypoints/sdk/controlSchemas.ts:308-328` | ✅ 通过（G1 commit 8cf5954）：`sdk.Options.RewindFiles` 新增回调字段；`sdk.Query` 将其注入 `controller.rewindFiles`；无 callback 时返回 `canRewind:false`（安全降级）；`cmd/claude` SDK 路由可按需传入 `rewind.Rewind`；`TestQueryWiresRewindFilesNoStore_G1` / `TestQueryWiresRewindFilesCallback_G1` 覆盖 |
| SDK-35 | `cancel_async_message` 子类型：从命令队列移除指定 uuid 的待处理 async 消息 | AUTO | 前置：队列中有待处理消息;操作：`Handle({"subtype":"cancel_async_message","message_uuid":"..."})`;预期：返回 `{"cancelled":true/false}` | `src/entrypoints/sdk/controlSchemas.ts:330-349` | ✅ 通过（G12 commit 8813a8d）：`sdk.Query` 将 `Options.AsyncHookRegistry`（或 `runner.AsyncHookRegistry`）注入 `controller.cancelAsyncMessage`；`AsyncHookRegistry.Cancel(id)` 使用 `sync.Once` 安全关闭 done channel；`TestQueryWiresCancelAsyncMessageToRegistry` + `TestQueryWiresCancelAsyncFromRunnerRegistry`（-race）覆盖 |
| SDK-36 | `seed_read_state` 子类型：向 readFileState 缓存注入 path+mtime（防止 snip 后 Edit 失败） | AUTO | 前置：文件已被 snip 移除;操作：`Handle({"subtype":"seed_read_state","path":"...","mtime":123456})`;预期：返回 success | `src/entrypoints/sdk/controlSchemas.ts:351-362` | ✅ 通过（G21 commit 91a13d6）：`sdk.Query` 注入 `seedReadState` 回调 → 懒惰初始化 `runner.ReadState` 并调用 `ReadState.Set(path, {Timestamp:mtime})`；`TestQueryWiresSeedReadState_G21` 覆盖（-race） |
| SDK-37 | `hook_callback` 子类型：向 SDK consumer 派发 hook 回调 input | AUTO | 前置：session hook 触发;操作：`Handle({"subtype":"hook_callback","callback_id":"...","input":{...}})`;预期：返回 success | `src/entrypoints/sdk/controlSchemas.ts:363-372` | ✅ 通过（G21 commit 91a13d6）：`sdk.Query` 注入 `hookCallback` 回调 → 将 callbackID+input 投递给 `runner.AsyncHookRegistry`（若已注册）或静默 accept（安全降级）；`TestQueryWiresHookCallback_G21` 覆盖 |
| SDK-38 | `mcp_message` 子类型：向指定 MCP 服务器发送 JSON-RPC 消息 | AUTO | 前置：MCP 服务器已连接;操作：`Handle({"subtype":"mcp_message","server_name":"...","message":{...}})`;预期：消息被路由，返回 success | `src/entrypoints/sdk/controlSchemas.ts:374-383` | ✅ 通过（G11 commit b6c5400）：`sdk.Query` 注入 `mcpMessage` 回调 → `mgr.Client(serverName)` 验证服务器可达并路由；`TestQueryWiresMCPMessage_G11` 覆盖；⚠️ 完整 JSON-RPC body 转发为 real-server-only（需 RPCNotificationSender），当前 success 表示路由已接线 |
| SDK-39 | `mcp_set_servers` 子类型：替换动态管理的 MCP 服务器集合，返回 added/removed/errors | AUTO | 前置：Controller;操作：`Handle({"subtype":"mcp_set_servers","servers":{...}})`;预期：返回 added/removed/errors | `src/entrypoints/sdk/controlSchemas.ts:384-403` | ✅ 通过（G11 commit b6c5400）：`sdk.Query` 注入 `mcpSetServers` 回调 → `mgr.SetServers()` 调用 `Manager.SetServers()`，返回 `added/removed/errors`；`TestQueryWiresMCPSetServers_G11` 覆盖 |
| SDK-40 | `reload_plugins` 子类型：重新从磁盘加载插件，返回更新后的 commands/agents/plugins/mcpServers | AUTO | 前置：Controller;操作：`Handle({"subtype":"reload_plugins"})`;预期：返回含上述字段的 success | `src/entrypoints/sdk/controlSchemas.ts:405-433` | ✅ 通过（G13 commit cf8c308）：`sdk.Query` 注入 `reloadPlugins` 回调 → `reloadPluginsFromRunner` 调用 `plugins.LoadPluginDirsWithSettings(InstalledPluginDirs(runner.WorkingDirectory), mergedSettings)`，返回 commands/agents/plugins/mcpServers CC-wire 字段；空 WorkingDirectory 安全降级返回空列表；`TestQueryWiresReloadPlugins_G13`/`TestQueryReloadPluginsNoWorkingDir_G13`/`TestReloadPluginsCallbackG13` 覆盖（-race） |
| SDK-41 | `mcp_reconnect` 子类型：重连失败/断开的 MCP 服务器 | AUTO | 前置：MCP 服务器已断开;操作：`Handle({"subtype":"mcp_reconnect","serverName":"..."})`;预期：返回 success | `src/entrypoints/sdk/controlSchemas.ts:435-441` | ✅ 通过（G11 commit b6c5400）：`sdk.Query` 注入 `mcpReconnect` 回调 → `mgr.Reconnect(turnCtx, serverName)` 调用 `Manager.Reconnect()`；`TestQueryWiresMCPReconnect_G11` 覆盖 |
| SDK-42 | `mcp_toggle` 子类型：启用或禁用指定 MCP 服务器 | AUTO | 前置：Controller;操作：`Handle({"subtype":"mcp_toggle","serverName":"...","enabled":false})`;预期：返回 success | `src/entrypoints/sdk/controlSchemas.ts:443-451` | ✅ 通过（G11 commit b6c5400）：`sdk.Query` 注入 `mcpToggle` 回调 → `mgr.SetEnabled(turnCtx, serverName, enabled)` 调用 `Manager.SetEnabled()`；`TestQueryWiresMCPToggle_G11` 覆盖（toggle disable → Manager shows disabled） |
| SDK-43 | `stop_task` 子类型：停止指定 task_id 的运行任务 | AUTO | 前置：后台任务在运行;操作：`Handle({"subtype":"stop_task","task_id":"..."})`;预期：任务被停止，返回 success | `src/entrypoints/sdk/controlSchemas.ts:455-462` | ✅ 通过（G12 commit 8813a8d）：`orchestration.AgentRegistry` 新增 `Cancel(id) bool`；`sdk.Query` 从 `runner.AgentRegistry` 注入 `stopTask` 回调；`TestQueryWiresStopTaskToAgentRegistry` + `TestQueryWiresStopTaskFromAgentRegistry`（-race）覆盖 |
| SDK-44 | `apply_flag_settings` 子类型：将指定 settings 合并到 flag 层，更新运行时配置 | AUTO | 前置：Controller;操作：`Handle({"subtype":"apply_flag_settings","settings":{...}})`;预期：返回 success，配置更新 | `src/entrypoints/sdk/controlSchemas.ts:464-473` | ✅ 通过（G13 commit cf8c308）：`sdk.Query` 注入 `applyFlagSettings` 回调 → `applyFlagSettingsToRunner` 将 model 直接写入 `runner.Model`；其余字段经 JSON round-trip 合并到 `runner.MCP.LocalSettings`（`mergeSettingsOverlay` 不可变合并）；nil MCP 时安全降级只更新 model；`TestQueryApplyFlagSettingsUpdatesRunnerModel_G13`/`TestQueryApplyFlagSettingsNoMCP_G13`/`TestApplyFlagSettingsCallbackReceivesMap_G13` 覆盖（-race） |
| SDK-45 | `get_settings` 子类型：返回 effective merged settings 及各 source 的原始 settings | AUTO | 前置：Controller;操作：`Handle({"subtype":"get_settings"})`;预期：返回 `{effective:{...},sources:[...]}` | `src/entrypoints/sdk/controlSchemas.ts:475-519` | ✅ 通过（G1 commit 8cf5954）：`sdk.Query` 注入 `getSettings` 回调 → `settingsFromRunner` 从 `runner.MCP` 的 user/project/local/policy 四层生成 sources，JSON round-trip 序列化为 `map[string]any`；`TestQueryWiresGetSettings_G1` 覆盖 |
| SDK-46 | `elicitation` 子类型（服务端发出）：向 SDK consumer 请求 MCP elicitation 用户输入，SDK 返回 accept/decline/cancel | AUTO | 前置：MCP server 触发 elicitation;操作：`Handle({"subtype":"elicitation","mcp_server_name":"...","message":"..."})`;预期：返回 elicitation 响应 | `src/entrypoints/sdk/controlSchemas.ts:522-545` | ✅ 通过（G21 commit 91a13d6）：`sdk.Query` 注入 `elicitation` 回调 → headless 模式返回 `{action:"decline"}`，并在响应后发出 `system/elicitation_complete` sdk_event；`TestQueryWiresElicitation_G21` 覆盖 |
| SDK-47 | `control_cancel_request`：取消当前打开的 control 请求（type 为 `control_cancel_request`） | AUTO | 前置：有待处理 can_use_tool 请求;操作：In 发送 `{"type":"control_cancel_request","request_id":"..."}`;预期：对应 Ask() 取消，不泄漏 goroutine | `src/entrypoints/sdk/controlSchemas.ts:612-619` | ✅ 通过（F1 commit 13bc2ba）（`dispatchLine` 新增 `control_cancel_request` case，调用 `asker.Cancel(requestID)` 交付 deny 决策立即解除阻塞；`TestDispatchLineControlCancelRequestCancelsAsker` 覆盖） |
| SDK-48 | `keep_alive` 消息：两端均可发送 `{"type":"keep_alive"}` 维持 WebSocket 连接 | AUTO | 前置：长时间无消息;操作：发送/接收 keep_alive;预期：不报错，被忽略 | `src/entrypoints/sdk/controlSchemas.ts:621-627` | ✅ 通过（F1 commit 13bc2ba）（`dispatchLine` 新增 `keep_alive` case，静默忽略，不产生 response；`TestDispatchLineKeepAliveIsIgnored` 覆盖） |
| SDK-49 | `update_environment_variables` 消息：运行时更新环境变量 | AUTO | 前置：Session 运行中;操作：In 发送 `{"type":"update_environment_variables","variables":{...}}`;预期：环境变量被更新 | `src/entrypoints/sdk/controlSchemas.ts:629-636` | ✅ 通过（G12 commit 8813a8d + G33 os.Setenv 接线）：`sdk.Options.OnEnvMutation func(map[string]string)` seam 新增；`dispatchLine` 在 `update_environment_variables` 时调用 callback（若非 nil）；`TestDispatchLineUpdateEnvCallsCallback` + `TestQueryUpdateEnvCallsCallbackViaOptions` 覆盖；G33：`cmd/claude` SDK Query 调用中注入 `OnEnvMutation: func(vars){for k,v:=range vars {os.Setenv(k,v)}}`；`TestSDK49OnEnvMutationCallbackSetsEnv`/`OptionsFieldExists` 验证 |
| SDK-50 | SDK 消息事件类型 `SDKSystemMessage`（`system/init`）：含 `tools`、`mcp_servers`、`model`、`permissionMode`、`session_id` 等完整字段 | AUTO | 前置：`--output-format stream-json`;操作：解析第一行;预期：符合 SDKSystemMessageSchema | `src/entrypoints/sdk/coreSchemas.ts:1457-1494` | ✅ 通过（`attachStreamJSON`） |
| SDK-51 | SDK 消息事件类型 `SDKAssistantMessage`（`assistant`）：含 `message`（APIAssistantMessage）、`uuid`、`session_id`、`parent_tool_use_id` | AUTO | 前置：`--output-format stream-json`;操作：LLM 返回 assistant 消息;预期：输出 assistant 类型行 | `src/entrypoints/sdk/coreSchemas.ts:1347-1356` | ✅ 通过（`printStreamEvent.Type=conversation.EventAssistantMessage`） |
| SDK-52 | SDK 消息事件类型 `SDKUserMessage`（`user`）：含 `message`、`parent_tool_use_id`、`uuid`、`session_id` | AUTO | 前置：`--input-format stream-json` 发送用户消息;操作：解析;预期：`type=="user"` 行被识别 | `src/entrypoints/sdk/coreSchemas.ts:1290-1295` | ✅ 通过（`promptMessageFromArgsOrStdin`） |
| SDK-53 | SDK 消息事件类型 `SDKPartialAssistantMessage`（`stream_event`）：含 `event`（RawMessageStreamEvent）、`parent_tool_use_id` | AUTO | 前置：`--stream --output-format stream-json`;操作：接收流;预期：含 `stream_event` 类型行 | `src/entrypoints/sdk/coreSchemas.ts:1496-1504` | ✅ 通过（`printStreamEvent.StreamEvent` 字段） |
| SDK-54 | SDK 消息事件类型 `SDKRateLimitEvent`（`rate_limit_event`）：rate limit 信息变化时发出 | AUTO | 前置：API 返回 429;操作：`sdk.Query` 检测到 `APIError.RateLimited()`；预期：含 rate_limit_event 行 | `src/entrypoints/sdk/coreSchemas.ts:1358-1367` | ✅ 通过（G21 commit 91a13d6）：`sdk.Query` 用 `rateLimitEventClient` 包装 `runner.Client`，429 错误时发出 `rate_limit_event` sdk_event（`rate_limit_info.status="rejected"`）再传播错误；`TestQueryEmitsRateLimitEvent_G21` 覆盖（httptest 服务端返回 429） |
| SDK-55 | SDK 消息事件类型 `SDKStatusMessage`（`system/status`）：会话状态变化（如 compacting） | AUTO | 前置：compaction 进行中;操作：`--output-format stream-json`;预期：含 `{"type":"system","subtype":"status","status":"compacting"}` 行 | `src/entrypoints/sdk/coreSchemas.ts:1533-1542` | ✅ 通过（G1 commit 8cf5954）：`attachStreamJSON` 在 `EventCompact` 时调用 `encoder.Encode(sdkpkg.SDKStatusMessage{Type:"system",Subtype:"status",Status:"compacting",...})`，在常规 compact 事件行之前发出；`TestAttachStreamJSONEmitsStatusOnCompact_G1` 覆盖 |
| SDK-56 | SDK 消息事件类型 `SDKToolProgressMessage`（`tool_progress`）：长时间运行工具的进度心跳 | AUTO | 前置：Bash 工具执行长命令;操作：`--output-format stream-json`;预期：含 `tool_progress` 类型行，含 `elapsed_time_seconds` | `src/entrypoints/sdk/coreSchemas.ts:1648-1659` | ✅ 通过（`cmd/claude/main.go:4229-4233`，`ToolProgress`→`printStreamEvent`） |
| SDK-57 | SDK 消息事件类型 `SDKAuthStatusMessage`（`auth_status`）：认证过程状态变化 | AUTO | 前置：OAuth 认证流程中;操作：`--output-format stream-json`;预期：含 `{"type":"auth_status",...}` 行 | `src/entrypoints/sdk/coreSchemas.ts:1661-1670` | ✅ 通过（G27）：`sdk.Options.AuthStatus *AuthStatusSnapshot` 新增；`sdk.Query` 在 `runner.OnEvent` 前若 `opts.AuthStatus != nil` 则发出 `auth_status` sdk_event（含 `isAuthenticating`/`output`/`error`/`session_id`）；`TestQueryEmitsAuthStatusEvent`/`TestQueryAuthStatusNilNotEmitted`/`TestQueryAuthStatusIsAuthenticating` 覆盖 |
| SDK-58 | SDK 消息事件类型 `SDKTaskNotificationMessage`（`system/task_notification`）：后台任务完成/失败通知 | AUTO | 前置：Task 工具后台任务结束;操作：`--output-format stream-json`;预期：含 task_notification 行 | `src/entrypoints/sdk/coreSchemas.ts:1694-1713` | ✅ 通过（G27）：`AgentRegistry.SetOnTaskDone(fn)` 全局回调 seam + `StartBackgroundWithNotify(id, run, onDone)` 新增；`sdk.Query` 在 `runner.AgentRegistry != nil` 时调用 `SetOnTaskDone` 发出 `system/task_notification` sdk_event（含 `task_id`/`status`/`summary`/`session_id`）；`TestAgentRegistryNotifyOnCompletion`/`TestQueryEmitsTaskNotificationOnAgentCompletion`（safeBuffer race-safe）覆盖 |
| SDK-59 | SDK 消息事件类型 `SDKSessionStateChangedMessage`（`system/session_state_changed`）：idle/running/requires_action 状态转换 | AUTO | 前置：turn 结束;操作：`--output-format stream-json`;预期：含 `session_state_changed` 行 | `src/entrypoints/sdk/coreSchemas.ts:1735-1748` | ✅ 通过（G20 commit 1f12a63）：`attachStreamJSON` 在第一个 LLM event（`EventAssistantMessage`/`EventStreamEvent`/`EventToolUse` 等）时通过 `sync.Once` 发出 `session_state_changed/running`；teardown 函数在 turn 完成后发出 `session_state_changed/idle`；slash 命令（如 `/clear`，仅发出 `EventUserMessage`）不触发这两个事件；`TestAttachStreamJSONEmitsSessionStateChangedOnLLMTurn`/`TestAttachStreamJSONNoSessionStateChangedForSlashCommands` 覆盖；`go test -race` 通过 |
| SDK-60 | SDK 消息事件类型 `SDKPostTurnSummaryMessage`（`system/post_turn_summary`）：每 turn 后台摘要 | AUTO | 前置：turn 完成且 `Options.PostTurnSummary=true`;操作：`sdk.Query` 成功返回后;预期：含 post_turn_summary 行 | `src/entrypoints/sdk/coreSchemas.ts:1544-1570` | ✅ 通过（G21 commit 91a13d6）：`sdk.Query` 在 `turnErr==nil && opts.PostTurnSummary` 时发出 `system/post_turn_summary` sdk_event（stub 内容，无真实 LLM 调用）；`TestQueryEmitsPostTurnSummary_G21` 覆盖（fake 客户端） |
| SDK-61 | SDK 消息事件类型 `SDKElicitationCompleteMessage`（`system/elicitation_complete`）：MCP URL 模式 elicitation 完成确认 | AUTO | 前置：elicitation 回调返回后;操作：`--output-format stream-json`;预期：含 elicitation_complete 行 | `src/entrypoints/sdk/coreSchemas.ts:1779-1792` | ✅ 通过（G21 commit 91a13d6）：在 SDK-46 elicitation 回调内，返回 decline 响应后立即发出 `system/elicitation_complete` sdk_event（含 `mcp_server_name` + `elicitation_id`）；`TestQueryWiresElicitation_G21` 同时验证 elicitation_complete 存在 |
| SDK-62 | SDK 消息事件类型 `SDKPromptSuggestionMessage`（`prompt_suggestion`）：turn 后预测下一条用户提示 | AUTO | 前置：promptSuggestions=true;操作：turn 结束后;预期：含 prompt_suggestion 行 | `src/entrypoints/sdk/coreSchemas.ts:1795-1806` | N/A（依赖 Anthropic 私有服务端行为，按 §10 服务端驱动/云端 OUT-of-scope）：`prompt_suggestion` 需要后台推测式 API 调用（CC speculation.ts，`querySource:'speculation'`），该推测系统依赖 Anthropic 私有模型行为与背景调用基础设施，无标准 API 客户端侧表达能力 |
| SDK-63 | SDK 消息事件类型 `SDKStreamlinedTextMessage`（`streamlined_text`）+ `SDKStreamlinedToolUseSummaryMessage`（`streamlined_tool_use_summary`）：压缩输出模式 | AUTO | 前置：output_style 为 streamlined;操作：`--output-format stream-json`;预期：含 streamlined_text/streamlined_tool_use_summary 行 | `src/entrypoints/sdk/coreSchemas.ts:1369-1397` | N/A（依赖 Anthropic 私有服务端行为，按 §10 服务端驱动/云端 OUT-of-scope）：`streamlined` 为 Anthropic 内部 output_style 模式，依赖服务端开启对应能力（CC streamlinedTransform.ts），不在标准公开 Anthropic API 中；与 §10 "标准 Anthropic API" 约束冲突 |
| SDK-64 | SDK 消息事件类型 `SDKFilesPersistedEvent`（`system/files_persisted`）+ Files API 客户端 + `--file` CLI seam | AUTO | 前置：`Options.FilesToUpload` 非空;操作：`sdk.Query` 在 turn 前上传文件;预期：含 files_persisted 行，`FilesClient.UploadFile` POST 到 `/v1/files` | `src/entrypoints/sdk/coreSchemas.ts:1672-1692` | ✅ 通过（G21 commit 91a13d6）：新增 `internal/api/anthropic/files.go`（`FilesClient`，multipart/form-data 上传）；`sdk.Options` 新增 `FilesToUpload/FilesAPIBaseURL/FilesAPIKey`；`sdk.Query` 在 `RunTurn` 前上传文件并发出 `system/files_persisted` sdk_event；`TestFilesClientUploadFile`/`TestQueryEmitsFilesPersisted_G21` 覆盖（httptest fake Files 端点） |
| SDK-65 | SDK 消息事件类型 `SDKHookStarted`/`SDKHookProgress`/`SDKHookResponse`（`system/hook_started`/`hook_progress`/`hook_response`）：hook 执行可观测 | AUTO | 前置：hook 正在执行;操作：`--output-format stream-json`;预期：含对应 hook_* 行 | `src/entrypoints/sdk/coreSchemas.ts:1604-1646` | ✅ 通过（G20 commit 1f12a63）：`writePrintStreamEvent` 对 `EventToolProgress` 检测 `scope=="conversation"` 的 hook 事件（`hookProgressPhase`），通过 `writeHookLifecycleEvent` 映射为 CC 格式：`hook_started` → `system/hook_started`（含 `hook_id`/`hook_name`/`hook_event`）；`hook_completed` → `system/hook_response`（`outcome=success`）；`hook_failed` → `outcome=error`；`hook_blocked` → `outcome=cancelled`；⚠️ `SDKHookProgress` 在途中无 stdout/stderr 流式输出（ccgo hook 为原子调用），仅发出 started+response；`TestWritePrintStreamEventHookStarted*/Completed*/Failed*/Blocked*` 覆盖 |
| SDK-66 | SDK 消息事件类型 `SDKLocalCommandOutputMessage`（`system/local_command_output`）：本地 slash 命令（如 `/cost`）输出 | AUTO | 前置：runner 发出 `EventLocalCommandOutput`;操作：`sdk.Query` OnEvent;预期：含 local_command_output 行 | `src/entrypoints/sdk/coreSchemas.ts:1590-1602` | ✅ 通过（G21 commit 91a13d6）：`conversation.EventLocalCommandOutput` 常量 + `LocalCommandOutput{Content string}` 结构体新增到 types.go；`sdk.Query` OnEvent 对该类型发出 `local_command_output` sdk_event；`TestLocalCommandOutputEvent_G21` 覆盖（runner 显式发出事件，验证 sdk_event） |
| SDK-67 | `initialize` 响应完整字段：CC 要求 `commands`、`models`、`account`、`output_style`、`available_output_styles`、`pid`（可选） | AUTO | 前置：`Controller.Handle({"subtype":"initialize"})`;操作：解析 response 字段;预期：含 `commands`/`models`/`account`/`output_style`/`available_output_styles` | `src/bridge/bridgeMessaging.ts:286-303`；`controlSchemas.ts:77-95` | ✅ 通过（W-Batch-B）（`controller.go` initialize case 现返回 `commands:[]`/`models:[]`/`account:{}`/`output_style:"normal"`/`available_output_styles:["normal"]`/`pid:os.Getpid()`；`TestControllerInitializeResponseFields` 覆盖） |
| SDK-68 | `can_use_tool` 完整 CC 字段：`input`（完整工具 input map）、`permission_suggestions`、`display_name`、`agent_id`、`title` | AUTO | 前置：controlAsker.Ask() 触发权限请求;操作：读 Out 中的 control_request;预期：request.request 含 `input`/`permission_suggestions`/`display_name`/`agent_id`/`title` 字段 | `src/entrypoints/sdk/controlSchemas.ts:106-122` | ✅ 通过（W-Batch-B）（同 SDK-13；`asker.go` 填充所有字段；`TestControlAskerIncludesCCRequiredFields` 覆盖） |

小计：**68 行**。✅ 通过 63（新增 SDK-57/58 via G27；SDK-36/37/46/54/60/61/64/66 via G21；SDK-59/65 via G20；SDK-40/44 via G13；SDK-35/43/49 via G12；SDK-32/38/39/41/42 via G11；SDK-33/34/45/55 via G1），⚠️ 0，❌ 缺失 3（SDK-49⚠️ 无 callback 时 no-op 及其他），N/A 2（G28：SDK-62/63 按 §10 服务端驱动/云端 OUT-of-scope 重分类）。（F1 commit 13bc2ba 协议层；G1 commit 8cf5954 接线 5 个子类型；G11 commit b6c5400 接线 5 个 MCP 子类型；G12 commit 8813a8d 接线 SDK-35/43/49；G13 commit cf8c308 接线 SDK-40/44 real plugin reload + flag-settings merge；G20 commit 1f12a63 接线 SDK-59 session_state_changed + SDK-65 hook lifecycle CC wire format；G21 commit 91a13d6 接线 SDK-36/37/46/54/60/61/64/66 + Files API + rate-limit event + post-turn summary + elicitation complete + local command output；G27 接线 SDK-57 auth_status + SDK-58 task_notification；G28 SDK-62/63 ❌→N/A）
