## 8. CLI 子命令（行为深度）

本章覆盖各子命令的**详细行为**（非纯存在性/dispatch，那属于区域 1）。MCP 子命令传输行为归区域 9；`auth` 子命令行为已在区域 1 覆盖，本章仅列 doctor / update / agents / completion / config / plugin / setup-token / install / migrate-installer 各自的行为深度。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| SUBCMD-DOCTOR-01 | `doctor` 报告安装类型（npm-global/npm-local/native/package-manager/development/unknown） | AUTO | 前置：二进制已安装；操作：`claude doctor`；预期：输出中含安装路径或安装类型字段 | `src/utils/doctorDiagnostic.ts:514`（`getDoctorDiagnostic`）、`src/screens/Doctor.tsx:100` | ✅ 通过（W-Batch-D）：`DetectInstallType()` 解析 6 种安装类型；`internal/doctor.Run()` 添加 "Install type" Check；`ExecutableFn` 注入用于测试；`TestRunChecksInstallTypePresent`/`InFormat` 验证 |
| SUBCMD-DOCTOR-02 | `doctor` 报告当前版本号 | AUTO | 前置：二进制；操作：`claude doctor`；预期：stdout 含当前版本字符串 | `src/utils/doctorDiagnostic.ts:517`（`version` 字段） | ✅ 通过（ccgo doctor 含 "Version" 检查行，detail=version） |
| SUBCMD-DOCTOR-03 | `doctor` 检查 ripgrep（rg）是否在 PATH 中 | AUTO | 前置：rg 在 PATH；操作：`claude doctor`；预期：输出含 "rg" 相关 OK 状态 | `src/utils/doctorDiagnostic.ts:589`（`getRipgrepStatus()`） | ✅ 通过（ccgo `internal/doctor` 用 exec.LookPath 检查 rg，有 OK/WARN 两种输出） |
| SUBCMD-DOCTOR-04 | `doctor` 当 rg 缺失时输出 WARN（而非 ERR），不阻断其他检查 | AUTO | 前置：PATH 中无 rg；操作：`claude doctor`；预期：含 WARN 行，exit 0 | `src/utils/doctorDiagnostic.ts:589` | ✅ 通过（ccgo `StatusWarn` 且 `HasErrors()` 返回 false） |
| SUBCMD-DOCTOR-05 | `doctor` 解析用户 settings.json，JSON 错误时输出 ERR + exit 1 | AUTO | 前置：`~/.claude/settings.json` 含无效 JSON；操作：`claude doctor`；预期：输出含 ERR，exit 1 | `src/screens/Doctor.tsx:123`（`useSettingsErrors()`） | ✅ 通过（ccgo `settingsCheck` 函数返回 `StatusError` → `HasErrors()=true` → exit 1） |
| SUBCMD-DOCTOR-06 | `doctor` 解析项目 `.claude/settings.json`，合法时输出 OK | AUTO | 前置：`<cwd>/.claude/settings.json` 含合法 JSON；操作：`claude doctor`；预期：project settings 行为 OK | `src/screens/Doctor.tsx:123` | ✅ 通过（ccgo `settingsCheck("Project settings", ...)` 有该逻辑） |
| SUBCMD-DOCTOR-07 | `doctor` 检查 `.claude/` 目录是否存在 | AUTO | 前置：CWD 下无 `.claude/`；操作：`claude doctor`；预期：输出含 "Config dir" WARN 行 | `src/screens/Doctor.tsx:168`（`userDirExists`/`projectDirExists`） | ✅ 通过（ccgo 有 "Config dir (.claude)" 检查，目录缺失→WARN） |
| SUBCMD-DOCTOR-08 | `doctor` 检测多重安装（npm-global + native 并存）并输出 warning | AUTO | 前置：系统同时有 npm 与 native 安装；操作：`claude doctor`；预期：显示多重安装 warning | `src/utils/doctorDiagnostic.ts:521`（`detectMultipleInstallations()`） | ✅ 通过（F3）：`DetectMultipleInstallations()`、`Input.AdditionalBinaryPaths` 注入、Run 中多装置 WARN 检查；`TestRunChecksMultipleInstallationsWarning` 验证 |
| SUBCMD-DOCTOR-09 | `doctor` 检测安装方法与配置不一致（install method mismatch）并输出 warning | AUTO | 前置：config.installMethod 与实际安装路径不符；操作：`claude doctor`；预期：warning 含 fix 指引 | `src/utils/doctorDiagnostic.ts:433`（`detectConfigurationIssues`） | ❌ 缺失（ccgo doctor 无 config/reality mismatch 检测） |
| SUBCMD-DOCTOR-10 | `doctor` 通过网络拉取最新 dist-tag（stable/latest）并对比当前版本 | AUTO | 前置：有网络连接；操作：`claude doctor`；预期：输出 "Latest version" 或 "up to date" | `src/screens/Doctor.tsx:131`（`getDoctorDiagnostic().then(...)` + `distTagsPromise`） | ❌ 缺失（ccgo doctor 明确声明 network-free；无版本对比） |
| SUBCMD-DOCTOR-11 | `doctor` 检查 Linux 沙箱 glob pattern warnings | AUTO | 前置：Linux 平台且 sandbox 规则含 glob；操作：`claude doctor`；预期：输出 glob pattern warning | `src/utils/doctorDiagnostic.ts:487`（`detectLinuxGlobPatternWarnings()`） | ❌ 缺失（ccgo doctor 无沙箱 glob 检查） |
| SUBCMD-DOCTOR-12 | `doctor` 检查并清理旧 PID 锁文件（`versionLockInfo`） | AUTO | 前置：`~/.local/state/claude/locks/` 下有陈旧 lock 文件；操作：`claude doctor`；预期：stale locks 被清除并报告数量 | `src/screens/Doctor.tsx:191`（`isPidBasedLockingEnabled()` + `cleanupStaleLocks`） | ❌ 缺失（ccgo 无 PID lock 体系） |
| SUBCMD-DOCTOR-13 | `doctor` 检查 MCP server 解析错误 | AUTO | 前置：`.mcp.json` 含语法错误；操作：`claude doctor`；预期：MCP parsing warnings 显示在 Doctor 界面 | `src/screens/Doctor.tsx:6`（`McpParsingWarnings`）、`src/screens/Doctor.tsx:123` | ✅ 通过（F3）：`Input.MCPConfigContent` 注入、Run 中 MCP JSON 解析 WARN/OK 检查；`TestRunChecksMCPParseError`/`TestRunChecksMCPParseOK` 验证 |
| SUBCMD-UPDATE-01 | `claude update` 打印当前版本并报告安装方法 | AUTO | 前置：二进制；操作：`claude update`；预期：stdout 含当前版本，exit 0 | `src/cli/update.ts:32`（`writeToStdout('Current version: ...')`) | ✅ 通过（ccgo `runUpdateCLI` 输出版本 + install method） |
| SUBCMD-UPDATE-02 | `claude update` 读取 `autoUpdatesChannel`（latest/stable）并据此选择 tag | AUTO | 前置：settings 中 `autoUpdatesChannel=stable`；操作：`claude update`；预期：检查 stable channel | `src/cli/update.ts:34`（`getInitialSettings()?.autoUpdatesChannel`） | ✅ 通过（F3）：`resolveAutoUpdatesChannel()` 读取 `~/.claude/settings.json`；`updateOptions.Channel`；`TestUpdateReadsAutoUpdatesChannel` 验证 |
| SUBCMD-UPDATE-03 | `claude update` 网络拉取最新版本，版本相同时输出 "up to date"，版本不同时执行下载安装 | AUTO | 前置：有网络；操作：`claude update`；预期：版本相同→"up to date"，版本新→自动安装并输出新版本号 | `src/cli/update.ts:308-420`（版本比较 + 安装流程） | ⚠️ 网络/发布基础设施缺失：check+report+guidance 已实现；自动下载安装需要实际 release 服务器，无法在无发布基础设施时实现 |
| SUBCMD-UPDATE-04 | `claude update` 检测 homebrew/winget/package-manager 安装，输出对应 `brew upgrade` / `winget upgrade` 命令 | AUTO | 前置：系统通过 homebrew 安装；操作：`claude update`；预期：输出 `brew upgrade claude-code` 指引 | `src/cli/update.ts:122-162`（package-manager 分支） | ✅ 通过（F3）：`runUpdateCLIv2` 按 PackageManager 精确分支输出 `brew upgrade claude-code`/`winget upgrade Anthropic.ClaudeCode`；`TestUpdateHomebrewInstallCommand`/`TestUpdateWingetInstallCommand` 验证 |
| SUBCMD-UPDATE-05 | `claude update` 检测多重安装并警告 | AUTO | 前置：系统有 npm + native 双重安装；操作：`claude update`；预期：warning 列出各安装路径 | `src/cli/update.ts:48-58`（`multipleInstallations` 检查） | ✅ 通过（F3）：`updateOptions.MultipleInstallations`；`TestUpdateMultipleInstallationsWarning` 验证 |
| SUBCMD-UPDATE-06 | `claude update` 对 development build 输出 warning 并 exit 1 | AUTO | 前置：NODE_ENV=development 构建；操作：`claude update`；预期：stderr 含 "Cannot update development build"，exit 非 0 | `src/cli/update.ts:108-115`（`installationType==='development'` 分支） | ✅ 通过（F3）：`runUpdateCLIv2` 中 installType=="development" → warning + exit 1；`TestUpdateDevelopmentBuildWarning` 验证 |
| SUBCMD-UPDATE-07 | `claude update`（或 `upgrade`）的别名行为一致 | AUTO | 前置：二进制；操作：`claude upgrade`；预期：与 `claude update` 相同输出 | `src/main.tsx:4362`（`.alias('upgrade')`） | ✅ 通过（F3）：main.go dispatch 中 `upgrade` 与 `update` 共用同一分支；`TestUpdateUpgradeAlias` 验证 |
| SUBCMD-AGENTS-01 | `claude agents` 无代理文件时输出 "(none)" 占位符 | AUTO | 前置：`~/.claude/agents/` 和 `<cwd>/.claude/agents/` 均为空/不存在；操作：`claude agents`；预期：stdout 含 "(none)"，exit 0 | `src/cli/handlers/agents.ts:61`（`console.log('No agents found.')`) | ✅ 通过（ccgo `printAgentList` 输出 "(none)" 占位符） |
| SUBCMD-AGENTS-02 | `claude agents` 列出项目代理（`<cwd>/.claude/agents/*.md`） | AUTO | 前置：`<cwd>/.claude/agents/reviewer.md` 存在；操作：`claude agents`；预期：stdout 含 "reviewer" | `src/cli/handlers/agents.ts:34`（`getAgentDefinitionsWithOverrides(cwd)`） | ✅ 通过（ccgo `agentfile.ProjectDir(cwd)` + `agentfile.List`） |
| SUBCMD-AGENTS-03 | `claude agents` 列出用户代理（`~/.claude/agents/*.md`） | AUTO | 前置：`~/.claude/agents/helper.md` 存在；操作：`claude agents`；预期：stdout 含 "helper" | `src/cli/handlers/agents.ts:34`（`userAgentsDir` 分组） | ✅ 通过（ccgo `agentfile.UserDir()` + `agentfile.List`） |
| SUBCMD-AGENTS-04 | `claude agents` 输出按 source 分组（User / Project / Local / Plugin / Built-in）并显示 model | AUTO | 前置：user 代理有 model 字段；操作：`claude agents`；预期：每组有标题，model 显示在代理名后 | `src/cli/handlers/agents.ts:20-30`（`formatAgent` 含 model + memory）、`src/tools/AgentTool/agentDisplay.ts:24`（`AGENT_SOURCE_GROUPS`） | ❌ 缺失（ccgo 仅分两组：Project / User，无 Local/Plugin/Built-in；不展示 model 字段） |
| SUBCMD-AGENTS-05 | `claude agents` 标注被高优先级同名代理覆盖（shadowed）的代理 | AUTO | 前置：project 和 user 目录中同名 agent.md；操作：`claude agents`；预期：低优先级项显示 "(shadowed by ...)" | `src/cli/handlers/agents.ts:49-53`（`agent.overriddenBy` 判断） | ❌ 缺失（ccgo 无覆盖/shadow 逻辑，两个目录的同名代理均会显示） |
| SUBCMD-AGENTS-06 | 代理文件格式：frontmatter 含 name/description/tools/model/effort/color/memory，body 为 system prompt | AUTO | 前置：合法 `.md` 代理文件；操作：`claude agents`；预期：代理被正确解析，description 显示 | `src/tools/AgentTool/loadAgentsDir.ts:73`（`AgentJsonSchema`）、ccgo `internal/agentfile/agentfile.go:17` | ✅ 通过（ccgo `AgentFile` struct 含全部字段，`Parse` 函数覆盖全部 frontmatter 字段） |
| SUBCMD-AGENTS-07 | 代理文件名必须匹配 `[a-zA-Z0-9_-]+`，否则 Parse 报错 | AUTO | 前置：`~/.claude/agents/bad name.md` 含空格；操作：`claude agents`；预期：该文件被跳过或报错，不崩溃 | `src/tools/AgentTool/loadAgentsDir.ts`（Zod schema 校验）、ccgo `agentfile.go:51` | ✅ 通过（ccgo `validName` 正则校验，Parse 返回 error） |
| SUBCMD-COMPLETION-01 | `claude completion bash` 输出合法 bash 补全脚本（含 `complete -F` 行） | AUTO | 前置：二进制；操作：`claude completion bash`；预期：stdout 含 `complete -F _claude_completions claude`，exit 0 | `src/main.tsx:4492`（`program.command('completion <shell>')`） | ✅ 通过（ccgo `runCompletionCLI` 输出 `bashCompletionTemplate`） |
| SUBCMD-COMPLETION-02 | `claude completion zsh` 输出合法 zsh 补全脚本（含 `#compdef claude`） | AUTO | 前置：二进制；操作：`claude completion zsh`；预期：stdout 含 `#compdef claude`，exit 0 | `src/main.tsx:4492` | ✅ 通过（ccgo 输出 `zshCompletionTemplate`） |
| SUBCMD-COMPLETION-03 | `claude completion fish` 输出合法 fish 补全脚本（含 `complete -c claude -f`） | AUTO | 前置：二进制；操作：`claude completion fish`；预期：stdout 含 `complete -c claude`，exit 0 | `src/main.tsx:4492` | ✅ 通过（ccgo 输出 `fishCompletionTemplate`） |
| SUBCMD-COMPLETION-04 | `claude completion <unknown>` 以非 0 退出并输出 usage | AUTO | 前置：二进制；操作：`claude completion powershell`；预期：stderr 含 "supported shells"，exit 1 | `src/main.tsx:4492` | ✅ 通过（ccgo `default` 分支输出错误并返回 1） |
| SUBCMD-COMPLETION-05 | `claude completion` 无参数以非 0 退出 | AUTO | 前置：二进制；操作：`claude completion`（无 shell 参数）；预期：stderr 含 usage，exit 1 | `src/main.tsx:4492`（`<shell>` 是必须参数） | ✅ 通过（ccgo `len(args)==0` 时返回 1 并打印 usage） |
| SUBCMD-COMPLETION-06 | `claude completion bash --output <file>` 将脚本写入文件而非 stdout | AUTO | 前置：二进制；操作：`claude completion bash --output /tmp/claude.bash`；预期：文件被创建，stdout 为空 | `src/main.tsx:4494`（`--output <file>` option） | ❌ 缺失（ccgo `runCompletionCLI` 无 `--output` 标志） |
| SUBCMD-CONFIG-01 | `claude config` 在交互式 REPL 中打开配置面板（TUI 覆盖层） | MANUAL | 前置：REPL 已启动；操作：输入 `/config`；预期：Settings 面板弹出，可浏览 Config 标签页 | `src/commands/config/config.tsx:5`（`<Settings defaultTab="Config" />`） | ❌ 缺失（ccgo 无 Settings TUI 覆盖层；`/config` slash 命令未实现） |
| SUBCMD-CONFIG-02 | `claude config` 同时注册 `settings` 别名，两者行为相同 | MANUAL | 前置：REPL 已启动；操作：`/settings`；预期：与 `/config` 相同效果 | `src/commands/config/index.ts:4`（`aliases: ['settings']`） | ❌ 缺失（ccgo 无 `/settings` 别名） |
| SUBCMD-PLUGIN-01 | `claude plugin list` 列出已安装插件（ID/版本/scope/状态） | AUTO | 前置：已安装至少一个插件；操作：`claude plugin list`；预期：stdout 列出 plugin ID 和版本，exit 0 | `src/main.tsx:4159`（`pluginCmd.command('list')`） | ✅ 通过（ccgo `runPluginListCLI` 输出已安装插件列表） |
| SUBCMD-PLUGIN-02 | `claude plugin list --json` 以 JSON 输出插件列表 | AUTO | 前置：有插件；操作：`claude plugin list --json`；预期：stdout 为合法 JSON 数组，exit 0 | `src/main.tsx:4159`（`--json` option） | ✅ 通过（ccgo `--json` 标志走 `encoder.Encode(installed)`） |
| SUBCMD-PLUGIN-03 | `claude plugin list --json --available` 同时列出已安装和可安装插件 | AUTO | 前置：已配置 marketplace；操作：`claude plugin list --json --available`；预期：JSON 含 `installed` + `available` 字段 | `src/main.tsx:4159`（`--available` requires `--json`） | ✅ 通过（ccgo `pluginCLIAvailableList` struct 含双字段） |
| SUBCMD-PLUGIN-04 | `claude plugin install <plugin>` 从 marketplace 安装插件 | AUTO | 前置：已配置 marketplace，插件 ID 合法；操作：`claude plugin install my-plugin`；预期：插件被安装到 user scope，exit 0 | `src/main.tsx:4209`（`pluginCmd.command('install <plugin>')`） | ✅ 通过（ccgo `runPluginInstallCLI` 实现） |
| SUBCMD-PLUGIN-05 | `claude plugin install <plugin@marketplace>` 指定 marketplace 安装 | AUTO | 前置：有多个 marketplace；操作：`claude plugin install foo@mymarket`；预期：从 mymarket 安装 foo | `src/main.tsx:4209`（`plugin@marketplace` 格式） | ✅ 通过（ccgo `parsePluginRef` 解析 `plugin@marketplace` 格式） |
| SUBCMD-PLUGIN-06 | `claude plugin install --scope project` 安装到项目 scope | AUTO | 前置：在 git 项目中；操作：`claude plugin install foo --scope project`；预期：插件安装在 `<cwd>/.claude/plugins/`，exit 0 | `src/main.tsx:4209`（`-s, --scope <scope>`） | ✅ 通过（ccgo `--scope project/user/local` 支持） |
| SUBCMD-PLUGIN-07 | `claude plugin uninstall <plugin>` 卸载已安装插件 | AUTO | 前置：已安装 foo 插件；操作：`claude plugin uninstall foo`；预期：插件被移除，exit 0 | `src/main.tsx:4220`（`pluginCmd.command('uninstall <plugin>')`） | ✅ 通过（ccgo `runPluginUninstallCLI` 实现） |
| SUBCMD-PLUGIN-08 | `claude plugin uninstall --keep-data` 保留插件数据目录 | AUTO | 前置：已安装含数据的插件；操作：`claude plugin uninstall foo --keep-data`；预期：插件移除，`~/.claude/plugins/data/foo/` 保留 | `src/main.tsx:4220`（`--keep-data` option） | ✅ 通过（ccgo `--keep-data` 标志，`writePluginUninstallResult` 报告） |
| SUBCMD-PLUGIN-09 | `claude plugin enable <plugin>` 启用已禁用插件 | AUTO | 前置：foo 插件已禁用；操作：`claude plugin enable foo`；预期：settings 中 foo 状态改为 enabled，exit 0 | `src/main.tsx:4232`（`pluginCmd.command('enable <plugin>')`） | ✅ 通过（ccgo `runPluginEnableDisableCLI(enabled=true)`） |
| SUBCMD-PLUGIN-10 | `claude plugin disable <plugin>` 禁用已启用插件 | AUTO | 前置：foo 插件已启用；操作：`claude plugin disable foo`；预期：settings 中 foo 状态改为 disabled，exit 0 | `src/main.tsx:4243`（`pluginCmd.command('disable [plugin]')`） | ✅ 通过（ccgo `runPluginEnableDisableCLI(enabled=false)`） |
| SUBCMD-PLUGIN-11 | `claude plugin disable --all` 禁用所有已启用插件 | AUTO | 前置：多个已启用插件；操作：`claude plugin disable --all`；预期：所有插件被禁用，exit 0 | `src/main.tsx:4243`（`-a, --all` option） | ✅ 通过（ccgo `--all` 标志走 `SetPluginsEnabledInSettingsFile`） |
| SUBCMD-PLUGIN-12 | `claude plugin update <plugin>` 将插件更新到最新版本 | AUTO | 前置：已安装旧版本 foo 插件，marketplace 有新版；操作：`claude plugin update foo`；预期：插件更新，提示重启生效 | `src/main.tsx:4255`（`pluginCmd.command('update <plugin>')`） | ❌ 缺失（ccgo `pluginCLIUsage` 中 `update` 仅列在 usage 字符串里，无实际实现函数） |
| SUBCMD-PLUGIN-13 | `claude plugin validate <path>` 校验插件/marketplace manifest | AUTO | 前置：有 plugin manifest 文件；操作：`claude plugin validate ./plugin.json`；预期：manifest 合法时 exit 0，有错误时输出详情 exit 1 | `src/main.tsx:4149`（`pluginCmd.command('validate <path>')`） | ❌ 缺失（ccgo main.go plugin dispatch 中无 `validate` case） |
| SUBCMD-PLUGIN-14 | `claude plugin marketplace add <source>` 添加 marketplace | AUTO | 前置：有 marketplace URL；操作：`claude plugin marketplace add https://example.com/market`；预期：marketplace 写入 settings，exit 0 | `src/main.tsx:4172`（`marketplaceCmd.command('add <source>')`） | ✅ 通过（ccgo `runMarketplaceAddCLI` 实现） |
| SUBCMD-PLUGIN-15 | `claude plugin marketplace add --scope project` 写入项目 settings | AUTO | 前置：在项目目录；操作：`claude plugin marketplace add <url> --scope project`；预期：marketplace 写入 `<cwd>/.claude/settings.json` | `src/main.tsx:4172`（`--scope <scope>`） | ✅ 通过（ccgo `scopeSettingsPath` 按 scope 选择 settings 文件） |
| SUBCMD-PLUGIN-16 | `claude plugin marketplace add --sparse` 对 monorepo git marketplace 使用 sparse-checkout | AUTO | 前置：monorepo marketplace；操作：`claude plugin marketplace add <url> --sparse .claude-plugin`；预期：仅 checkout 指定目录 | `src/main.tsx:4172`（`--sparse <paths...>` option） | ❌ 缺失（ccgo `runMarketplaceAddCLI` 无 `--sparse` 支持） |
| SUBCMD-PLUGIN-17 | `claude plugin marketplace remove <name>` 移除 marketplace 配置 | AUTO | 前置：已配置 marketplace "mymarket"；操作：`claude plugin marketplace remove mymarket`；预期：marketplace 从 settings 移除，exit 0 | `src/main.tsx:4191`（`marketplaceCmd.command('remove <name>')`） | ✅ 通过（ccgo `runMarketplaceRemoveCLI` 实现） |
| SUBCMD-PLUGIN-18 | `claude plugin marketplace list` 列出所有已配置的 marketplace | AUTO | 前置：有配置 marketplace；操作：`claude plugin marketplace list`；预期：stdout 列出 marketplace 名称和来源，exit 0 | `src/main.tsx:4182`（`marketplaceCmd.command('list')`） | ✅ 通过（ccgo `runMarketplaceListCLI` 实现） |
| SUBCMD-PLUGIN-19 | `claude plugin marketplace search <query>` 在 marketplace 中搜索插件 | AUTO | 前置：已配置含插件的 marketplace；操作：`claude plugin marketplace search foo`；预期：输出匹配的插件列表，显示安装状态 | `src/main.tsx`（`marketplaceCmd` 子命令）、ccgo `runMarketplaceSearchCLI` | ✅ 通过（ccgo `runMarketplaceSearchCLI` 实现，支持 query 过滤） |
| SUBCMD-PLUGIN-20 | `claude plugin` 无子命令时输出 usage 并 exit 非 0 | AUTO | 前置：二进制；操作：`claude plugin`（无子命令）；预期：stderr 含 usage 说明，exit 1 | `src/main.tsx:4148`（plugin 命令结构） | ✅ 通过（ccgo dispatch 中无子命令时 `fmt.Fprintln(stderr, pluginCLIUsage())`，exit 1） |
| SUBCMD-SETUP-TOKEN-01 | `claude setup-token` 启动 OAuth 流程生成 1 年期 inference-only token | MANUAL | 前置：有 Claude 订阅账户；操作：`claude setup-token`；预期：浏览器打开授权页，完成后 token 写入 keychain，输出成功信息 | `src/main.tsx:4267`（`program.command('setup-token')`）、`src/cli/handlers/util.tsx:20`（`setupTokenHandler`） | ✅ 通过（F3）：`runSetupTokenCLI` 实现；main.go dispatch 注册；`TestSetupTokenCommandRegistered` 验证 |
| SUBCMD-SETUP-TOKEN-02 | `setup-token` 若已有 auth（env var / keychain）则显示 warning 提示已有认证 | MANUAL | 前置：`ANTHROPIC_API_KEY` 已设置；操作：`claude setup-token`；预期：显示 warning "You already have authentication configured" | `src/cli/handlers/util.tsx:22`（`showAuthWarning = !isAnthropicAuthEnabled()`） | ✅ 通过（F3）：检查 `ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_REFRESH_TOKEN`；`TestSetupTokenWarnsWhenAlreadyAuthenticated` 验证 |
| SUBCMD-SETUP-TOKEN-03 | `setup-token` 生成的 token 仅有 `user:inference` scope，不含管理权限 | MANUAL | 前置：完成 setup-token 流程；操作：用生成 token 调用管理 API；预期：权限拒绝 | `src/components/ConsoleOAuthFlow.tsx:202`（`inferenceOnly: mode === 'setup-token'`） | ✅ 通过（F3）：`LoginOptions.InferenceOnly=true` → URL scope=user:inference；`TestSetupTokenUsesInferenceOnlyScope` 验证（URL capture via OnURL hook） |
| SUBCMD-INSTALL-01 | `claude install` 检测当前安装状态并安装/升级到 native build | MANUAL | 前置：未安装 native build；操作：`claude install`；预期：下载最新 native binary 到 `~/.local/bin/claude`，输出成功信息 | `src/main.tsx:4395`（`program.command('install [target]')`）、`src/commands/install.tsx:12` | ⚠️ 结构已实现；下载部分需要 release 服务器：`runInstallCLI` 注册、flag 解析、install type 报告；actual binary download 需要 live release infra |
| SUBCMD-INSTALL-02 | `claude install <target>` 安装指定版本（stable/latest/x.y.z） | MANUAL | 前置：二进制；操作：`claude install stable`；预期：下载 stable channel 最新版，输出版本号 | `src/main.tsx:4395`（`[target]` 参数）、`src/commands/install.tsx:17`（`target?: string`) | ⚠️ target 参数解析已实现；下载需 live release infra |
| SUBCMD-INSTALL-03 | `claude install --force` 强制重新安装即使版本未变 | MANUAL | 前置：已安装同版本；操作：`claude install --force`；预期：重新安装，输出成功，不因"已是最新"跳过 | `src/main.tsx:4395`（`--force` option）、`src/commands/install.tsx:19` | ⚠️ --force 标志解析已实现；下载逻辑需 live release infra |
| SUBCMD-INSTALL-04 | `claude install` 清理旧 npm 安装（npm-global/npm-local）再安装 native | MANUAL | 前置：系统有遗留 npm 安装；操作：`claude install`；预期：输出 "Cleaning up old npm installations"，npm 安装被移除 | `src/commands/install.tsx:26`（`type: 'cleaning-npm'`）、`cleanupNpmInstallations` | ⚠️ install type 检测已实现；npm 卸载+native 安装 pipeline 需 live release infra |

小计：55 行，✅ 通过 32，⚠️ 已建未接 5，❌ 缺失 18，N/A 0。（F3 新增 ✅ +10，⚠️ +5，❌ -15）
