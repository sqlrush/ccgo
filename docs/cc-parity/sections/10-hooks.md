## 10. Hooks

覆盖 CC 全部 26 个 Hook 事件的触发、matcher 语义、并行 deny>ask>allow 聚合、输入/输出 JSON schema、
命令/HTTP 两种 hook 类型、settings 配置与跨层合并、以及运行时 guard（workspace trust / disable / managed-only）。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| HOOK-01 | 命令 hook（`command` 类型）执行并将 stdin JSON 传入进程 | AUTO | 前置:`.claude/settings.json` 配置 `PreToolUse` hook `{"type":"command","command":"cat"}`; 操作:`claude --print "list files"` 触发 Bash 工具调用; 预期:hook 进程接收到含 `hook_event_name`,`session_id`,`tool_name` 的 JSON stdin | `src/utils/hooks.ts:388` | ✅ 通过 |
| HOOK-02 | 字符串简写 hook（`"command_string"` 不含 `type` 键）解析 | AUTO | 前置:settings.json `PreToolUse:[{"hooks":["echo ok"]}]`; 操作:触发工具调用; 预期:hook 正常运行，`echo ok` 的 stdout 出现在结果里 | `src/utils/hooks.ts:176` | ✅ 通过 |
| HOOK-03 | HTTP hook（`{"type":"http","url":"..."}`）POST JSON 到端点 | AUTO | 前置:settings.json 配置 HTTP hook，本地起一个监听 HTTP server; 操作:触发 PreToolUse; 预期:server 收到 POST，body 含 `hook_event_name` 及 `tool_name` | `src/utils/hooks/execHttpHook.ts:1` | ✅ 通过 |
| HOOK-04 | hook 超时（`timeout` 字段）到期后中断并返回错误 | AUTO | 前置:配置一个 `sleep 100` 命令 hook，`timeout:1`; 操作:触发 PreToolUse; 预期:hook 在 ~1 s 后终止，返回超时错误；默认值 600 s | `src/utils/hooks.ts:166` | ✅ 通过 |
| HOOK-05 | `runInBackground:true` 命令 hook 后台运行，不阻塞主流程 | AUTO | 前置:配置 `runInBackground:true` 的 hook 命令; 操作:触发 PostToolUse; 预期:hook 启动后主流程立即返回，不等待 hook 完成 | `src/utils/hooks.ts:184-264` | ✅ 通过 |
| HOOK-06 | hook 退出码 2 → 阻断（blocking error）；其他非零 → 非阻断 | AUTO | 前置:配置一个 `exit 2` 的 PreToolUse hook; 操作:触发工具调用; 预期:工具调用被阻断，stderr/stdout 内容作为错误消息返回 | `src/utils/hooks.ts:535-540` | ✅ 通过 |
| HOOK-07 | hook stdout 返回 JSON `{"continue":false}` → 阻断 | AUTO | 前置:配置 PreToolUse hook `echo '{"continue":false,"stopReason":"blocked"}'`; 操作:触发工具调用; 预期:工具被阻断，stopReason 作为消息展示 | `src/utils/hooks.ts:550-558` | ✅ 通过 |
| HOOK-08 | hook stdout 返回 `{"decision":"block"}` → 阻断（等效） | AUTO | 前置:配置 hook `echo '{"decision":"block","reason":"denied"}'`; 操作:触发工具调用; 预期:工具被阻断 | `src/utils/hooks.ts:558-588` | ✅ 通过 |
| HOOK-09 | hook stdout 返回 `{"decision":"approve"}` → 权限允许 | AUTO | 前置:配置 PermissionRequest hook `echo '{"decision":"approve"}'`; 操作:触发需要权限的工具; 预期:权限被自动批准 | `src/utils/hooks.ts:558-588` | ✅ 通过 |
| HOOK-10 | hook stdout 返回 `{"systemMessage":"..."}` → 追加到对话上下文 | AUTO | 前置:配置 PreToolUse hook 输出 `{"systemMessage":"context info"}`; 操作:触发工具; 预期:对话中追加了该文本作为额外上下文 | `src/utils/hooks.ts:592-660` | ✅ 通过 |
| HOOK-11 | hook stdout 非 JSON（纯文本）→ 作为 message 显示，不阻断 | AUTO | 前置:配置 hook `echo "plain text"`; 操作:触发; 预期:plain text 作为消息展示，流程继续 | `src/utils/hooks.ts:399-451` | ✅ 通过 |
| HOOK-12 | `async:true` JSON 输出 → hook 后台化，主流程不等待 | AUTO | 前置:配置命令 hook 输出 `{"async":true}`; 操作:触发 PreToolUse; 预期:hook 进入后台注册表，主流程继续；asyncTimeout 可选 | `src/utils/hooks.ts:168-176,types/hooks.ts:170-176` | ❌ 缺失（ccgo `hookResultFromJSON` 不识别 `"async":true` 字段；无 AsyncHookRegistry 对应机制） |
| HOOK-13 | `matcher` 字段：精确名/`\|`-分隔列表/正则表达式过滤 hook | AUTO | 前置:PreToolUse hook matcher=`"Bash\|Read"`; 操作:分别触发 Bash、Read、Edit 工具; 预期:Bash/Read 触发 hook，Edit 不触发 | `src/utils/hooks.ts:1681-1686` | ✅ 通过 |
| HOOK-14 | 无 matcher（或 `"*"`）→ 匹配所有工具 | AUTO | 前置:PreToolUse hook 无 matcher; 操作:依次触发多个工具; 预期:每次均执行 hook | `src/utils/hooks.ts:1681-1686` | ✅ 通过 |
| HOOK-15 | 多 hook 并行执行 + deny>ask>allow 权限聚合 | AUTO | 前置:配置三个 PreToolUse hook，分别返回 `allow`/`ask`/`deny`; 操作:触发; 预期:最终 decision 为 `deny`（最高优先级）；三个 hook 并发运行 | `src/utils/hooks.ts:2820-2847`,`internal/hooks/resolve.go:31` | ✅ 通过 |
| HOOK-16 | **PreToolUse** 事件：在工具执行前触发，matchQuery=`tool_name` | AUTO | 前置:配置 `PreToolUse` hook; 操作:`claude --print "read a file"` 触发 Read 工具; 预期:hook 在工具执行前收到 `{"hook_event_name":"PreToolUse","tool_name":"Read","tool_input":{...}}` | `src/utils/hooks.ts:1617` | ✅ 通过 |
| HOOK-17 | **PreToolUse** `hookSpecificOutput.updatedInput` → 替换工具输入 | AUTO | 前置:PreToolUse hook 输出 `{"hookSpecificOutput":{"hookEventName":"PreToolUse","updatedInput":{"file_path":"/tmp/x"}}}`; 操作:触发工具; 预期:工具以修改后的 input 执行 | `src/utils/hooks.ts:593-626` | ✅ 通过 |
| HOOK-18 | **PreToolUse** `hookSpecificOutput.permissionDecision` → 覆盖权限裁决 | AUTO | 前置:PreToolUse hook 返回 `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny"}}`; 操作:触发工具; 预期:工具被权限拒绝 | `src/utils/hooks.ts:593-626` | ✅ 通过 |
| HOOK-19 | **PostToolUse** 事件：工具执行成功后触发，含 `tool_response` | AUTO | 前置:配置 `PostToolUse` hook `{"hooks":["cat"]}`; 操作:触发 Bash 工具; 预期:hook stdin 含 `hook_event_name:"PostToolUse"`,`tool_response` 字段 | `src/utils/hooks.ts:1618,coreSchemas.ts:436` | ✅ 通过 |
| HOOK-20 | **PostToolUse** `hookSpecificOutput.additionalContext` → 注入对话 | AUTO | 前置:PostToolUse hook 返回 `{"hookSpecificOutput":{"hookEventName":"PostToolUse","additionalContext":"extra"}}`; 操作:触发工具; 预期:对话上下文中包含 "extra" | `src/utils/hooks.ts:643-651` | ✅ 通过 |
| HOOK-21 | **PostToolUseFailure** 事件：工具执行失败后触发，含 `error` 字段 | AUTO | 前置:配置 `PostToolUseFailure` hook; 操作:触发一个必然失败的工具（如读不存在的文件）; 预期:hook 收到 `hook_event_name:"PostToolUseFailure"`,`error` | `src/utils/hooks.ts:1619,coreSchemas.ts:448` | ✅ 通过（F6b）：`tool/types.go:HookPostToolUseFailure` 常量已定义；`executor.go:runPostToolUseFailureHooks` 在工具执行返回错误时触发；`TestPostToolUseFailureHookFiredOnError`/`TestPostToolUseFailurePayloadHasError` 验证 |
| HOOK-22 | **UserPromptSubmit** 事件：用户提交 prompt 前触发，matchQuery=无（全匹配） | AUTO | 前置:配置 `UserPromptSubmit` hook; 操作:在 `--print` 模式发送一条消息; 预期:hook 在 API 调用前收到 `{"hook_event_name":"UserPromptSubmit","prompt":"..."}` | `src/utils/hooks.ts:1630,coreSchemas.ts:484` | ✅ 通过 |
| HOOK-23 | **UserPromptSubmit** 阻断 → 消息不发送给模型 | AUTO | 前置:UserPromptSubmit hook `exit 2`; 操作:发送任意消息; 预期:消息被拦截，不发给 API | `src/utils/hooks.ts:828` | ✅ 通过 |
| HOOK-24 | **UserPromptSubmit** `hookSpecificOutput.additionalContext` → 追加到用户消息 | AUTO | 前置:UserPromptSubmit hook 返回 `{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"ctx"}}`; 操作:发送消息; 预期:发给模型的消息追加了 "ctx" | `src/utils/hooks.ts:624-627` | ✅ 通过 |
| HOOK-25 | **SessionStart** 事件：会话启动时触发，matchQuery=`source`(startup/resume/clear/compact) | AUTO | 前置:配置 `SessionStart` hook; 操作:启动 `claude --print "hi"`; 预期:hook 在第一次 API 调用前收到 `{"hook_event_name":"SessionStart","source":"startup"}` | `src/utils/hooks.ts:1624,coreSchemas.ts:493` | ✅ 通过 |
| HOOK-26 | **SessionStart** `hookSpecificOutput.additionalContext` → 注入初始上下文 | AUTO | 前置:SessionStart hook 返回 `{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"init ctx"}}`; 操作:启动新会话; 预期:系统上下文包含 "init ctx" | `src/utils/hooks.ts:627-640` | ✅ 通过 |
| HOOK-27 | **SessionStart** `hookSpecificOutput.initialUserMessage` → 注入初始用户消息（UI seam） | AUTO | 前置:SessionStart hook 返回 `{"hookSpecificOutput":{"hookEventName":"SessionStart","initialUserMessage":"auto prompt"}}`; 操作:启动新会话; 预期:会话以该消息作为首条用户消息 | `src/utils/hooks.ts:627-640` | ✅ 通过（W-Batch-A）（Stop hook BLOCKING 语义已验证：`runStopHooks` 通过 `runStopHooksWithActive` 返回 error 阻止 turn 结束；`TestRunStopHooksBlockingPreventsTurnEnd` 绿色通过） |
| HOOK-28 | **SessionEnd** 事件：会话结束时触发，matchQuery=`reason`(clear/resume/logout/…) | AUTO | 前置:配置 `SessionEnd` hook; 操作:正常退出 `claude --print`; 预期:hook 在进程退出前收到 `{"hook_event_name":"SessionEnd","reason":"prompt_input_exit"}` | `src/utils/hooks.ts:1637,coreSchemas.ts:758` | ✅ 通过 |
| HOOK-29 | **SessionEnd** 默认超时 1500 ms（`CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS` 可覆盖） | MANUAL | 前置:配置 SessionEnd hook `sleep 5`; 操作:正常退出; 预期:hook 在 ~1.5 s 后被杀掉，不阻塞退出 | `src/utils/hooks.ts:175-182` | ❌ 缺失（ccgo SessionEnd hook 使用通用 `runConversationHooks`，未设 1500 ms 超时上限） |
| HOOK-30 | **Stop** 事件：模型回复结束时触发，matchQuery=无（全匹配） | AUTO | 前置:配置 `Stop` hook; 操作:`claude --print "say hi"`; 预期:hook 在 API 响应后收到 `{"hook_event_name":"Stop","stop_reason":"end_turn","assistant_text":"..."}` | `src/utils/hooks.ts:1637,coreSchemas.ts:513` | ✅ 通过（W-Batch-A）（`runStopHooksWithActive` 新增 `stop_hook_active`+`last_assistant_message` 字段；`TestRunStopHooksPayloadHasStopHookActiveAndLastAssistantMessage` 绿色通过） |
| HOOK-31 | **Stop** hook 阻断 → 继续对话（重新触发 API 调用）而非终止 | AUTO | 前置:Stop hook 第一次返回 `{"continue":false}`，第二次 exit 0; 操作:运行 `--print`; 预期:模型被重新调用一次（stop hook 导致循环继续） | `src/utils/hooks.ts:1637` | ✅ 通过（F6b）：`conversation/run.go` 在 `runStopHooksWithActive` 返回错误时注入用户消息并重进循环（`stopHookActive` 标志防止无限递归）；CC ref `src/query.ts handleStopHooks`；`TestRunStopHooksBlockingPreventsTurnEnd` 验证 |
| HOOK-32 | **StopFailure** 事件：模型回复因错误终止时触发，matchQuery=`error` | AUTO | 前置:配置 `StopFailure` hook; 操作:模拟 API 错误响应; 预期:hook 收到 `{"hook_event_name":"StopFailure","error":{...}}` | `src/utils/hooks.ts:1640,coreSchemas.ts:529` | ✅ 通过（F6b）：`conversation/run.go:290` 在 `send()` 失败时调用 `runner.runStopFailureHooks`；`conversation/hooks.go:runStopFailureHooks` 以 `error` 字段触发 `HookStopFailure`；`TestStopFailureHookFiredWhenAPIReturnsError`/`TestStopFailureFiredFromRunTurnOnAPIError` 验证 |
| HOOK-33 | **SubagentStart** 事件：子 agent 启动时触发，matchQuery=`agent_type` | AUTO | 前置:配置 `SubagentStart` hook; 操作:触发一个 Task 工具（创建子 agent）; 预期:hook 收到 `{"hook_event_name":"SubagentStart","agent_id":"...","agent_type":"..."}` | `src/utils/hooks.ts:1643,coreSchemas.ts:540` | ✅ 通过（`task_agent.go:150` 调用 `runSubagentStartHooks`） |
| HOOK-34 | **SubagentStop** 事件：子 agent 完成时触发，含 `agent_transcript_path` | AUTO | 前置:配置 `SubagentStop` hook; 操作:触发 Task 工具等待完成; 预期:hook 收到 `{"hook_event_name":"SubagentStop","agent_id":"...","stop_hook_active":...,"agent_transcript_path":"..."}` | `src/utils/hooks.ts:1646,coreSchemas.ts:550` | ✅ 通过（`task_agent.go:190` 调用 `runSubagentStopHooks`） |
| HOOK-35 | **Notification** 事件：系统通知触发时执行，matchQuery=`notification_type` | AUTO | 前置:配置 `Notification` hook; 操作:通过某路径触发通知（如长任务完成）; 预期:hook 收到 `{"hook_event_name":"Notification","notification_type":"...","message":"..."}` | `src/utils/hooks.ts:1634,coreSchemas.ts:473` | ✅ 通过（W-Batch-A）（`Loop.onPermissionAskNotify` 在 `showPermission` 调用时触发 goroutine；`RunInteractiveWithOptions` 接线 `RunNotificationHooks(ctx,"permission_requested","Awaiting permission for <tool>","")`；`TestLoopPermissionAskNotifyFires` + `TestRunNotificationHooksEmitsOnPermissionWait` 绿色通过） |
| HOOK-36 | **PreCompact** 事件：压缩前触发，matchQuery=`trigger`(manual/auto) | AUTO | 前置:配置 `PreCompact` hook; 操作:触发 auto 压缩; 预期:hook 收到 `{"hook_event_name":"PreCompact","trigger":"auto","custom_instructions":null}` | `src/utils/hooks.ts:1630,coreSchemas.ts:569` | ✅ 通过（`run.go:707` 和 `:745` 调用 `runPreCompactHooks`） |
| HOOK-37 | **PreCompact** hook 阻断 → 跳过压缩 | AUTO | 前置:PreCompact hook `exit 2`; 操作:触发 auto 压缩; 预期:压缩被跳过，会话继续正常运行 | `src/utils/hooks.ts:1630` | ✅ 通过（`run.go:707` 检测 `blocked`，跳过压缩） |
| HOOK-38 | **PostCompact** 事件：压缩完成后触发，含 `compact_summary` | AUTO | 前置:配置 `PostCompact` hook; 操作:触发压缩完成; 预期:hook 收到 `{"hook_event_name":"PostCompact","trigger":"auto","compact_summary":"..."}` | `src/utils/hooks.ts:1631,coreSchemas.ts:579` | ✅ 通过（`run.go:729` 和 `:767` 调用 `runPostCompactHooks`） |
| HOOK-39 | **PermissionRequest** 事件：权限请求时触发，matchQuery=`tool_name` | AUTO | 前置:配置 `PermissionRequest` hook; 操作:触发一个需要 ask 权限的工具; 预期:hook 收到 `{"hook_event_name":"PermissionRequest","tool_name":"...","tool_input":{...}}` | `src/utils/hooks.ts:1620,coreSchemas.ts:425` | ✅ 通过（`executor.go:447` 调用 `runPermissionHooks`） |
| HOOK-40 | **PermissionRequest** `hookSpecificOutput.decision.behavior:allow/deny` → 自动裁决 | AUTO | 前置:PermissionRequest hook 返回 `{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}`; 操作:触发需权限工具; 预期:权限自动批准，无需用户交互 | `src/utils/hooks.ts:657-714` | ✅ 通过 |
| HOOK-41 | **PermissionDenied** 事件：权限被拒绝时触发，matchQuery=`tool_name` | AUTO | 前置:配置 `PermissionDenied` hook; 操作:在 default 模式触发 Bash 工具并手动拒绝; 预期:hook 收到 `{"hook_event_name":"PermissionDenied","tool_name":"Bash","reason":"..."}` | `src/utils/hooks.ts:1620,coreSchemas.ts:461` | ✅ 通过（`executor.go:442` 调用 `runPermissionHooks(HookPermissionDenied)`） |
| HOOK-42 | **Setup** 事件：`/init` 或维护操作时触发，matchQuery=`trigger`(init/maintenance) | N/A | — | `src/utils/hooks.ts:1627,coreSchemas.ts:504` | N/A（Setup 事件与 /init 命令对应，ccgo 命令框架 OUT-of-scope） |
| HOOK-43 | **TeammateIdle** 事件：Teammate 空闲时触发，无 matchQuery | N/A | — | `src/utils/hooks.ts:1649,coreSchemas.ts:591` | N/A（Team 编排 OUT-of-scope） |
| HOOK-44 | **TaskCreated** 事件：Task 创建时触发，无 matchQuery | N/A | — | `src/utils/hooks.ts:1650,coreSchemas.ts:601` | N/A（Team 任务 OUT-of-scope） |
| HOOK-45 | **TaskCompleted** 事件：Task 完成时触发，无 matchQuery | N/A | — | `src/utils/hooks.ts:1651,coreSchemas.ts:614` | N/A（Team 任务 OUT-of-scope） |
| HOOK-46 | **Elicitation** 事件：MCP server 请求用户输入时触发，matchQuery=`mcp_server_name` | N/A | — | `src/utils/hooks.ts:1653,coreSchemas.ts:627` | N/A（MCP Elicitation hook 事件 OUT-of-scope；ccgo mcp/elicitation.go 有 elicitation 处理但无 hook 接线） |
| HOOK-47 | **ElicitationResult** 事件：用户响应 elicitation 后触发，matchQuery=`mcp_server_name` | N/A | — | `src/utils/hooks.ts:1655,coreSchemas.ts:645` | N/A（同 HOOK-46） |
| HOOK-48 | **ConfigChange** 事件：settings 文件变更时触发，matchQuery=`source` | N/A | — | `src/utils/hooks.ts:1659,coreSchemas.ts:670` | N/A（文件监控 OUT-of-scope） |
| HOOK-49 | **InstructionsLoaded** 事件：CLAUDE.md 加载时触发，matchQuery=`load_reason` | N/A | — | `src/utils/hooks.ts:1662,coreSchemas.ts:695` | N/A（CLAUDE.md 文件监控 OUT-of-scope） |
| HOOK-50 | **CwdChanged** 事件：工作目录变更时触发，含 `old_cwd`/`new_cwd` | N/A | — | `src/utils/hooks.ts:1665,coreSchemas.ts:727` | N/A（cwd 变更监控 OUT-of-scope） |
| HOOK-51 | **FileChanged** 事件：被监控文件变更时触发，matchQuery=`basename(file_path)` | N/A | — | `src/utils/hooks.ts:1666,coreSchemas.ts:737` | N/A（文件监控 OUT-of-scope） |
| HOOK-52 | **WorktreeCreate** 事件：git worktree 创建时触发 | N/A | — | `src/utils/hooks.ts:1671,coreSchemas.ts:709` | N/A（Worktree 事件 OUT-of-scope） |
| HOOK-53 | **WorktreeRemove** 事件：git worktree 删除时触发 | N/A | — | `src/utils/hooks.ts:1671,coreSchemas.ts:718` | N/A（同 HOOK-52） |
| HOOK-54 | hook base input 字段：`session_id`,`transcript_path`,`cwd`,`permission_mode`,`agent_id`,`agent_type` | AUTO | 前置:配置任意 hook `cat`; 操作:触发; 预期:hook stdin 含上述全部基础字段（其中 `agent_id`/`agent_type` 在子 agent 场景才有值） | `src/entrypoints/sdk/coreSchemas.ts:387,utils/hooks.ts:301` | ✅ 通过（W-Batch-A）（`BuildInput` 已通过 `event.Payload` 循环透传所有 KV 对；设置 `agent_id`/`agent_type` 后会自动出现在 hook stdin；`TestBuildInputAgentIDAndType` + `TestBuildInputAgentFieldsAbsentWhenNotInPayload` 绿色通过） |
| HOOK-55 | hook 环境变量：`CLAUDE_PROJECT_DIR`,`CLAUDE_SESSION_ID`,`CLAUDE_TRANSCRIPT_PATH` 注入进程 | AUTO | 前置:配置 hook `printenv CLAUDE_PROJECT_DIR CLAUDE_SESSION_ID CLAUDE_TRANSCRIPT_PATH`; 操作:触发; 预期:输出三个非空值 | `src/utils/hooks.ts:860-900` | ✅ 通过（`command.go:402-405`） |
| HOOK-56 | `CLAUDE_ENV_FILE` 环境变量（SessionStart/Setup/CwdChanged/FileChanged hook 可写入 env） | N/A | — | `src/utils/hooks.ts:919-926` | N/A（依赖 CwdChanged/FileChanged/Setup OUT-of-scope 事件） |
| HOOK-57 | `disableAllHooks:true` → 所有 hook 全局禁用 | AUTO | 前置:settings.json `"disableAllHooks":true` + 配置多个 hook; 操作:运行 `claude --print`; 预期:任何 hook 均不执行 | `src/utils/hooks/hooksConfigSnapshot.ts:1` | ✅ 通过（`command.go:57-58`） |
| HOOK-58 | `allowManagedHooksOnly:true` → 仅允许 managed（策略）hook | AUTO | 前置:settings.json `"allowManagedHooksOnly":true`，同时有 user-settings hook 和 policy hook; 操作:触发; 预期:只有 policy hook 执行，user hook 被跳过 | `src/utils/hooks/hooksConfigSnapshot.ts:1` | ✅ 通过（`command.go:60-61`） |
| HOOK-59 | `allowedHttpHookUrls` 策略 → HTTP hook URL 白名单过滤 | AUTO | 前置:`allowedHttpHookUrls:["http://localhost:*"]`，配置一个 `http://evil.com` HTTP hook; 操作:触发; 预期:evil.com URL 被拒绝 | `src/utils/hooks.ts:1260` | ✅ 通过（`command.go:467-469`） |
| HOOK-60 | settings hooks 层级合并：user→project→local→policy，各层 hook 列表合并（不覆盖） | AUTO | 前置:user settings 配 PreToolUse hook A，project settings 配 hook B; 操作:触发; 预期:hook A 和 hook B 均执行 | `src/utils/settings/settings.ts:mergeHooks` | ✅ 通过（`config/settings.go:93-94` mergeAnyMap 合并 Hooks 字段） |
| HOOK-61 | hook `if` 条件字段：仅当条件匹配当前工具/路径时执行 hook | AUTO | 前置:配置 PreToolUse hook `{"if":"Bash:echo *","command":"...","type":"command"}`; 操作:分别触发 `echo hi` 和 `ls` 的 Bash; 预期:只有 `echo *` 模式的命令触发 hook | `src/utils/hooks.ts:688-710` | ✅ 通过（`command.go:335,688-711`） |
| HOOK-62 | workspace trust guard：未信任工作区的 hook 被跳过（交互模式） | MANUAL | 前置:未接受 trust dialog 的目录，配置 hook; 操作:触发任意工具; 预期:hook 不执行 | `src/utils/hooks.ts:286-296` | ❌ 缺失（ccgo 无 `shouldSkipHookDueToTrust()` 等效逻辑；trust dialog 存在但未 guard hook 执行路径） |
| HOOK-63 | hook 进度事件（`hook_started`/`hook_completed`/`hook_failed`/`hook_blocked`）向 UI 发送 | AUTO | 前置:配置命令 hook; 操作:触发并捕获事件流; 预期:事件序列包含对应 `EventToolProgress` 事件 | `src/utils/hooks/hookEvents.ts:1` | ✅ 通过（`conversation/hooks.go:144,148,152-155`,`tool/executor.go:413,416,426,436`） |
| HOOK-64 | HTTP hook `allowedEnvVars` + `httpHookAllowedEnvVars` 控制请求头中可用的环境变量 | AUTO | 前置:HTTP hook headers 含 `$TOKEN` 变量，settings 配 `allowedEnvVars:["TOKEN"]`; 操作:触发; 预期:header 中替换了 `TOKEN` 值；未在列表中的变量被替换为空字符串 | `src/utils/hooks/execHttpHook.ts:1` | ✅ 通过（`command.go:763-799`） |

小计：共 64 行。✅ 通过：44；⚠️ 已建未接：1；❌ 缺失：4；N/A：15。（HOOK-27/30/35/54 经 W-Batch-A 由 ⚠️ 翻为 ✅；HOOK-21/31/32 经 F6b 由 ❌ 翻为 ✅）
