## 6. 权限

涵盖规则匹配（allow/deny/ask 三种行为、tool-name + 参数/命令/host 定向、glob/前缀匹配）、4 种权限模式（default/acceptEdits/plan/bypassPermissions；附 dontAsk/auto）、交互 ask 对话流（allow-once / allow-always / deny）、allow-always 持久化（写哪个 settings 文件、scopeRequired 降级为 allow-once 的安全机制）、`/permissions` 命令界面、各工具专属对话框、additionalDirectories / --add-dir、dangerously-skip-permissions，共 35 行。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| PERM-RULE-01 | 规则字符串解析：`ToolName(content)` 格式，提取 toolName 与 ruleContent；括号转义 `\(` `\)` | AUTO | 前置：无需真实会话；操作：调用规则解析函数，输入 `"Bash(npm run:*)"` `"Edit(src/**)"` `"Bash\\(foo\\)"`；预期：toolName/ruleContent 分离正确，转义字符被还原 | `src/utils/permissions/permissionRuleParser.ts` | ✅ 通过（`internal/permissions/rules.go:PermissionRuleValueFromString`，已覆盖完整转义逻辑） |
| PERM-RULE-02 | 规则匹配：工具名大小写不敏感精确匹配与 glob 匹配 | AUTO | 前置：一条规则 `Bash`；操作：请求工具名 `bash`、`BASH`、`Bash`；预期：全部命中 | `src/utils/permissions/permissions.ts` | ✅ 通过（`rules.go:matchTool` 使用 `strings.EqualFold` + `filepath.Match`） |
| PERM-RULE-03 | 规则匹配：content 精确匹配、前缀规则 `cmd:*`、glob `*`、子字符串包含（无通配时） | AUTO | 前置：规则 `Bash(git:*)`；操作：命令 `git status`、`git log`、`npm install`；预期：前两个命中，第三个不命中 | `src/utils/permissions/permissionRuleParser.ts` | ✅ 通过（`rules.go:matchPattern` 实现前缀、glob、子字符串三层逻辑） |
| PERM-RULE-04 | 规则匹配：WebFetch `domain:` 前缀域名规则 | AUTO | 前置：allow 规则 `WebFetch(domain:github.com)`；操作：请求 URL `https://github.com/foo` 和 `https://evil.com`；预期：前者 Allow，后者 Ask | `src/utils/permissions/filesystem.ts` | ✅ 通过（`engine.go:Decide` → `rules.go:matchPattern` 走前缀路径，domain: 前缀传递到 MatchTarget） |
| PERM-RULE-05 | 规则优先级：deny > allow > ask（同工具多条规则时最高优先级规则胜出） | AUTO | 前置：同时有 `allow Bash` 和 `deny Bash(rm:*)`；操作：请求 `rm -rf /tmp/foo`；预期：deny 规则胜出，返回 Deny | `src/utils/permissions/permissions.ts:applyPermissionRulesToPermissionContext` | ✅ 通过（`engine.go:matchingRules` + `behaviorRank` 确保 deny 优先） |
| PERM-RULE-06 | 规则来源层级：userSettings / projectSettings / localSettings / policySettings / cliArg / session 全部加载并作为来源标注 | AUTO | 前置：user 设置和 project 设置各有 allow/deny 规则；操作：构建引擎，检查各规则的 Source 字段；预期：来源标注正确，不混淆 | `src/types/permissions.ts:PermissionRuleSource` | ✅ 通过（`contracts/permissions.go:PermissionRuleSource` 定义 8 个来源，`engine.go:NewEngineFromSettingsSources` 按来源分配） |
| PERM-RULE-07 | allowManagedPermissionRulesOnly：policySettings 设置此标志时，只采用 policySettings 来源的规则，忽略 user/project/local 规则 | AUTO | 前置：`policySettings.allowManagedPermissionRulesOnly=true`，user settings 有 allow rule；操作：决策某工具；预期：user rule 被忽略，仅 policy rule 生效 | `src/utils/permissions/permissionsLoader.ts:shouldAllowManagedPermissionRulesOnly` | ✅ 通过（`engine.go:NewEngineFromSettingsSources` managedRulesOnly 分支）|
| PERM-RULE-08 | 规则通配符 `*`（匹配所有工具）作为 catch-all allow/deny/ask 规则 | AUTO | 前置：allow 规则 `*`；操作：任意工具请求；预期：全部 Allow | `src/types/permissions.ts` | ✅ 通过（`rules.go:ParseRule` raw=`"*"` 时 toolName 与 pattern 均为 `"*"`，`matchTool` 直接返回 true） |
| PERM-RULE-09 | 规则验证：tool name 必须首字母大写；括号不匹配报错；WebFetch 规则必须用 `domain:` 前缀；WebSearch 规则禁止通配符；文件工具禁止 `:*` 语法 | AUTO | 前置：无；操作：调用 `ValidatePermissionRule` 传入各类非法规则；预期：返回 ValidationResult.Valid=false 并附 Suggestion | `src/utils/permissions/permissionRuleParser.ts` | ✅ 通过（`permissions/validation.go:ValidatePermissionRule` 完整实现所有校验分支） |
| PERM-RULE-10 | 规则 addRules / replaceRules / removeRules 运行时动态更新 | AUTO | 前置：空引擎；操作：调用 `ApplyUpdate(addRules)`，再 `ApplyUpdate(removeRules)`；预期：规则先增后删，引擎 Rules() 正确反映 | `src/types/permissions.ts:PermissionUpdate` | ✅ 通过（`engine.go:ApplyUpdate` 全部类型已实现） |
| PERM-MODE-01 | default 模式：只读工具自动 Allow，写工具 Ask | AUTO | 前置：`--permission-mode default`；操作：运行 Read 工具，再运行 Bash 写命令；预期：Read→Allow，Bash 写→Ask | `src/utils/permissions/PermissionMode.ts` | ✅ 通过（`engine.go:Decide` default 分支：`req.ReadOnly→Allow`，否则 Ask） |
| PERM-MODE-02 | acceptEdits 模式：只读工具 + 文件写入工具（WritesFiles=true）均自动 Allow，非文件变更动作 Ask | AUTO | 前置：`--permission-mode acceptEdits`；操作：运行 Edit 工具（WritesFiles=true）和 Bash 任意命令；预期：Edit→Allow，Bash→Ask | `src/utils/permissions/PermissionMode.ts:acceptEdits` | ✅ 通过（`engine.go:Decide` acceptEdits 分支：`req.ReadOnly || req.WritesFiles→Allow`，否则 Ask） |
| PERM-MODE-03 | plan 模式：只读工具 Allow，所有变更工具 Ask | AUTO | 前置：`--permission-mode plan`；操作：运行 Glob（只读）和 Write 工具；预期：Glob→Allow，Write→Ask | `src/utils/permissions/PermissionMode.ts:plan` | ✅ 通过（`engine.go:Decide` plan 分支） |
| PERM-MODE-04 | bypassPermissions 模式（isBypassAvailable=true）：所有工具 Allow，跳过询问 | AUTO | 前置：`--permission-mode bypassPermissions`，BypassAvailable=true；操作：运行任意写工具；预期：直接 Allow，不弹对话框 | `src/utils/permissions/PermissionMode.ts:bypassPermissions` | ✅ 通过（`engine.go:Decide` bypassPermissions 分支，BypassAvailable 检查） |
| PERM-MODE-05 | bypassPermissions 被 disableBypassPermissionsMode 关闭后降级为 Ask | AUTO | 前置：`disableBypassPermissionsMode=true`；操作：`--permission-mode bypassPermissions` 运行写工具；预期：返回 Ask 而非 Allow | `src/utils/permissions/bypassPermissionsKillswitch.ts` | ✅ 通过（`engine.go` BypassAvailable=false 时返回 Ask） |
| PERM-MODE-06 | dontAsk 模式：只读工具 Allow，所有写/破坏性工具 Deny（不弹对话框） | AUTO | 前置：`--permission-mode dontAsk`；操作：运行 Bash 写命令；预期：返回 Deny，不触发 ask | `src/types/permissions.ts:dontAsk` | ✅ 通过（`engine.go:Decide` PermissionDontAsk 分支） |
| PERM-MODE-07 | Shift+Tab 在 REPL 中循环切换模式（default→acceptEdits→plan→bypassPermissions→default），状态栏更新 | MANUAL | 前置：交互 REPL 中；操作：按 Shift+Tab 4 次；预期：状态栏依次显示 accept edits / plan mode / bypass permissions / (default 无显示)，循环回 default | `src/utils/permissions/getNextPermissionMode.ts:getNextPermissionMode` | ⚠️ 已建未接（`mode_switch.go:cycleMode` + `loop.go` Shift+Tab 已更新 `l.mode` 及状态栏；但 `run.go` `StartTurn` 闭包做 `r := base` 值复制，`l.mode` 的变化**不会**同步到下一轮的 `EnginePermissionDecider.Engine`，导致模式切换对实际决策无效） |
| PERM-MODE-08 | `--permission-mode` CLI flag 设置初始权限模式 | AUTO | 前置：`claude --print "echo hi" --permission-mode acceptEdits`；操作：执行 Bash 写命令；预期：被自动 Allow（无交互提示） | `src/cli/print.ts:permissionMode` | ✅ 通过（`cmd/claude/main.go:effectivePermissionMode` 解析 flag，传入 engine） |
| PERM-MODE-09 | `--dangerously-skip-permissions` 等价于 bypassPermissions 模式；与 `--permission-mode` 互斥 | AUTO | 前置：使用 `--dangerously-skip-permissions` 同时指定 `--permission-mode plan`；预期：报错提示互斥 | `src/main.tsx:dangerouslySkipPermissions` | ✅ 通过（`cmd/claude/main.go:effectivePermissionMode` 互斥检查） |
| PERM-ASK-01 | ask 决策触发交互对话框，显示工具名 + 描述 + path（若有） | MANUAL | 前置：default 模式，无 allow 规则；操作：触发 Bash 写命令（无已有 allow rule）；预期：REPL 弹出对话框，显示 "Allow once / Allow always for this command / Deny" 三个选项，工具名和命令可见 | `src/components/permissions/BashPermissionRequest/BashPermissionRequest.tsx` | ✅ 通过（`repl/permission_dialog.go:permissionActions` + `loop.go:showPermission`，tui 对话框显示 ToolName/Path/Description） |
| PERM-ASK-02 | 用户选择 "Allow once"：本次 Allow，不写 settings，不产生 Suggestions | MANUAL | 前置：同上，对话框弹出；操作：选择 "Allow once"；预期：工具执行成功，settings 文件无新增规则 | `src/components/permissions/FilePermissionDialog/usePermissionHandler.ts:handleAcceptOnce` | ✅ 通过（`permission_dialog.go:decisionForAction` 返回 `{Behavior:Allow}` 无 Suggestions） |
| PERM-ASK-03 | 用户选择 "Allow always"：产生带 Suggestions 的决策，settings 写入 allow 规则（localSettings） | MANUAL | 前置：同上；操作：选择 "Allow always for this command"；预期：工具执行成功，`~/.claude/settings.local.json`（或 localSettings 等效文件）的 `permissions.allow` 数组新增对应规则 | `src/utils/permissions/PermissionUpdate.ts:persistPermissionUpdate` | ✅ 通过（`permission_dialog.go:persistUpdate` destination=`localSettings`；`loop.go:persistDecision` 调用 `settings.Apply`；`settingswriter.Writer.Apply` 调用 `config.AddPermissionRule`） |
| PERM-ASK-04 | scopeRequired 工具（Bash / PowerShell / WebFetch / WebSearch）在无法推导具体 scope 时，选"Allow always"降级为 Allow-once（不写 settings，防止宽泛授权） | MANUAL | 前置：规则 Decision.Suggestions 为空，且 req.Path 为空（纯内存工具调用）；操作：对 Bash 工具选 "Allow always for this command"；预期：工具执行成功，但 settings **未**新增 `Bash` catch-all 规则 | `src/utils/permissions/permissionsLoader.ts:addPermissionRulesToSettings` | ✅ 通过（`permission_dialog.go:decisionForAction` scopeRequired(toolName) && persistScope(req)=="" 时返回无 Suggestions 的 Allow） |
| PERM-ASK-05 | 用户选择 "Deny"：工具执行被取消，返回 permission denied 错误给模型 | MANUAL | 前置：对话框弹出；操作：选 "Deny"；预期：工具返回错误内容，模型接收到带 isError=true 的 tool_result | `src/components/permissions/PermissionRequest.tsx` | ✅ 通过（`loop.go:handleScreenEvent` DialogResultDenied → Deny decision → `executor.go` 返回 PermissionError） |
| PERM-ASK-06 | 并发 ask 请求排队：第一个 ask 对话框未关闭时，第二个 ask 请求进入队列，前一个关闭后自动弹出下一个 | MANUAL | 前置：同一轮多工具并发触发 ask；操作：先后出现两个对话框；预期：一次只显示一个，关闭第一个后第二个自动出现 | `src/hooks/toolPermission/PermissionContext.ts` | ✅ 通过（`loop.go:enqueueAsk` 队列机制，`showNext` 自动推进） |
| PERM-ASK-07 | loop 退出时所有挂起 ask 请求被 Deny 答复，防止 goroutine 泄漏 | AUTO | 前置：有 ask 在队列中；操作：loop 退出；预期：所有 askCh/askQueue 中的请求收到 Deny 回复，goroutine 正常退出 | `src/hooks/toolPermission/PermissionContext.ts` | ✅ 通过（`loop.go:denyPendingAsks`） |
| PERM-PERSIST-01 | allow-always 规则写入 localSettings（`.claude/settings.local.json`），不写 projectSettings 或 userSettings | AUTO | 前置：选择 "Allow always"，各 settings 文件均存在；操作：观察写入目标；预期：仅 `settings.local.json` 新增规则 | `src/utils/permissions/PermissionUpdate.ts:persistPermissionUpdate` destination=`localSettings` | ✅ 通过（`permission_dialog.go:persistUpdate` hardcode `PermissionSourceLocalSettings`） |
| PERM-PERSIST-02 | allow-always 规则去重幂等：多次选择 "Allow always" 同一规则，settings 中不产生重复条目 | AUTO | 前置：settings 中已有规则 `Bash(git:*)`；操作：再次添加相同规则；预期：settings 中仍只有一条该规则 | `src/utils/permissions/permissionsLoader.ts:addPermissionRulesToSettings` | ✅ 通过（`config/permissions_write.go:AddPermissionRule` 使用 set 去重） |
| PERM-PERSIST-03 | allow-always 规则写入后立即对后续 ask 生效（session 内不需要重启） | AUTO | 前置：先触发 ask → 选 "Allow always" 写入规则；操作：再次触发同工具同命令；预期：直接 Allow，不再弹对话框 | `src/utils/permissions/PermissionUpdate.ts:applyPermissionUpdate` | ⚠️ 已建未接（`persistDecision` 成功写磁盘，但 `EnginePermissionDecider.Engine` 是值拷贝，运行时规则集不会从磁盘刷新；同会话内二次调用仍走 Ask 路径。需要 `loop.ApplyUpdate` 联动更新引擎） |
| PERM-PERSIST-04 | `/permissions` 命令列出当前 allow / deny / ask 规则 | AUTO | 前置：settings 有若干规则；操作：在 REPL 输入 `/permissions`（或 `/allowed-tools`）；预期：输出分 allow/deny/ask 三组列出规则条目 | `src/commands/permissions/index.ts` | ✅ 通过（`repl/commands_permissions.go:listPermissions` 读 localSettings → 格式化输出） |
| PERM-PERSIST-05 | `/permissions allow <rule>` 直接添加 allow 规则；`/permissions deny <rule>` 添加 deny；`/permissions remove allow <rule>` 删除规则 | AUTO | 前置：settings 无该规则；操作：执行上述命令；预期：对应 settings 中规则变更，再次 `/permissions` 可见 | `src/commands/permissions/permissions.tsx:PermissionRuleList` | ✅ 通过（`commands_permissions.go:permissionsHandlerWith` 实现 allow/deny/ask/remove 子命令） |
| PERM-PERSIST-06 | `/permissions` TUI 覆盖层：Tab 切换 Rules / Recent-Denials / Workspace；可在 UI 中交互添加/删除规则、retry 拒绝历史 | MANUAL | 前置：CC TUI；操作：`/permissions` 打开覆盖层，切换 Tab，添加规则；预期：每个 Tab 功能正常，添加的规则持久化 | `src/components/permissions/rules/PermissionRuleList.tsx`（WorkspaceTab/RecentDenialsTab/AddPermissionRules） | ❌ 缺失（ccgo `/permissions` 仅文本输出，无 TUI 覆盖层，无 Tab 切换、Recent-Denials、Workspace 目录管理） |
| PERM-DIR-01 | `--add-dir <path>` 将额外目录加入 permission scope（允许该目录下文件的读写） | AUTO | 前置：工作目录为 `/project`，文件在 `/other`；操作：`--add-dir /other` 后运行 Edit `/other/file.txt`；预期：Edit 被 Allow（而非 Ask） | `src/commands/add-dir/validation.ts` | ✅ 通过（`cmd/claude/main.go` `--add-dir` flag + `cliPermissionsSetting` → `AdditionalDirectories` → `engine.pathDecision` → `CheckEditableInternalPath`/`CheckPathSafetyForAutoEdit`） |
| PERM-DIR-02 | settings 中 `permissions.additionalDirectories` 数组加入额外目录，与 `--add-dir` 效果相同，支持多来源合并 | AUTO | 前置：projectSettings 有 `additionalDirectories: ["/shared"]`；操作：Edit `/shared/file.ts`；预期：Allow | `src/utils/permissions/permissionSetup.ts:setupPermissions additionalDirectories` | ✅ 通过（`engine.go:ContextFromSettings` 解析 AdditionalDirectories，`engine.go:NewEngineFromSettingsSources` 合并） |
| PERM-TOOL-01 | 各工具 ask 对话框的 "Always" 标签文案差异化：Bash/PowerShell→"Allow always for this command"；WebFetch→"Allow always for this host"；文件工具→"Allow always for this session"；其他→"Allow always for this tool" | MANUAL | 前置：default 模式；操作：触发各工具的 ask 对话框；预期：各对话框显示对应标签文案 | `src/components/permissions/BashPermissionRequest/bashToolUseOptions.tsx`；`src/components/permissions/WebFetchPermissionRequest/WebFetchPermissionRequest.tsx` | ✅ 通过（`permission_dialog.go:alwaysLabel` switch 实现对应文案） |
| PERM-TOOL-02 | 工具专属权限对话框展示工具特定内容：Bash 对话框显示完整命令文本、文件 Edit 对话框显示 diff；WebFetch 显示 URL | MANUAL | 前置：对应工具触发 ask；操作：观察对话框内容；预期：Bash 显示 command，Edit 显示 patch diff，WebFetch 显示 URL | `src/components/permissions/BashPermissionRequest/BashPermissionRequest.tsx`；`src/components/permissions/FileEditPermissionRequest/FileEditPermissionRequest.tsx` | ⚠️ 已建未接（`loop.go:showPermission` 传入 `PermissionRequest{ToolName, Path, Description}`；TUI 对话框目前仅显示工具名+描述文本，无 Bash 命令高亮 / Edit diff / URL 域名解析等工具特定内容，功能部分丢失） |

小计：35 行 — ✅ 通过 29 / ⚠️ 已建未接 3 / ❌ 缺失 1 / N/A 0 / MANUAL 10 / AUTO 25
