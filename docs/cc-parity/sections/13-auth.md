## 13. 认证 (Auth)

覆盖 CC 全部认证功能面：交互式 OAuth 登录（本地回调服务器、浏览器启动、PKCE、CSRF 状态、code→token 交换、token 刷新）；`/login` `/logout` slash 命令；`claude auth` CLI 子命令（login/logout/status）；凭证存储（macOS keychain via `/usr/bin/security`；非 mac 文件回退含权限）；`apiKeyHelper`（外部命令 + TTL 缓存）；API-key / `ANTHROPIC_API_KEY` 环境变量 / OAuth 优先级；subscription vs API 认证；`setup-token` 顶级命令。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| AUTH-OAUTH-01 | PKCE 参数生成：code_verifier (32B base64url)、code_challenge (S256) | AUTO | 前置：无；操作：调用 `auth.GenerateCodeVerifier()` + `GenerateCodeChallenge(verifier)`；预期：verifier 长度 43 字符，challenge 为 SHA-256/base64url，二者满足 RFC 7636 | `src/services/oauth/crypto.ts:11-24` | ✅ 通过 |
| AUTH-OAUTH-02 | 单次 CSRF state 生成（32B base64url，不可重用） | AUTO | 前置：无；操作：调用 `auth.GenerateState()`；预期：返回长度 43 的 base64url 字符串，每次调用结果随机不同 | `src/services/oauth/crypto.ts:22-24` | ✅ 通过 |
| AUTH-OAUTH-03 | BuildAuthURL：包含 code_challenge、code_challenge_method=S256、state、scope、redirect_uri、client_id | AUTO | 前置：生成 PKCE 参数和 state；操作：`auth.BuildAuthURL(params)`；预期：解析 URL，各参数均存在且值正确，code=true 存在，loginWithClaudeAi 时使用 `claude.com/cai/oauth/authorize` | `src/services/oauth/client.ts:46-105` | ✅ 通过 |
| AUTH-OAUTH-04 | BuildAuthURL：inferenceOnly=true 时 scope 仅含 `user:inference` | AUTO | 前置：inferenceOnly=true；操作：`auth.BuildAuthURL` 含 InferenceOnly；预期：scope 参数等于 `user:inference` 单项 | `src/services/oauth/client.ts:81-84` | ✅ 通过 |
| AUTH-OAUTH-05 | BuildAuthURL：可附加 orgUUID、login_hint、login_method 参数 | AUTO | 前置：提供 OrgUUID/LoginHint/LoginMethod；操作：`auth.BuildAuthURL`；预期：URL 含对应查询参数 | `src/services/oauth/client.ts:88-104` | ✅ 通过 |
| AUTH-OAUTH-06 | 本地回调服务器（AuthCodeListener/CallbackListener）：OS 分配空闲端口、监听 `/callback` 路径 | AUTO | 前置：无；操作：`auth.StartCallbackListener(state)` 后读 `listener.Port()`；预期：端口 > 0，`localhost:<port>/callback` 可访问 | `src/services/oauth/auth-code-listener.ts:37-52` | ✅ 通过 |
| AUTH-OAUTH-07 | 回调服务器 CSRF state 校验：state 不匹配时返回 400 并拒绝 | AUTO | 前置：CallbackListener 已启动；操作：向 `/callback?code=X&state=BAD` 发 GET；预期：HTTP 400，`Wait()` 返回 state mismatch 错误 | `src/services/oauth/auth-code-listener.ts:163-169` | ✅ 通过 |
| AUTH-OAUTH-08 | 回调服务器：state 匹配且含 code 时提取 code 并关闭等待 | AUTO | 前置：CallbackListener 已启动；操作：向 `/callback?code=AUTHCODE&state=STATE` 发 GET；预期：`Wait()` 返回 code=AUTHCODE，HTTP 200 | `src/services/oauth/auth-code-listener.ts:134-175` | ✅ 通过 |
| AUTH-OAUTH-09 | 回调服务器：IdP 返回 `?error=access_denied` 时向客户端报错而不暴露原始错误字符串 | AUTO | 前置：CallbackListener 已启动；操作：`GET /callback?error=access_denied&state=STATE`；预期：`Wait()` 返回错误（含 sanitized error code），HTTP 400，错误消息不含攻击者可控制的任意文本 | `src/services/oauth/auth-code-listener.ts:71-79` | ✅ 通过 |
| AUTH-OAUTH-10 | 全流程 RunLoginFlow：PKCE 生成 → 启动监听 → 构建 URL → 打开浏览器 → 等待回调 → 交换 code → 持久化凭证 | MANUAL | 前置：未登录，有浏览器；操作：`claude auth login`（交互式，确认 consent）；预期：浏览器打开 authorize URL，完成授权后终端打印 "Login successful."，凭证写入 keychain/文件 | `src/services/oauth/index.ts:32-132` `src/cli/handlers/auth.ts:112-229` | ⚠️ 已建未接（RunLoginFlow、CallbackListener、BuildAuthURL、ExchangeAuthorizationCode 原语均已实现并可测；`claude auth login` → `runAuthLogin` 已接线；但接线采用 `auth.NewOSBrowserOpener()`，自动浏览器打开通路理论可用；REPL 内的 `/login` slash 命令返回 `LocalCommandResultLogin`，但 newProductionRouter 未注册 `login` 处理器，REPL 内点击 `/login` 无效果） |
| AUTH-OAUTH-11 | 手动 fallback：显示 URL 供用户复制粘贴（非浏览器环境） | MANUAL | 前置：BROWSER 环境变量设为无效命令或浏览器打开失败；操作：`claude auth login`；预期：终端显示 "If your browser did not open, visit: <url>" | `src/services/oauth/index.ts:80-86` | ✅ 通过（`OnURL` 回调已实现，ccgo `runAuthLogin` 中写入 stdout） |
| AUTH-OAUTH-12 | code → token 交换：POST JSON 到 TokenURL，含 grant_type/code/redirect_uri/client_id/code_verifier/state | AUTO | 前置：mock HTTP server 返回 200 + token JSON；操作：`auth.ExchangeAuthorizationCode(ctx, client, config, params)`；预期：返回含 access_token/refresh_token/scopes 的 Credentials | `src/services/oauth/client.ts:107-144` | ✅ 通过 |
| AUTH-OAUTH-13 | code → token 交换：服务端返回 401 时返回明确错误，不回显 token 响应体 | AUTO | 前置：mock HTTP server 返回 401；操作：`ExchangeAuthorizationCode`；预期：err 含 "status 401"，不含响应体内容 | `src/services/oauth/client.ts:133-141` | ✅ 通过 |
| AUTH-OAUTH-14 | token 刷新：用 refresh_token 换新 access_token，刷新成功后更新存储 | AUTO | 前置：Credentials 含过期 access_token 和有效 refresh_token；操作：`OAuthTokenProvider.CurrentAccessToken(ctx)`；预期：POST refresh_token 请求，返回新 access_token，CredentialStore.Save 被调用 | `src/services/oauth/client.ts:146-274` | ✅ 通过 |
| AUTH-OAUTH-15 | token 提前刷新：到期前 1 分钟（refreshMargin）主动刷新 | AUTO | 前置：ExpiresAt = now + 30s（< margin 1min）；操作：`OAuthTokenProvider.CurrentAccessToken(ctx)`；预期：触发刷新而非返回即将过期的 token | `src/services/oauth/client.ts:344-353`（isOAuthTokenExpired 5 min buffer） | ✅ 通过 |
| AUTH-OAUTH-16 | `CLAUDE_CODE_OAUTH_REFRESH_TOKEN` + `CLAUDE_CODE_OAUTH_SCOPES` 环境变量直接登录（跳过浏览器流程） | AUTO | 前置：设置两个环境变量；操作：`auth.FromEnv()`；预期：返回 Source=OAuth、RefreshToken 已填充，不启动浏览器 | `src/cli/handlers/auth.ts:140-186` | ✅ 通过（`auth.FromEnv()` 已实现此分支） |
| AUTH-STORE-01 | macOS keychain 存储：`/usr/bin/security add-generic-password` 通过 stdin 写入（安全，进程列表不可见 payload） | AUTO | 前置：macOS 平台；操作：`keychainCredentialStore.Save(ctx, creds)`；预期：凭证写入 service=`Claude Code-credentials`，account=$USER，stdin 方式写入（hex 编码） | `src/utils/secureStorage/macOsKeychainStorage.ts:97-158` | ✅ 通过 |
| AUTH-STORE-02 | macOS keychain 读取：`security find-generic-password -w` 返回 JSON，反序列化为 Credentials | AUTO | 前置：keychain 已有凭证；操作：`keychainCredentialStore.Load(ctx)`；预期：返回正确 Credentials，Source 字段填充 | `src/utils/secureStorage/macOsKeychainStorage.ts:26-66` | ✅ 通过 |
| AUTH-STORE-03 | macOS keychain 删除：logout 时调用 `security delete-generic-password`，不存在时静默成功 | AUTO | 前置：keychain 已有凭证；操作：`keychainCredentialStore.Delete(ctx)`；预期：凭证被删除，后续 Load 返回 SourceNone | `src/utils/secureStorage/macOsKeychainStorage.ts:159-175` | ✅ 通过 |
| AUTH-STORE-04 | 非 macOS 文件回退：凭证写入 `credentials.json`，权限 0600，目录权限 0700 | AUTO | 前置：非 macOS 或 keychain 不可用；操作：`FileCredentialStore.Save(ctx, creds)`；预期：文件创建，stat 权限为 0600 | `src/utils/secureStorage/plainTextStorage.ts:19-61` | ✅ 通过（`store.go` 中 `tmp.Chmod(0o600)` + `MkdirAll(dir, 0o700)`） |
| AUTH-STORE-05 | macOS keychain 不可用时（非 darwin 平台）自动降级为文件存储 | AUTO | 前置：非 macOS 环境（或 GOOS!=darwin）；操作：`auth.NewKeychainCredentialStore("")`；预期：返回 FileCredentialStore，Save/Load 操作对 credentials.json 而非 keychain | `src/utils/secureStorage/index.ts:9-17` | ✅ 通过 |
| AUTH-STORE-06 | 文件存储迁移 fallback：keychain 为空但 `credentials.json` 存在时仍能读取（向后兼容） | AUTO | 前置：keychain 为空，`credentials.json` 含有效凭证；操作：`credentialsFromEnvOrStore(ctx, "")`；预期：返回文件中的凭证，Source 非 none | `src/cli/handlers/auth.ts`（getClaudeAIOAuthTokens 降级逻辑） | ✅ 通过（`cmd/claude/main.go:3735-3743` 明确实现文件 fallback） |
| AUTH-PREC-01 | API key 优先级：apiKeyHelper > `ANTHROPIC_API_KEY` 环境变量 > OAuth keychain > 文件凭证 | AUTO | 前置：apiKeyHelper 已配置且返回 key，同时 `ANTHROPIC_API_KEY` 也设置；操作：`credentialsFromEnvOrStore(ctx, helperCmd)`；预期：使用 apiKeyHelper 返回的 key | `src/utils/auth.ts:320-337` | ✅ 通过 |
| AUTH-PREC-02 | `ANTHROPIC_API_KEY` 环境变量优先于 keychain OAuth | AUTO | 前置：设置 `ANTHROPIC_API_KEY`，keychain 中有 OAuth 凭证；操作：`auth.FromEnv()` → `credentialsFromEnvOrStore`；预期：返回 Source=APIKey，使用环境变量中的 key | `src/utils/auth.ts:234-248` | ✅ 通过 |
| AUTH-PREC-03 | OAuth 凭证（keychain）优先于"无凭证"情况，但低于 API key 环境变量 | AUTO | 前置：无环境变量，keychain 含 OAuth token；操作：`credentialsFromEnvOrStore(ctx, "")`；预期：返回 Source=OAuth | `src/utils/auth.ts:200-206` | ✅ 通过 |
| AUTH-PREC-04 | 全无凭证时返回 SourceNone，headless 模式输出明确错误而非崩溃 | AUTO | 前置：无 `ANTHROPIC_API_KEY`，无 keychain 凭证，无 apiKeyHelper；操作：`claude --print "hi"`；预期：stderr 含 "missing Anthropic credentials"，exit 1 | `src/utils/auth.ts:344-348` | ✅ 通过 |
| AUTH-APIHELPER-01 | apiKeyHelper：执行配置的 shell 命令，trim stdout 作为 API key | AUTO | 前置：settings 中 `apiKeyHelper = "echo sk-test-key"`；操作：`APIKeyHelperResolver.Resolve(ctx)`；预期：返回 `sk-test-key` | `src/utils/auth.ts:469-499` | ✅ 通过 |
| AUTH-APIHELPER-02 | apiKeyHelper TTL 缓存：5 分钟内复用上次结果，不重复执行命令 | AUTO | 前置：helper 已执行并缓存；操作：TTL 内再次 `Resolve(ctx)`；预期：命令不再执行，返回缓存值 | `src/utils/auth.ts:474-491` | ✅ 通过 |
| AUTH-APIHELPER-03 | `CLAUDE_CODE_API_KEY_HELPER_TTL_MS` 环境变量覆盖默认 5 分钟 TTL | AUTO | 前置：`CLAUDE_CODE_API_KEY_HELPER_TTL_MS=60000`；操作：多次调用 Resolve，时间间隔 61s；预期：第二次调用重新执行命令 | `src/utils/auth.ts:435-450` | ✅ 通过 |
| AUTH-APIHELPER-04 | apiKeyHelper 失败时不崩溃，fallback 到其他凭证源 | AUTO | 前置：helperCmd="exit 1"，同时设置 `ANTHROPIC_API_KEY`；操作：`credentialsFromEnvOrStore(ctx, helperCmd)`；预期：helper 报错但 fallthrough 继续，返回 env var key | `src/utils/auth.ts:501-525` | ✅ 通过（`main.go:3716-3718` 注释明确） |
| AUTH-CLI-01 | `claude auth login`：启动 OAuth 流程，浏览器打开授权 URL，完成后打印 "Login successful." | MANUAL | 前置：未登录；操作：`claude auth login`，输入 `y` 确认 consent，在浏览器完成授权；预期：终端打印 "Login successful."，exit 0 | `src/cli/handlers/auth.ts:112-229` | ✅ 通过（runAuthLogin 完整接线；ccgo 额外有 consent gate 提示） |
| AUTH-CLI-02 | `claude auth login --yes`（或 `-y`）：跳过 consent 提示直接进入 OAuth 流程 | AUTO | 前置：未登录（可用 mock store）；操作：`claude auth login --yes`；预期：跳过 "Continue? [y/N]" 步骤，直接进入 OAuth 流程 | `src/cli/handlers/auth.ts:112-229`（CC 无 consent gate，ccgo 额外加） | ✅ 通过 |
| AUTH-CLI-03 | `claude auth login --console`：选择 Console（API 计费）而非 claude.ai 订阅 | MANUAL | 前置：未登录；操作：`claude auth login --console`；预期：授权 URL 指向 `platform.claude.com/oauth/authorize` 而非 `claude.com/cai/oauth/authorize` | `src/cli/handlers/auth.ts:131-136` | ❌ 缺失（`runAuthLogin` 固定 `LoginWithClaudeAI: true`，未解析 `--console`/`--claudeai` 标志） |
| AUTH-CLI-04 | `claude auth login --sso`：强制 SSO 登录方式（login_method=sso） | MANUAL | 前置：未登录；操作：`claude auth login --sso`；预期：授权 URL 含 `login_method=sso` | `src/cli/handlers/auth.ts:188` | ❌ 缺失（`runAuthLogin` 未解析 `--sso`/`--email` 标志） |
| AUTH-CLI-05 | `claude auth login --email <email>`：在登录页面预填邮箱（login_hint） | MANUAL | 前置：未登录；操作：`claude auth login --email user@example.com`；预期：授权 URL 含 `login_hint=user%40example.com` | `src/cli/handlers/auth.ts:132-136` | ❌ 缺失（`runAuthLogin` 未解析 `--email` 标志） |
| AUTH-CLI-06 | `claude auth logout`：删除存储的凭证，打印 "Signed out." | AUTO | 前置：已登录（keychain 或文件中有凭证）；操作：`claude auth logout`；预期：凭证被删除，打印 "Signed out. Stored credentials removed."，exit 0 | `src/cli/handlers/auth.ts:321-329` | ✅ 通过 |
| AUTH-CLI-07 | `claude auth status`：报告当前认证状态（source 类型，不打印凭证值） | AUTO | 前置：已登录（OAuth）；操作：`claude auth status`；预期：stdout 含 "Authenticated via OAuth (keychain)."，exit 0；未登录时 exit 1 | `src/cli/handlers/auth.ts:232-319` | ⚠️ 已建未接（功能已实现；但 CC 的 `auth status` 支持 `--json`/`--text`，含 email/orgId/subscriptionType 等详细字段；ccgo 仅输出单行文本，无 `--json` 选项，无账号详细信息） |
| AUTH-LOGIN-01 | REPL 内 `/login` slash 命令：触发 OAuth 登录流程，成功后刷新 API key 上下文 | MANUAL | 前置：REPL 已启动，未登录；操作：在 REPL 输入 `/login`；预期：触发登录流程，完成后会话继续使用新凭证 | `src/commands/login/login.tsx:19-59` | ❌ 缺失（`commands.ExecuteBuiltinLocalCommand` 中 `/login` 返回 `LocalCommandResultLogin`，但 `newProductionRouter` 未注册 `login` 处理器，REPL loop 未处理该结果类型，实际无效果） |
| AUTH-LOGIN-02 | REPL 内 `/logout` slash 命令：清除存储凭证，允许切换账号 | MANUAL | 前置：REPL 已启动，已登录；操作：在 REPL 输入 `/logout`；预期：凭证被清除，终端提示需要重新登录 | `src/commands/logout/logout.tsx:16-48` | ❌ 缺失（与 `/login` 同理：有 LocalCommandResultLogout 类型定义，但 REPL 路由器未接线，无实际效果） |
| AUTH-TOKEN-01 | OAuth token 过期判断：`IsOAuthTokenExpired(expiresAt, now)`，buffer 为 CC 5 分钟（ccgo 为 refreshMargin 1 分钟） | AUTO | 前置：给定 expiresAt 和 now；操作：`auth.IsOAuthTokenExpired(expiresAt, now)` 及 OAuthTokenProvider 内部判断；预期：在 expiresAt 之前（超出 margin）返回 false，之后返回 true | `src/services/oauth/client.ts:344-353` | ✅ 通过（实现存在，margin 略有差异：CC 5 分钟 buffer，ccgo 默认 1 分钟，属有意差异） |
| AUTH-TOKEN-02 | OAuth scopes 解析：`ParseScopes("user:profile user:inference")` 返回 `["user:profile", "user:inference"]` | AUTO | 前置：无；操作：`auth.ParseScopes(scopeString)`；预期：按空白字符分割，忽略空项 | `src/services/oauth/client.ts:42-44` | ✅ 通过 |
| AUTH-TOKEN-03 | `ShouldUseClaudeAIAuth(scopes)`：scopes 含 `user:inference` 时返回 true（走 claude.ai 认证头） | AUTO | 前置：无；操作：`auth.ShouldUseClaudeAIAuth([]string{"user:inference"})`；预期：true；不含时返回 false | `src/services/oauth/client.ts:38-40` | ✅ 通过 |
| AUTH-SETUP-01 | `claude setup-token`：引导订阅用户生成长期 inference-only token（交互式 TUI 流程） | MANUAL | 前置：已有 Claude 订阅；操作：`claude setup-token`；预期：触发 inferenceOnly=true 的 OAuth 流程，token 只含 `user:inference` scope | `src/main.tsx:4267-4274` `src/cli/handlers/util.tsx:20` | ❌ 缺失（ccgo 未实现 `setup-token` 顶级命令；`cmd/claude/main.go` 未注册该子命令） |
| AUTH-ENV-01 | `ANTHROPIC_API_KEY` 环境变量认证：设置后直接用于 API 请求，无需 keychain | AUTO | 前置：设置 `ANTHROPIC_API_KEY=sk-ant-xxx`；操作：`claude --print "hello"`；预期：请求成功，使用环境变量 key，不读取 keychain | `src/utils/auth.ts:234-248` | ✅ 通过 |
| AUTH-ENV-02 | `CLAUDE_CODE_OAUTH_REFRESH_TOKEN` + `CLAUDE_CODE_OAUTH_SCOPES` 组合：无浏览器环境下自动刷新登录 | AUTO | 前置：设置两个环境变量；操作：`claude --print "hello"`（headless）；预期：自动刷新得到 access_token，请求成功 | `src/cli/handlers/auth.ts:140-186` | ✅ 通过（`auth.FromEnv()` 解析这两个变量，OAuthTokenProvider 自动刷新） |

小计：35 项 — ✅ 22 / ⚠️ 2 / ❌ 8 / N/A 0
