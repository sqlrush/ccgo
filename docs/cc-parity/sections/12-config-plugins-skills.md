## 12. 配置 / 插件 / skills / output-styles

覆盖：设置层级与深度合并、全部 settings key、ConfigTool（`/config`）、插件系统（安装/市场/plugin 提供的 commands/agents/hooks/skills/output-styles）、skills 子系统（内置/发现/激活/前置字段/条件路径）、output styles（内置/自定义/切换）。参考源 `utils/settings/`、`skills/`、`utils/plugins/`、`constants/outputStyles.ts`。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照（src/…:line） | 状态 |
|---|---|---|---|---|---|
| CFG-01 | 设置层级：managed/enterprise（最高优先级）> user `~/.claude/settings.json` > project `.claude/settings.json` > local `.claude/settings.local.json`（最低） | AUTO | 前置：在四层各写不同 `model` 值；操作：`claude --print "什么model"` 读合并配置；预期：managed 的值覆盖所有下层值 | `utils/settings/constants.ts:7-22`、`utils/settings/settings.ts:680-730` | ✅ 通过（`internal/config/settings.go:MergeSettingsSources`，`internal/config/managed.go:LoadPolicySettings`） |
| CFG-02 | managed settings 子来源：远程 HTTP（`CLAUDE_CODE_REMOTE_MANAGED_SETTINGS_URL`）> macOS plist（`/Library/Managed Preferences/com.anthropic.claudecode`）> Windows 注册表（`HKLM\SOFTWARE\Policies\ClaudeCode`）> 文件（`managed-settings.json` + `managed-settings.d/*.json` drop-ins，按字母序，后文件覆盖前文件） | AUTO | 前置：设 `CLAUDE_CODE_REMOTE_MANAGED_SETTINGS_URL`；操作：启动并读 merged settings；预期：远程来源 model 值生效 | `utils/settings/settings.ts:680-730`、`utils/settings/managedPath.ts` | ✅ 通过（`internal/config/managed.go`：HTTP/plist/registry/file drop-in 均实现） |
| CFG-03 | `--settings <path>` flag settings（优先级高于所有文件来源，低于 managed） | AUTO | 前置：写 `/tmp/custom-settings.json`，含不同 `model`；操作：`claude --settings /tmp/custom-settings.json --print "x"`；预期：该文件的 model 覆盖 project/user settings | `utils/settings/constants.ts:15` | ✅ 通过（`cmd/claude/main.go:227 --settings flag`，`applySettingsFlag()`；F11 中发现已实现） |
| CFG-04 | 深度合并策略：array 字段拼接并去重（如 `permissions.allow`），object 字段递归合并，scalar 后来源覆盖 | AUTO | 前置：user settings `permissions.allow:["Bash(ls)"]`，project settings `permissions.allow:["Bash(pwd)"]`；操作：读合并 permissions；预期：合并后 allow 含两条且无重复 | `utils/settings/settings.ts:538-547`（`settingsMergeCustomizer` + `uniq`） | ✅ 通过（`internal/config/settings.go:MergeSettings`，lodash.mergeWith 对应 Go 实现） |
| CFG-05 | settings 写入目标仅限 user/project/local 三层；managed/flag 只读 | AUTO | 前置：尝试通过 `config.SetSettingsValue` 写 managed 路径；预期：返回错误 | `utils/settings/constants.ts:182-195` | ✅ 通过（ccgo 写路径 `SetSettingsValue` 仅接受 user/project/local 路径；managed 路径无写接口） |
| CFG-06 | settings key: `model`（覆盖默认模型） | AUTO | 前置：`~/.claude/settings.json` 设 `"model":"claude-opus-4-5"`；操作：`claude --print "x"`；预期：API 请求使用 `claude-opus-4-5` | `utils/settings/types.ts:255+` | ✅ 通过（`contracts.Settings.Model` 字段，已接线至 `anthropicClientFromEnv` 调用链） |
| CFG-07 | settings key: `availableModels`（企业模型白名单） | AUTO | 前置：managed settings 设 `availableModels:["claude-haiku-4-5"]`；操作：尝试用非白名单模型；预期：被拒 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`cmd/claude/main.go:checkAvailableModels`，`cmd/claude/w_c24_test.go`） |
| CFG-08 | settings key: `modelOverrides`（Bedrock ARN 等模型 ID 重映射） | AUTO | 前置：设 `modelOverrides:{"claude-opus-4-5":"arn:aws:bedrock:..."}` ；操作：请求时使用原始 model id；预期：实际发送 ARN | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`cmd/claude/main.go:applyModelOverrides`，`cmd/claude/w_c24_test.go`） |
| CFG-09 | settings key: `permissions`（allow/deny/ask/defaultMode/disableBypassPermissionsMode/additionalDirectories） | AUTO | 前置：project settings `permissions.allow:["Bash(echo *)"]`；操作：`claude --print` 触发 Bash echo；预期：不弹权限对话框直接执行 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/permissions/`，`internal/config/permissions_write.go`） |
| CFG-10 | settings key: `hooks`（lifecycle hooks，27 种事件） | AUTO | 前置：settings 配置 `hooks.PreToolUse:[{type:"command",command:"echo pre"}]`；操作：触发任意工具调用；预期：hook 命令被执行 | `schemas/hooks.ts`，`entrypoints/sdk/coreTypes.ts:25-53` | ✅ 通过（`internal/conversation/hooks.go:configuredHooks`，`hookpkg.FromSettings` + `hookpkg.Resolve`） |
| CFG-11 | settings key: `env`（为所有 session 注入环境变量） | AUTO | 前置：settings `env:{"MY_VAR":"hello"}`；操作：`claude --print "run: echo $MY_VAR"`；预期：Bash 返回 "hello" | `utils/settings/types.ts:255+` | ✅ 通过（`contracts.Settings.Env`，会话启动时合并到子进程环境） |
| CFG-12 | settings key: `apiKeyHelper`（外部脚本提供认证凭据） | AUTO | 前置：设 `apiKeyHelper:"/usr/local/bin/get-key"`，脚本输出 JSON；操作：`claude --print "x"`；预期：用脚本返回的 key 认证 | `utils/settings/types.ts:255+` | ✅ 通过（`cmd/claude/main.go:3658 anthropicClientFromEnv` 消费 `mergedSettings.APIKeyHelper`） |
| CFG-13 | settings key: `cleanupPeriodDays`（对话转录保留天数，0 禁用） | AUTO | 前置：设 `cleanupPeriodDays:1`；操作：等待超出时间后检查历史文件；预期：超期文件被删 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`cmd/claude/main.go:384 CleanupOldTranscripts`，`internal/session/cleanup_test.go`） |
| CFG-14 | settings key: `includeCoAuthoredBy`（deprecated，控制 Co-authored-by git attribution） | AUTO | 前置：设 `includeCoAuthoredBy:false`；操作：让 claude 做一个 git commit；预期：commit message 无 Co-authored-by 行 | `utils/settings/types.ts:255+` | ✅ 通过（F11；`internal/tools/bash/prompt.go:commitAttributionText()`，读取 settings.IncludeCoAuthoredBy 并将 Co-Authored-By trailer 注入/省略至 BashTool git 指令中；`internal/tools/bash/attribution_test.go`） |
| CFG-15 | settings key: `attribution`（git commit/PR attribution 文本） | AUTO | 前置：设 `attribution:{commit:"Custom Bot",pr:"Custom Bot"}`；操作：git commit；预期：attribution 使用自定义文本 | `utils/settings/types.ts:255+` | ✅ 通过（F11；`internal/tools/bash/prompt.go:commitAttributionText()`，读取 settings.Attribution.Commit 并使用自定义 attribution 文本；`internal/tools/bash/attribution_test.go`） |
| CFG-16 | settings key: `includeGitInstructions`（是否注入内置 git commit/PR 指引） | AUTO | 前置：设 `includeGitInstructions:false`；操作：查看发送到 API 的 system prompt；预期：不含默认 git 指引 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`Runner.IncludeGitInstructions`+`ShouldIncludeGitInstructions()`，`conversation/settings_wire_test.go`） |
| CFG-17 | settings key: `outputStyle`（output style 名称） | AUTO | 前置：设 `outputStyle:"Explanatory"`；操作：`claude --print "hello"`；预期：system prompt 含 Explanatory 提示词 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/outputstyles/outputstyles.go:Resolve` 消费 `settings.OutputStyle`，通过 `effectiveOutputStyleName()` 接线至 system prompt） |
| CFG-18 | settings key: `language`（首选语言，注入 system prompt） | AUTO | 前置：设 `language:"zh"`；操作：`claude --print "respond"`；预期：回复为中文 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`Runner.Language`+`systemPromptWithOutputStyle`，`conversation/settings_wire_test.go`） |
| CFG-19 | settings key: `statusLine`（`{type:"command",command:string}` 自定义状态行） | MANUAL | 前置：settings 设 `statusLine:{type:"command",command:"echo my-status"}`；操作：在 REPL 中操作；预期：状态行显示命令输出 | `utils/settings/types.ts:255+` | ✅ 通过（G17 commit 2b92b12）（`internal/repl/statusline_runner.go:RunStatusLineCommand`；`Loop.SetStatusLineCommand`→`refreshBaseStatus` 执行命令并用输出替换状态行；`RunInteractiveWithOptions` 通过 `InteractiveOptions.StatusLineCommand` 接线；`statusline_loop_test.go` 测试；`tui.RenderStatusLineWithCommand` 渲染层支持） |
| CFG-20 | settings key: `sandbox`（沙箱配置：enabled/rules/型号）| AUTO | 前置：settings `sandbox:{enable:true}`；操作：在 macOS 触发 Bash；预期：seatbelt 生效 | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有；bash tool 仅处理 CLI 开关，sandbox settings 字段未映射至执行层） |
| CFG-21 | settings key: `mcpServers`（settings.json 内内联 MCP 服务器配置） | AUTO | 前置：settings `mcpServers:{"my-mcp":{type:"stdio",command:"python3 server.py"}}`；操作：`claude --print` 调用 my-mcp 工具；预期：MCP 工具可用 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/mcp/load.go:BuildConfiguredToolSets` 读取 `mergedSettings.MCPServers`） |
| CFG-22 | settings key: `enabledMcpjsonServers` / `disabledMcpjsonServers`（`.mcp.json` 中服务器的批准/拒绝名单） | AUTO | 前置：`.mcp.json` 含 server A、B；settings `enabledMcpjsonServers:["A"]`；操作：列出可用 MCP 工具；预期：只有 A 的工具可见 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/mcp/load.go` 实现过滤逻辑） |
| CFG-23 | settings key: `allowedMcpServers` / `deniedMcpServers`（企业 MCP 白名单/黑名单） | AUTO | 前置：managed settings `deniedMcpServers:[{name:"risky-mcp"}]`；操作：尝试调用该 MCP；预期：被拒 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/mcp/load.go:TestBuildConfiguredToolSetsHonorsStrictPluginOnlyMCP` 验证） |
| CFG-24 | settings key: `strictPluginOnlyCustomization`（锁定 skills/agents/hooks/mcp 只允许插件来源） | AUTO | 前置：managed settings `strictPluginOnlyCustomization:["skills"]`；操作：在 project `.claude/skills/` 放一个 skill；预期：该 skill 不出现在 `/` 菜单 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/commands/registry.go:49 skillsLocked`，`internal/config/plugin_only.go:IsRestrictedToPluginOnly`） |
| CFG-25 | settings key: `disableAllHooks`（全局关闭 hooks 和 statusLine） | AUTO | 前置：settings 配置若干 hooks，再设 `disableAllHooks:true`；操作：触发工具调用；预期：hooks 不执行 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/conversation/hooks.go:17-24`：`DisableAllHooks` 检查） |
| CFG-26 | settings key: `allowManagedHooksOnly`（只允许 managed settings 里的 hooks 运行） | AUTO | 前置：managed settings 有 hookA，user settings 有 hookB，managed 设 `allowManagedHooksOnly:true`；操作：触发 hook phase；预期：只有 hookA 运行 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`conversation/hooks.go:25-32`，`conversation/w_c24_hooks_permissions_test.go`） |
| CFG-27 | settings key: `allowedHttpHookUrls` / `httpHookAllowedEnvVars`（HTTP hook URL 白名单和允许插值的 env vars） | AUTO | 前置：HTTP hook 指向非白名单 URL；预期：执行时被拒 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`internal/hooks/command.go:185-186 AllowedURLPatterns`，`internal/hooks/command_test.go:TestHTTPHookRespectsAllowedURLPatterns`） |
| CFG-28 | settings key: `allowManagedPermissionRulesOnly`（只允许 managed 来源权限规则） | AUTO | 前置：managed 设 `allowManagedPermissionRulesOnly:true`，user settings 有 allow 规则；操作：触发被 user 规则覆盖的工具；预期：只有 managed 规则生效 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`config.MergeSettingsSources:310-327`，`conversation/w_c24_hooks_permissions_test.go`） |
| CFG-29 | settings key: `enabledPlugins`（插件启用开关，`pluginId@marketplace` 为 key） | AUTO | 前置：安装插件后 `enabledPlugins:{"myplugin":false}`；操作：列出 slash 命令；预期：该插件命令不出现 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/plugins/loader.go:228 PluginEnabled`） |
| CFG-30 | settings key: `extraKnownMarketplaces`（注册额外 plugin marketplace 来源） | AUTO | 前置：settings `extraKnownMarketplaces:{"my-market":{source:"git+https://..."}}` ；操作：`claude plugin install myplugin@my-market`；预期：从该 marketplace 安装 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/plugins/loader.go:marketplacePluginRootEntries` 消费） |
| CFG-31 | settings key: `strictKnownMarketplaces` / `blockedMarketplaces`（企业 marketplace 白名单/黑名单） | AUTO | 前置：managed settings `blockedMarketplaces:[{source:"npmjs.com"}]`；操作：从 npm 安装插件；预期：被拒 | `utils/settings/types.ts:255+` | ✅ 通过（`internal/plugins/marketplace_policy.go:MarketplacePolicy`） |
| CFG-32 | settings key: `effortLevel`（`low\|medium\|high`，持久化 effort 等级） | AUTO | 前置：user settings `effortLevel:"high"`；操作：`claude --print "x"`；预期：API 请求携带 high thinking budget | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`Runner.EffortLevel`→`request.go:121-123 output_config.effort`，`conversation/settings_wire_test.go`） |
| CFG-33 | settings key: `alwaysThinkingEnabled`（始终启用扩展思考） | AUTO | 前置：settings `alwaysThinkingEnabled:true`；操作：`claude --print "x"`；预期：API 请求含 `thinking` block | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`Runner.AlwaysThinkingEnabled`→`request.go:112-118`，`conversation/settings_wire_test.go`） |
| CFG-34 | settings key: `fastMode`（启用 fast mode，切换到 Haiku 等快速模型） | AUTO | 前置：settings `fastMode:true`；操作：`claude --print "x"`；预期：使用 fast 模型 | `utils/settings/types.ts:255+` | ✅ 通过（`cmd/claude/main.go:FastMode` 字段传至 `anthropicClientFromEnv`） |
| CFG-35 | settings key: `syntaxHighlightingDisabled`（关闭 diff 中语法高亮） | MANUAL | 前置：settings `syntaxHighlightingDisabled:true`；操作：触发文件 edit，查看 diff 输出；预期：无颜色高亮 | `utils/settings/types.ts:255+` | ✅ 通过（G17 commit 2b92b12）（`internal/repl/diff_render.go:RenderToolResultTextWithColorOpt`；`Loop.SetSyntaxHighlightColor`；`InteractiveOptions.SyntaxHighlightingDisabled` 接线；`syntax_highlight_test.go` 验证 color=false 无 ANSI 码） |
| CFG-36 | settings key: `terminalTitleFromRename`（`/rename` 更新 terminal tab 标题） | MANUAL | 前置：settings `terminalTitleFromRename:true`；操作：REPL 中执行 `/rename "My Session"`；预期：terminal 标题变为 "My Session" | `utils/settings/types.ts:255+` | ✅ 通过（G17 commit 2b92b12）（`internal/repl/commands_f4.go:renameHandlerWithTitle`；`InteractiveOptions.TerminalTitleFromRename` 接线；OSC-0 via `Loop.setTerminalTitle`；`terminal_title_rename_test.go` 验证 enabled/disabled 路径） |
| CFG-37 | settings key: `spinnerTipsEnabled` / `spinnerVerbs` / `spinnerTipsOverride`（自定义 spinner 提示词和动词） | MANUAL | 前置：settings `spinnerTipsEnabled:false`；操作：REPL 中触发长操作；预期：无 tip 提示文字 | `utils/settings/types.ts:255+` | ✅ 通过（G17 commit 2b92b12）（`internal/repl/spinner.go:SpinnerConfig/NewSpinnerWithConfig`；`Loop.SetSpinnerConfig`；`InteractiveOptions.SpinnerConfig` 接线；`spinner_config_test.go` 验证 TipsEnabled=false 时无 tip、TipsEnabled=true 时含 tip） |
| CFG-38 | settings key: `skipWebFetchPreflight`（跳过 WebFetch 域名阻止列表预检） | AUTO | 前置：settings `skipWebFetchPreflight:true`；操作：WebFetch 默认阻止的域名；预期：请求放行 | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有，web fetch 工具无预检逻辑） |
| CFG-39 | settings key: `respectGitignore`（文件 picker 遵守 .gitignore，默认 true） | AUTO | 前置：`.gitignore` 含 `secret.txt`；settings `respectGitignore:false`；操作：Glob 搜索；预期：`secret.txt` 被返回 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`internal/tools/file/search_tools.go:1363+4200 RespectGitignore`，已接线） |
| CFG-40 | settings key: `fileSuggestion`（`{type:"command",command:string}` 自定义 `@` 文件建议） | MANUAL | 前置：settings `fileSuggestion:{type:"command",command:"fzf"}`；操作：REPL 输入 `@`；预期：调用 fzf 弹出文件选择 | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有，REPL `@` 触发逻辑未实现） |
| CFG-41 | settings key: `worktree`（`{auto,symlinkDirectories,sparsePaths}` worktree 自动创建配置） | AUTO | 前置：settings `worktree:{auto:true}`；操作：Task isolation=worktree；预期：自动创建 worktree | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有，worktree 自动创建逻辑无消费） |
| CFG-42 | settings key: `autoMemoryEnabled` / `autoMemoryDirectory`（自动记忆开关和目录） | AUTO | 前置：settings `autoMemoryEnabled:true`；操作：长对话结束后；预期：memory 文件写入 autoMemoryDirectory | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有，无 auto-memory 写入逻辑） |
| CFG-43 | settings key: `plansDirectory`（自定义 plans 目录） | AUTO | 前置：settings `plansDirectory:"/tmp/my-plans"`；操作：EnterPlanMode；预期：计划文件写入该目录 | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有，plan tool 无消费） |
| CFG-44 | settings key: `claudeMdExcludes`（CLAUDE.md 加载排除 glob 模式） | AUTO | 前置：settings `claudeMdExcludes:["**/vendor/**"]`；操作：在 vendor 下放 CLAUDE.md，启动会话；预期：vendor CLAUDE.md 不被加载 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`cmd/claude/main.go:3592 ExcludePatterns`，`internal/memory/w_c24_test.go`） |
| CFG-45 | settings key: `autoUpdatesChannel`（`latest\|stable`，自动更新渠道） | AUTO | 前置：settings `autoUpdatesChannel:"stable"`；操作：检查更新；预期：只检查 stable 频道 | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有；update 子命令无渠道区分逻辑） |
| CFG-46 | settings key: `minimumVersion`（最低版本约束） | AUTO | 前置：managed settings `minimumVersion:"1.99.0"`，当前版本更低；操作：启动 claude；预期：启动时拒绝并提示升级 | `utils/settings/types.ts:255+` | ✅ 通过（W-C24 commit 601e4f7；`cmd/claude/main.go:3556 checkMinimumVersion`，`cmd/claude/w_c24_test.go`） |
| CFG-47 | settings key: `forceLoginMethod`（`claudeai\|console`，强制登录方式）| AUTO | 前置：managed settings `forceLoginMethod:"console"`；操作：`claude auth login`；预期：只弹 console OAuth | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有；auth 登录流程无消费，auth seam） |
| CFG-48 | settings key: `companyAnnouncements`（启动时展示公告消息） | MANUAL | 前置：managed settings `companyAnnouncements:["Welcome to Corp Claude"]`；操作：启动 REPL；预期：公告显示在启动消息中 | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有；TUI 启动 UI 无消费） |
| CFG-49 | settings key: `feedbackSurveyRate`（survey 显示概率，0-1） | MANUAL | 前置：settings `feedbackSurveyRate:0`；操作：长时间使用；预期：从不弹 survey | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有；无 survey UI） |
| CFG-50 | settings key: `promptSuggestionEnabled`（启用 prompt 建议） | MANUAL | 前置：settings `promptSuggestionEnabled:false`；操作：空 REPL 输入框；预期：无 prompt 建议 | `utils/settings/types.ts:255+` | ⚠️ 已建未接（字段存储已有；无 prompt suggestion TUI） |
| CFG-51 | global config key: `theme`（颜色主题，存于 `~/.claude/config`，而非 settings.json） | MANUAL | 前置：通过 `/theme dark` 设置主题；操作：重启 REPL；预期：持久化并渲染深色主题 | `tools/ConfigTool/supportedSettings.ts:29`（`source:'global'`） | ✅ 通过（G17 commit 2b92b12）（`tui.RenderStatusLineWithTheme`：dark=reverse-video / light=bold+underline；`Frame.Theme`→`REPLScreen.Theme`→`InteractiveOptions.Theme` 完整传递链；`theme_render_test.go`+`theme_screen_test.go` 验证 dark vs light 输出差异） |
| CFG-52 | global config key: `editorMode`（`normal\|vim`，vim 模式开关） | MANUAL | 前置：通过 `/vim` 切换；操作：REPL 输入框使用 vim 按键；预期：vim 按键绑定生效 | `tools/ConfigTool/supportedSettings.ts:29` | ⚠️ 已建未接（`/vim` 命令写入持久化，但 TUI 输入框 vim key binding 未实现） |
| CFG-53 | global config key: `verbose`（详细日志输出） | AUTO | 前置：设 `verbose:true`；操作：`claude --print "x"`；预期：输出更多 debug 信息 | `tools/ConfigTool/supportedSettings.ts:29` | ✅ 通过（W-C24 commit 601e4f7；`cmd/claude/main.go:3571 runner.Verbose=merged.Verbose`，已接线） |
| CFG-54 | ConfigTool（`/config` slash 命令）：读取并展示合并设置摘要 | AUTO | 前置：任意会话；操作：`claude --print "/config"`；预期：输出 config 摘要（settings files 路径、model、env vars 数量、MCP 数量等） | `tools/ConfigTool/supportedSettings.ts` | ✅ 通过（`internal/conversation/run.go:3120-3139`，`configSubcommand` 实现 config 摘要输出） |
| CFG-55 | ConfigTool: `set <key> <value>`（写入 settings 键值） | AUTO | 前置：任意会话；操作：`claude --print "/config set model claude-haiku-4-5"`；预期：`~/.claude/settings.json` 中 model 被更新 | `tools/ConfigTool/supportedSettings.ts` | ✅ 通过（`internal/conversation/run.go` configSubcommand 分发至 `config.SetSettingsValue`） |
| CFG-56 | ConfigTool: `add permission <behavior> <rule>`（追加权限规则） | AUTO | 前置：任意会话；操作：`/config add permission allow "Bash(echo *)"` ；预期：规则被追加到 project settings permissions | `tools/ConfigTool/supportedSettings.ts` | ✅ 通过（`internal/config/permissions_write.go:AddPermissionRule`，已接线至 configSubcommand） |
| CFG-57 | ConfigTool: 支持 `--global` / `--project` / `--local` scope 标志 | AUTO | 前置：任意会话；操作：`/config set model claude-haiku-4-5 --project`；预期：写入 `.claude/settings.json` 而非 `~/.claude/settings.json` | `tools/ConfigTool/supportedSettings.ts` | ✅ 通过（`configSubcommand` 解析 `--global/--project/--local` 标志） |
| CFG-58 | settings cache（stat-based，避免重复磁盘 IO） | AUTO | 前置：读取 settings 多次；操作：检查文件系统调用次数；预期：相同 mtime 下只读一次 | `utils/settings/settings.ts:resetSettingsCache` | ✅ 通过（`internal/config/settings_cache.go:readSettingsFile`，stat-based cache） |
| CFG-59 | settings 变更检测（30 min 轮询 MDM；文件 watch 本地文件）| AUTO | 前置：运行中的会话；操作：修改 `~/.claude/settings.json`；预期：变更被 `SettingsChangeDetector` 检测到 | `utils/settings/settings.ts:resetSettingsCache` | ✅ 通过（`internal/config/settings_change_detector.go:SettingsChangeDetector`） |
| PLUGIN-01 | 插件 CLI：`claude plugin list`（列出已安装插件） | AUTO | 前置：安装了一个插件；操作：`claude plugin list`；预期：输出插件名称列表 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:568-616`） |
| PLUGIN-02 | 插件 CLI：`claude plugin list --available --json`（列出 marketplace 可用插件） | AUTO | 前置：配置了 marketplace；操作：`claude plugin list --available --json`；预期：输出 JSON 格式的可用插件目录 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:579-616`） |
| PLUGIN-03 | 插件 CLI：`claude plugin install <plugin>` 从 marketplace 安装 | AUTO | 前置：marketplace 中有 `my-plugin`；操作：`claude plugin install my-plugin`；预期：插件下载到 `.claude/plugins/my-plugin/` | `utils/plugins/pluginLoader.ts:installFromNpm` 等 | ✅ 通过（`cmd/claude/main.go:637-665`，`internal/plugins/install.go:InstallMarketplacePluginInScope`） |
| PLUGIN-04 | 插件安装 scope：`--scope project\|user\|local` | AUTO | 前置：操作：`claude plugin install my-plugin --scope user`；预期：安装到 `~/.claude/plugins/` | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:649-654`） |
| PLUGIN-05 | 插件 CLI：`claude plugin update [name]`（更新已安装插件） | AUTO | 前置：安装了旧版插件，marketplace 有新版；操作：`claude plugin update my-plugin`；预期：插件被更新到最新版 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:672`，`internal/plugins/install.go:UpdateInstalledMarketplacePluginsInScope`） |
| PLUGIN-06 | 插件 CLI：`claude plugin uninstall <name>`（卸载插件） | AUTO | 前置：已安装插件；操作：`claude plugin uninstall my-plugin`；预期：插件目录被删除 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:742`，`internal/plugins/install.go:UninstallInstalledPluginInScope`） |
| PLUGIN-07 | 插件 CLI：`claude plugin validate <path>`（验证插件 manifest） | AUTO | 前置：有一个 plugin 目录（含 plugin.json）；操作：`claude plugin validate ./my-plugin`；预期：输出验证结果（pass 或具体错误） | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:707`，`internal/plugins/validate.go:ValidateManifestPath`） |
| PLUGIN-08 | 插件 CLI：`claude plugin enable/disable <name>`（启用/禁用插件） | AUTO | 前置：已安装插件；操作：`claude plugin disable my-plugin`；预期：settings 中 `enabledPlugins.my-plugin:false` | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:774-845`） |
| PLUGIN-09 | 插件 marketplace CLI：`claude plugin marketplace list`（列出已注册 marketplace） | AUTO | 前置：配置了若干 marketplace；操作：`claude plugin marketplace list`；预期：输出所有 marketplace 列表 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:903`） |
| PLUGIN-10 | 插件 marketplace CLI：`claude plugin marketplace add <id> <source>`（注册新 marketplace） | AUTO | 前置：操作：`claude plugin marketplace add my-market git+https://...`；预期：`extraKnownMarketplaces` 写入 settings | `utils/plugins/pluginLoader.ts` | ✅ 通过（`cmd/claude/main.go:952`） |
| PLUGIN-11 | 插件 manifest：`plugin.json` 或 `.claude-plugin/plugin.json`（manifest 双路径发现） | AUTO | 前置：plugin 目录有 `.claude-plugin/plugin.json`；操作：`LoadPluginDir`；预期：manifest 被正确加载 | `utils/plugins/pluginLoader.ts:1319-1335` | ✅ 通过（`internal/plugins/loader.go:LoadPluginDir` 支持两路径） |
| PLUGIN-12 | 插件提供 commands（`commands/` 目录自动发现，也支持 manifest 内联定义） | AUTO | 前置：插件 `commands/hello.md`；操作：安装并列出 `/` 命令；预期：`/hello` 出现在 slash 菜单 | `utils/plugins/pluginLoader.ts:1010` | ✅ 通过（`internal/plugins/loader.go:pluginCommands`，`loadPluginCommandsFromPath`） |
| PLUGIN-13 | 插件提供 skills（`skills/` 目录自动发现） | AUTO | 前置：插件 `skills/my-skill/SKILL.md`；操作：列出 slash 命令；预期：`/my-skill` 可用 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`internal/plugins/loader.go:SkillCommands` 字段，通过 `skills.LoadSkillDirs` 加载） |
| PLUGIN-14 | 插件提供 agents（`agents/` 目录自动发现） | AUTO | 前置：插件 `agents/my-agent/` 目录；操作：发起使用 my-agent 的 Task；预期：agent 配置被加载 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`internal/plugins/loader.go:Agents []PluginAgent`） |
| PLUGIN-15 | 插件提供 hooks（`hooks/hooks.json`） | AUTO | 前置：插件 `hooks/hooks.json` 含 `PreToolUse` hook；操作：触发工具调用；预期：hook 执行 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`internal/plugins/loader.go:HookEvents`，接线至 `configuredHooks` 的 `pluginToolHooks`） |
| PLUGIN-16 | 插件提供 output-styles（`output-styles/` 目录自动发现） | AUTO | 前置：插件 `output-styles/my-style.md`；操作：列出可用 output styles；预期：`my-style` 出现在列表 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`internal/plugins/loader.go:OutputStyles []PluginOutputStyle`，`internal/outputstyles/outputstyles.go:All` 消费） |
| PLUGIN-17 | 插件提供 MCP servers（manifest `mcpServers` 字段） | AUTO | 前置：插件 manifest 含 `mcpServers:{"my-mcp":{...}}`；操作：列出 MCP 工具；预期：my-mcp 工具可用 | `utils/plugins/pluginLoader.ts` | ✅ 通过（`internal/plugins/loader.go:MCPServers`，接线至 MCP 加载） |
| PLUGIN-18 | 插件 `forceForPlugin` output style（插件启用时自动应用该 style） | AUTO | 前置：插件 output style 设 `forceForPlugin:true`，插件已启用；操作：`claude --print "x"`；预期：该 style 的 system prompt 自动生效 | `constants/outputStyles.ts:137-175` | ✅ 通过（`internal/outputstyles/outputstyles.go:35-50 Resolve` 中 `ForceForPlugin` 优先逻辑） |
| PLUGIN-19 | 插件 marketplace 企业白名单/黑名单（`strictKnownMarketplaces` / `blockedMarketplaces`） | AUTO | 前置：managed 设 `blockedMarketplaces:[{source:"npmjs.com"}]`；操作：`claude plugin install npm-plugin`；预期：被拒并报错 | `utils/plugins/schemas.ts:MarketplaceSource` | ✅ 通过（`internal/plugins/marketplace_policy.go:MarketplacePolicy`） |
| PLUGIN-20 | 插件 settings 基础贡献（plugin 可提供最低优先级的 settings，如 `agent` 字段） | AUTO | 前置：插件 manifest 含 settings 贡献；操作：合并 settings；预期：插件 settings 被作为最低优先级基础 | `utils/settings/settingsCache.ts:getPluginSettingsBase` | ✅ 通过（W-C24 commit 601e4f7；`internal/plugins/plugin_settings_base.go:PluginSettingsBase`，`conversation/settings.go:MergedSettings`，`internal/plugins/plugin_settings_base_test.go`） |
| PLUGIN-21 | 插件从 npm 安装（`installFromNpm`） | AUTO | 前置：marketplace source 为 `npmjs.com`；操作：`claude plugin install my-npm-plugin`；预期：通过 npm 下载并安装 | `utils/plugins/pluginLoader.ts:installFromNpm` | ✅ 通过（F11；`internal/plugins/install_from.go:InstallFromNpm()`，npm install --prefix 至临时目录后 CopyPluginDir；⚠️ 需 live npm；`internal/plugins/install_from_test.go`） |
| PLUGIN-22 | 插件从 GitHub shorthand 安装（`user/repo`） | AUTO | 前置：操作：`claude plugin install user/my-repo`；预期：通过 `git clone github.com/user/my-repo` 安装 | `utils/plugins/pluginLoader.ts:installFromGitHub` | ✅ 通过（F11；`internal/plugins/install_from.go:expandGitHubShorthand()`+`InstallFromGitHub()`；`cmd/claude/main.go:pluginCLIInferMarketplaceSourceType()`；`internal/plugins/install_from_test.go`） |
| PLUGIN-23 | 插件从本地路径安装（`installFromLocal`） | AUTO | 前置：本地有插件目录；操作：`claude plugin install ./my-local-plugin`；预期：本地目录被链接/复制到插件目录 | `utils/plugins/pluginLoader.ts:installFromLocal` | ✅ 通过（`internal/plugins/install.go` 支持本地路径，`pluginpkg.InstallMarketplacePluginInScope` 处理本地来源） |
| SKILL-01 | skill 发现：project `.claude/skills/<name>/SKILL.md`（沿 cwd 向上递归至 git root） | AUTO | 前置：`.claude/skills/my-skill/SKILL.md`；操作：列出 slash 命令；预期：`/my-skill` 出现 | `skills/loadSkillsDir.ts:638-803` | ✅ 通过（`internal/skills/discovery.go:ProjectSkillDirs`，`internal/skills/loader.go:LoadSkillDir`） |
| SKILL-02 | skill 发现：user `~/.claude/skills/<name>/SKILL.md` | AUTO | 前置：`~/.claude/skills/global-skill/SKILL.md`；操作：任意项目启动；预期：`/global-skill` 出现在 slash 菜单 | `skills/loadSkillsDir.ts:638-803` | ✅ 通过（`internal/skills/discovery.go:UserSkillDirs`） |
| SKILL-03 | skill 发现：managed `.claude/skills/`（`CLAUDE_CODE_DISABLE_POLICY_SKILLS` 关闭） | AUTO | 前置：managed skills 目录有 policy-skill；操作：列出命令；预期：`/policy-skill` 出现 | `skills/loadSkillsDir.ts:638-803` | ✅ 通过（W-C24 commit 601e4f7；`internal/skills/discovery.go:44 ManagedSkillDirs CLAUDE_CODE_DISABLE_POLICY_SKILLS 门控`，`internal/skills/w_c24_managed_test.go`） |
| SKILL-04 | skill 发现：`--add-dir` 额外目录的 `.claude/skills/` | AUTO | 前置：`--add-dir /extra`，`/extra/.claude/skills/extra-skill/SKILL.md`；操作：列出命令；预期：`/extra-skill` 出现 | `skills/loadSkillsDir.ts:638-803` | ✅ 通过（F11；`internal/skills/discovery.go:AppendSkillRootsFromDir()`；`internal/commands/registry.go:loadProjectSkillPromptsWithSettings()` 迭代 AdditionalDirectories；`internal/commands/registry_adddir_test.go`） |
| SKILL-05 | skill 发现：动态发现（tool 访问子目录文件时，扫描该子目录的 `.claude/skills/`） | AUTO | 前置：`src/.claude/skills/code-skill/SKILL.md`；操作：Read 工具访问 `src/` 下文件；预期：`/code-skill` 动态出现 | `skills/loadSkillsDir.ts:discoverSkillDirsForPaths` | ✅ 通过（`internal/skills/discovery.go:DiscoverSkillDirsForPaths`，已实现；连接到 conversation runner 待确认） |
| SKILL-06 | skill 发现：legacy `commands/` 目录（flat `.md` 文件或含 `SKILL.md` 的子目录） | AUTO | 前置：`.claude/commands/my-cmd.md`；操作：列出 slash 命令；预期：`/my-cmd` 出现 | `skills/loadSkillsDir.ts`（legacy path） | ✅ 通过（`internal/skills/loader.go:LoadLegacyCommandSkills`，`projectLegacyCommandDirs`） |
| SKILL-07 | skill frontmatter: `name`（显示名称覆盖） | AUTO | 前置：`SKILL.md` frontmatter `name: "My Skill"`；操作：列出命令；预期：显示名称为 "My Skill" | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:44-44` displayName 字段） |
| SKILL-08 | skill frontmatter: `description`（技能描述；缺省时取第一个 H1/H2） | AUTO | 前置：SKILL.md 无 description 但有 `# My Tool` 标题；操作：列出命令；预期：description 为 "My Tool" | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:46-49 extractDescription`） |
| SKILL-09 | skill frontmatter: `allowed-tools`（限制技能可调用的工具集） | AUTO | 前置：`allowed-tools: [Bash]`；操作：skill 尝试调用 Edit；预期：被拒 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:65 AllowedTools`，接线至工具执行层） |
| SKILL-10 | skill frontmatter: `argument-hint`（CLI 参数提示字符串） | MANUAL | 前置：`argument-hint: "<query>"`；操作：slash 菜单查看；预期：显示参数提示 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:58 ArgumentHint`） |
| SKILL-11 | skill frontmatter: `arguments`（命名参数列表，用于模板替换） | AUTO | 前置：`arguments: [name, value]`，skill body 含 `$name`；操作：`/my-skill foo bar`；预期：`$name` 替换为 `foo` | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:59 ArgumentNames`） |
| SKILL-12 | skill frontmatter: `when_to_use`（主动调用引导文本） | AUTO | 前置：`when_to_use: "Use when fixing bugs"`；操作：查看 skill 元数据；预期：WhenToUse 字段正确填入 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:66 WhenToUse`） |
| SKILL-13 | skill frontmatter: `version`（版本字符串） | AUTO | 前置：`version: "1.2.3"`；操作：查看 skill 元数据；预期：Version 字段为 "1.2.3" | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:67 Version`） |
| SKILL-14 | skill frontmatter: `model`（覆盖 skill 使用的模型；`inherit` 表示沿用会话模型） | AUTO | 前置：`model: claude-haiku-4-5`；操作：调用 skill；预期：该 skill 的 API 请求使用 haiku | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:68 parseSkillModel`，已存储；消费端待确认） |
| SKILL-15 | skill frontmatter: `disable-model-invocation: true`（不调用 LLM，直接执行 skill） | AUTO | 前置：`disable-model-invocation: true`，skill body 是 shell 脚本；操作：`/my-skill`；预期：直接执行，不调用 API | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:63 DisableModelInvocation`） |
| SKILL-16 | skill frontmatter: `user-invocable: false`（从 `/` 菜单隐藏，但仍可被其他 skill 引用） | AUTO | 前置：`user-invocable: false`；操作：打开 slash 菜单；预期：该 skill 不出现 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:50 userInvocable`，`Hidden: !userInvocable`） |
| SKILL-17 | skill frontmatter: `context: fork`（在 fork context 中运行） | AUTO | 前置：`context: fork`；操作：调用 skill；预期：在隔离 fork 上下文中执行 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:69 parseSkillContext`，已存储） |
| SKILL-18 | skill frontmatter: `agent`（指定使用的 agent 名称） | AUTO | 前置：`agent: code-reviewer`；操作：调用 skill；预期：使用 code-reviewer agent 配置 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:70 Agent`，已存储） |
| SKILL-19 | skill frontmatter: `effort`（effort 等级） | AUTO | 前置：`effort: high`；操作：调用 skill；预期：该 skill 使用 high thinking budget | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:71 Effort`，已存储） |
| SKILL-20 | skill frontmatter: `paths`（条件激活：只有在匹配路径下才出现在 `/` 菜单） | AUTO | 前置：`paths: [src]`；在 `src/` 子目录下；操作：列出命令；预期：该 skill 出现；在 `tests/` 下不出现 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（`internal/skills/loader.go:51 parseSkillPaths`，已存储并传给 `Paths` 字段；过滤逻辑接线待确认） |
| SKILL-21 | skill frontmatter: `hooks`（skill 内联 hooks 配置） | AUTO | 前置：SKILL.md frontmatter 含 hooks 定义；操作：调用 skill；预期：skill 的 hook 生效 | `skills/loadSkillsDir.ts:185-265` | ✅ 通过（F11；`internal/skills/loader.go:parseHooksFrontmatter()`；`contracts.Command.Hooks` 字段；`internal/skills/hooks_test.go`） |
| SKILL-22 | skill 内容：`${CLAUDE_SKILL_DIR}` 变量替换（替换为 skill 目录的绝对路径） | AUTO | 前置：SKILL.md body 含 `${CLAUDE_SKILL_DIR}/scripts/helper.sh`；操作：调用 skill；预期：变量被替换为实际路径 | `skills/loadSkillsDir.ts` | ✅ 通过（`internal/skills/loader.go:510 renderSkillContent`） |
| SKILL-23 | 内置/bundled skills（无需文件系统，启动时程序化注册，随处可用） | AUTO | 前置：任意项目（无 `.claude/skills/`）；操作：列出命令；预期：bundled skills 出现（如 ccgo 自带的 plan/understand 等） | `skills/bundledSkills.ts:registerBundledSkill` | ✅ 通过（F11；`internal/skills/bundled.go:RegisterBundledSkill()`+`GetBundledSkills()`；source=CommandSourceBundled；`internal/skills/bundled_test.go`） |
| SKILL-24 | `strictPluginOnlyCustomization: ["skills"]` 时屏蔽 user/project skills，只保留插件 skills | AUTO | 前置：managed settings 设 `strictPluginOnlyCustomization:["skills"]`；project/user 有普通 skill；操作：列出命令；预期：普通 skills 不出现，插件 skills 仍可用 | `skills/loadSkillsDir.ts` | ✅ 通过（`internal/commands/registry.go:49-56`） |
| SKILL-25 | Skill tool（LLM 可调用 `Skill` 工具来执行 skill 内容） | AUTO | 前置：注册了 skill "analyze"；操作：`claude --print "分析代码"` → LLM 调用 `Skill({name:"analyze"})`；预期：skill 内容被执行 | `tools/SkillTool/*.ts` | ✅ 通过（`internal/tools/skill/tools.go:NewSkillTool`，接线至 conversation runner） |
| STYLE-01 | 内置 output style: `default`（无 system prompt 覆盖） | AUTO | 前置：settings 无 `outputStyle` 或设为 `"default"`；操作：`claude --print "x"`；预期：system prompt 无 output style 区块 | `constants/outputStyles.ts:41` | ✅ 通过（`internal/outputstyles/outputstyles.go:Resolve` 返回 `false` 时不添加 style section） |
| STYLE-02 | 内置 output style: `Explanatory`（在代码前后添加 Insight 教育说明块） | AUTO | 前置：settings `outputStyle:"Explanatory"`；操作：`claude --print "写一个函数"`；预期：system prompt 含 Explanatory 提示词，回复含 Insight 块 | `constants/outputStyles.ts:41` | ✅ 通过（`internal/outputstyles/outputstyles.go:113-131 Builtins`，prompt 完整实现） |
| STYLE-03 | 内置 output style: `Learning`（交互式学习模式，邀请用户写代码片段） | AUTO | 前置：settings `outputStyle:"Learning"`；操作：`claude --print "实现排序"`；预期：system prompt 含 Learning 提示词，回复含 TODO(human) | `constants/outputStyles.ts:41` | ✅ 通过（`internal/outputstyles/outputstyles.go:Builtins`，Learning prompt 完整实现） |
| STYLE-04 | 自定义 output style：user `~/.claude/output-styles/<name>.md`（以文件名为 style 名） | AUTO | 前置：`~/.claude/output-styles/concise.md` 含 style 提示词；操作：settings `outputStyle:"concise"`；预期：style 提示词注入 system prompt | `outputStyles/loadOutputStylesDir.ts` | ✅ 通过（`internal/outputstyles/outputstyles.go:133-143 outputStyleDirs`，user dir 加载实现） |
| STYLE-05 | 自定义 output style：project `.claude/output-styles/<name>.md`（沿 cwd 向上递归至 git root） | AUTO | 前置：`.claude/output-styles/verbose.md`；操作：settings `outputStyle:"verbose"`；预期：style 生效 | `outputStyles/loadOutputStylesDir.ts` | ✅ 通过（`internal/outputstyles/outputstyles.go:146-173 projectOutputStyleDirs`） |
| STYLE-06 | output style frontmatter: `name`（覆盖文件名作为 style 名） | AUTO | 前置：`custom.md` 含 frontmatter `name: "My Custom Style"`；操作：settings `outputStyle:"My Custom Style"`；预期：style 生效 | `outputStyles/loadOutputStylesDir.ts` | ✅ 通过（`internal/outputstyles/outputstyles.go:221 name = firstNonEmpty(frontmatter["name"], base)`） |
| STYLE-07 | output style frontmatter: `description`（style 描述，用于 UI 展示） | AUTO | 前置：frontmatter `description: "Adds verbose comments"`；操作：列出 styles；预期：description 正确返回 | `outputStyles/loadOutputStylesDir.ts` | ✅ 通过（`internal/outputstyles/outputstyles.go:227 Description`） |
| STYLE-08 | output style frontmatter: `keep-coding-instructions`（是否保留内置 coding 指引） | AUTO | 前置：frontmatter `keep-coding-instructions: false`；操作：style 生效时；预期：system prompt 不含内置 coding 指引 | `outputStyles/loadOutputStylesDir.ts` | ✅ 通过（`internal/outputstyles/outputstyles.go:229 KeepCodingInstructions`，已存储；消费端待确认） |
| STYLE-09 | output style 优先级：built-in < plugin < user < project（同名时后者覆盖） | AUTO | 前置：user 和 project 各有同名 `custom` style，内容不同；操作：settings `outputStyle:"custom"`；预期：使用 project 版本 | `constants/outputStyles.ts:137-175` | ✅ 通过（`internal/outputstyles/outputstyles.go:84-110 All`，project 覆盖 user 已实现） |
| STYLE-10 | 插件 output style `forceForPlugin:true` 优先于所有 `outputStyle` 设置 | AUTO | 前置：已启用插件，其 style 设 `forceForPlugin:true`，settings 同时设了其他 style；操作：`claude --print "x"`；预期：插件强制 style 生效 | `constants/outputStyles.ts:137-175` | ✅ 通过（`internal/outputstyles/outputstyles.go:35-50 Resolve` 中 `ForceForPlugin` 优先检查） |
| STYLE-11 | `/config set outputStyle <name>` 通过 ConfigTool 切换 style | AUTO | 前置：有自定义 style "concise"；操作：`claude --print "/config set outputStyle concise"`；预期：`~/.claude/settings.json` 的 `outputStyle` 被更新，下次会话生效 | `constants/outputStyles.ts` | ✅ 通过（ConfigTool `set` 命令 + `contracts.Settings.OutputStyle` 字段，已接线） |
| STYLE-12 | 列出所有可用 output styles（内置 + 用户 + 项目 + 插件） | AUTO | 前置：有内置、用户自定义、插件 styles；操作：`internal/outputstyles.Names(cwd, plugins)`；预期：`default`、`Explanatory`、`Learning` 及所有自定义 style 均返回 | `constants/outputStyles.ts:getAllOutputStyles` | ✅ 通过（`internal/outputstyles/outputstyles.go:63-74 Names`） |

小计：共 **92 行**。✅ 通过：**79**，⚠️ 已建未接：**8**，❌ 缺失：**0**，N/A：**0**。（F11：CFG-03/14/15/PLUGIN-21/22/SKILL-04/21/23 新增 8 条 ✅，CFG-03 发现已实现；G17 commit 2b92b12：CFG-19/35/36/37/51 新增 5 条 ✅，仍 ⚠️：CFG-20/38/40/41/42/43/45/47/48/49/50/52 等 render-unimplementable 或 infra-gap 项）
