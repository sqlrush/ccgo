## 14. 编排

本章覆盖 ccgo 编排层的全部 CC 功能：同步/异步子 agent、本地 Team 调度与协调、git-worktree 隔离、Task 工具参数验证、AgentRegistry、AgentFile 定义，以及相关辅助工具（TaskOutput/KillTask/SendMessage/ResumeTask/TeamCreate 等）。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| ORCH-01 | Task 工具同步启动子 agent（`run=true`），完整执行模型轮次，返回 summary | AUTO | 前置：有效 session + agentfile；操作：`claude --print` 触发 Task `{run:true, subagent_type:"general-purpose", prompt:"echo hi"}`；预期：tool result 含 `status:completed`，`summary` 非空，sidechain transcript 落盘 | src/tools/AgentTool/AgentTool.tsx:608, src/tools/AgentTool/runAgent.ts:248 | ✅ 通过 |
| ORCH-02 | Task 工具仅记录 sidechain（`run=false`），不立即执行，返回 `status:running` | AUTO | 前置：有效 session；操作：Task `{run:false}`；预期：返回 `sidechain_id`，`run:false`，sidechain 状态 running；无 API 调用发生 | src/tools/AgentTool/AgentTool.tsx:608 | ✅ 通过 |
| ORCH-03 | `run_in_background=true` 时 Task 异步派发给 AgentRegistry，调用方立即收到 `status:async_launched` | AUTO | 前置：session 有效；操作：Task `{run_in_background:true, run:true}`；预期：工具立即返回，agentID 可从 registry 查询，子 agent 在后台执行 | src/tools/AgentTool/AgentTool.tsx:567, 688, src/tasks/LocalAgentTask/LocalAgentTask.tsx:registerAsyncAgent | ✅ 通过（W-Batch-C commit e735e5e）|
| ORCH-04 | AgentRegistry `StartBackground` 注册后台 agent，goroutine 存储 Outcome；`Snapshot` 返回所有状态拷贝；`Harvest` 取回结果并从 registry 删除 | AUTO | 前置：空 registry；操作：`StartBackground(id, fn)`；预期：`Snapshot` 返回 `AgentRunning`；fn 完成后 `Snapshot` 返回 `AgentDone`；`Harvest` 返回 Outcome 并清空条目 | src/tasks/LocalAgentTask/LocalAgentTask.tsx:registerAsyncAgent, completeAgentTask | ✅ 通过（AgentRegistry 本身实现完整；gap 在上层调用路径——见 ORCH-03） |
| ORCH-05 | Task `model` 参数运行时覆盖子 agent 使用的模型（优先于 agentfile model） | AUTO | 前置：agentfile 定义 `model:haiku`；操作：Task `{model:"opus", subagent_type:"...", run:true}`；预期：子 agent 使用 opus 模型（API 请求头可验证） | src/tools/AgentTool/runAgent.ts:340-345（`getAgentModel(agentDefinition.model, parentModel, toolSpecifiedModel, permMode)`，input model 优先） | ✅ 通过（W-Batch-C commit e735e5e）|
| ORCH-06 | Task `isolation:"worktree"` 在 `<repo-parent>/.ccgo-worktrees/` 创建 git worktree，子 agent 在其中执行 | AUTO | 前置：git 仓库；操作：Task `{isolation:"worktree", run:true}`；预期：`worktree_path` 指向 `.ccgo-worktrees/` 下独立目录，子 agent cwd 设置为该路径 | src/tools/AgentTool/AgentTool.tsx:590-592（`createAgentWorktree`） | ✅ 通过 |
| ORCH-07 | 子 agent 完成后如 worktree 无变更则自动清理（remove）；有变更则保留并通知 | AUTO | 前置：worktree 隔离的 task；操作：子 agent 运行，不修改任何文件；预期：worktree 目录被删除，sidechain metadata `worktree_cleanup_status:removed` | src/tools/AgentTool/AgentTool.tsx:667-684（`hasWorktreeChanges`→`removeAgentWorktree`） | ✅ 通过（`CleanupOwnedWorktree` 实现了此逻辑） |
| ORCH-08 | Task `isolation:"remote"` 被拒绝并返回明确错误（云端栈 OUT-of-scope） | AUTO | 前置：任意 session；操作：Task `{isolation:"remote"}`；预期：validate 返回错误 `"isolation \"remote\" is not supported"`，工具拒绝执行 | src/tools/AgentTool/AgentTool.tsx:435（仅 ant 编译才走 remote 路径） | ✅ 通过（`ValidateIsolation("remote")` 明确返回错误） |
| ORCH-09 | AgentFile 解析 `.claude/agents/*.md` frontmatter（name/description/tools/model/effort/color/memory/prompt），支持用户目录和项目目录 | AUTO | 前置：`~/.claude/agents/foo.md` 和 `./.claude/agents/bar.md`；操作：`agentfile.List(userDir, projectDir)`；预期：两个 AgentFile 正确解析 name/model/tools/prompt | src/tools/AgentTool/loadAgentsDir.ts:73-99（AgentJsonSchema），builtInAgents.ts | ✅ 通过 |
| ORCH-10 | AgentFile 支持 `permissionMode` frontmatter，子 agent 执行时使用该权限模式 | AUTO | 前置：agentfile 含 `permissionMode: plan`；操作：Task 触发该 agent；预期：子 agent runner 使用 plan 模式权限 | src/tools/AgentTool/loadAgentsDir.ts:73（`permissionMode: z.enum(PERMISSION_MODES).optional()`），runAgent.ts:413-430 | ✅ 通过（`taskSubagentPermissionMode` 在 task_agent.go 中处理） |
| ORCH-11 | AgentFile 支持 `tools` allowlist，子 agent 执行时仅持有指定工具 | AUTO | 前置：agentfile 含 `tools: [Read, Grep]`；操作：子 agent 尝试调用 Bash；预期：权限拒绝，仅 Read/Grep 可用 | src/tools/AgentTool/loadAgentsDir.ts:73（`tools: z.array(z.string()).optional()`），runAgent.ts:466-479 | ✅ 通过（`taskSubagentAllowedToolExecutor` 在 task_agent.go 中处理） |
| ORCH-12 | AgentFile `isolation: worktree` frontmatter 使 agent 默认使用 worktree 隔离（无需调用方显式传） | AUTO | 前置：agentfile 含 `isolation: worktree`；操作：Task 调用该 agent，未显式传 `isolation`；预期：worktree 自动创建 | src/tools/AgentTool/AgentTool.tsx:431（`const effectiveIsolation = isolation ?? selectedAgent.isolation`） | ❌ 缺失（ccgo agentfile.go 不解析 `isolation` frontmatter 字段；`tool.AgentInfo` 结构无 isolation 字段；`taskInputRequestsWorktree` 仅检查 `input.Isolation` 或 settings 中的 worktree 默认开关，不读 agent 定义） |
| ORCH-13 | AgentFile `background: true` frontmatter 使 agent 始终作为后台任务运行 | AUTO | 前置：agentfile 含 `background: true`；操作：Task 调用；预期：agent 自动进入后台注册路径，调用方不阻塞 | src/tools/AgentTool/AgentTool.tsx:361, 426, 567（`selectedAgent.background === true`） | ❌ 缺失（`tool.AgentInfo` 无 `Background` 字段；即使 agentfile 含此字段也不读取；且后台路径整体未接线——见 ORCH-03） |
| ORCH-14 | 子 agent 启动时执行 `SubagentStart` 钩子，完成时执行 `SubagentStop` 钩子 | AUTO | 前置：settings 含 SubagentStart/SubagentStop 钩子；操作：Task `{run:true}`；预期：钩子在子 agent 生命周期正确触发，附加上下文注入 | src/tools/AgentTool/runAgent.ts:531-555（`executeSubagentStartHooks`） | ✅ 通过（`runSubagentStartHooks`/`runSubagentStopHooks` 在 task_agent.go 中实现并测试） |
| ORCH-15 | TaskOutput 读取 sidechain 状态和 transcript；无 task_id 时列出全部 task | AUTO | 前置：已有多个 sidechain；操作：`TaskOutput{}` 和 `TaskOutput{task_id:"..."}` 两种调用；预期：前者返回 task 列表，后者返回指定 sidechain 的 transcript tail | src/tools/TaskOutputTool/TaskOutputTool.tsx:144 | ✅ 通过 |
| ORCH-16 | KillTask 取消运行中的 task，更新 sidechain 状态为 cancelled，并清理 owned worktree | AUTO | 前置：运行中 task + worktree 隔离；操作：`KillTask{task_id:...}`；预期：状态变 cancelled，worktree 目录被删除，cleanup 状态写入 metadata | src/tools/TaskStopTool/TaskStopTool.ts:39 | ✅ 通过 |
| ORCH-17 | SendMessage 向运行中 task 的 sidechain 追加 user 消息 | AUTO | 前置：task 状态 running；操作：`SendMessage{task_id:..., message:"继续"}`；预期：sidechain transcript 新增一条 user 消息，返回 message_uuid | src/tools/SendMessageTool | ✅ 通过 |
| ORCH-18 | ResumeTask 返回 task 的 `can_resume` 标志、metadata 和 tail 消息，供调用方决定是否继续 | AUTO | 前置：已完成 task；操作：`ResumeTask{task_id:...}`；预期：结果含 `can_resume`、`truncated`、`resume_messages` | src/tools/AgentTool（resume 路径），src/tools/AgentTool/resumeAgent.ts | ✅ 通过 |
| ORCH-19 | TeamCreate 创建/更新命名 team，关联 coordinator 和 member task | AUTO | 前置：2 个已存在 sidechain；操作：`TeamCreate{team_id:"t1", coordinator_task_id:"...", task_ids:[...]}`；预期：teams.json 落盘，返回 team 状态 | src/tools/TeamCreateTool | ✅ 通过 |
| ORCH-20 | TeamDelete 删除 team（不删除底层 task） | AUTO | 前置：已有 team；操作：`TeamDelete{team_id:"t1"}`；预期：teams.json 中条目删除，task sidechain 不受影响 | src/tools/TeamDeleteTool | ✅ 通过 |
| ORCH-21 | TeamOutput 列出所有 team 或返回单 team 状态（含 coordinator 和成员 task 状态） | AUTO | 前置：已有 team；操作：`TeamOutput{}` 和 `TeamOutput{team_id:"t1"}`；预期：前者列出所有 team，后者返回 coordinator + tasks 详情 | src/tools/TaskOutputTool（team 路径） | ✅ 通过 |
| ORCH-22 | TeamSendMessage 向 team 所有 running members（或 coordinator/all）广播同一消息 | AUTO | 前置：team 含 2 个 running tasks；操作：`TeamSendMessage{team_id:..., message:"..."}`；预期：2 个 sidechain 各追加一条 user 消息 | src/tools/SendMessageTool（team 版） | ✅ 通过 |
| ORCH-23 | TeamDispatch 向 team 各 member 派发个性化 assignment 消息（含格式化上下文） | AUTO | 前置：team 含 running members；操作：`TeamDispatch{team_id:..., assignments:[{task_id:..., message:...}]}`；预期：每个 task 收到含 team/assignment 上下文的格式化消息 | src/tools/TeamDispatch（CC 实现在 AgentTool.tsx 同步分支） | ⚠️ 已建未接（`callTeamDispatch` 将个性化消息追加到 sidechain 的 APPEND-ONLY 路径；`orchestration.TeamRunner` 存在且测试覆盖，但未在 `callTeamDispatch` 中调用；团队成员不会因此触发新的 model 轮次） |
| ORCH-24 | TeamCoordinate 向 coordinator task 发送含 team 状态上下文的协调请求消息 | AUTO | 前置：team 含 running coordinator；操作：`TeamCoordinate{team_id:..., message:"..."}`；预期：coordinator sidechain 追加含 team 状态摘要 + message 的格式化消息 | src/tools/TeamCreateTool（coordinate 路径，CC 的 KAIROS/Team 特性） | ⚠️ 已建未接（`callTeamCoordinate` 将消息追加至 coordinator sidechain；同样是 APPEND-ONLY——coordinator task 不会被 `TeamRunner.RunTeammate` 触发新的 model 轮次；`orchestration.TeamRunner` 已建未接） |
| ORCH-25 | TeamSchedule 对所有 running members 确定性分配 objective（按 member 编号格式化） | AUTO | 前置：team 含 3 个 running members；操作：`TeamSchedule{team_id:..., objective:"..."}`；预期：3 个 task 各收到含 member_index/member_count 的格式化 assignment | src/tools/AgentTool（team scheduling 路径） | ✅ 通过 |
| ORCH-26 | TeamAutoSchedule 先向 coordinator 发送 briefing，再对 members 分配 objective（可接受预生成 assignments） | AUTO | 前置：team 含 coordinator + 2 members；操作：`TeamAutoSchedule{team_id:..., objective:"..."}`；预期：coordinator 收到带 team 状态的 briefing，members 收到分配消息 | src/tools/AgentTool（auto-schedule 路径） | ✅ 通过 |
| ORCH-27 | ScheduleCron create/list/delete/trigger/run_due — 会话作用域 cron 调度 | AUTO | 前置：有效 session + team；操作：`ScheduleCron{action:"create", cron:"* * * * *", message:"..."}`，再 `list`，再 `delete`；预期：三步均成功，cron 状态写入 sidechain | src/tools/ScheduleCronTool | ✅ 通过 |
| ORCH-28 | RemoteTrigger 向 team 注入远程事件消息，event_id 用于去重幂等 | AUTO | 前置：team 含 running task；操作：同一 event_id 发送两次 `RemoteTrigger`；预期：第一次追加消息成功，第二次因 receipt 已存在而幂等跳过 | src/tools/RemoteTriggerTool | ✅ 通过 |
| ORCH-29 | Sleep 工具等待指定时长（支持 duration_ms/seconds/duration 三种格式），最大 60s，ctx 取消后提前退出 | AUTO | 前置：任意 session；操作：`Sleep{duration_ms:100}`；预期：工具约 100ms 后返回，ctx 取消时立即返回 | src/tools/SleepTool | ✅ 通过 |
| ORCH-30 | Brief 工具生成结构化 handoff 摘要（title/status/summary/details/next_steps/risks）并验证字符数 ≤ 20000 | AUTO | 前置：任意 session；操作：`Brief{summary:"...", next_steps:[...]}`；预期：返回格式化 brief，字符超限时验证拒绝 | src/tools/BriefTool | ✅ 通过 |
| ORCH-31 | 内置 general-purpose agent 在无 subagent_type 时作为默认 agent 类型 | AUTO | 前置：无 agentfile；操作：Task `{subagent_type:"general-purpose", run:true, prompt:"..."}`；预期：子 agent 以标准系统提示执行 | src/tools/AgentTool/built-in/generalPurposeAgent.ts | ✅ 通过 |
| ORCH-32 | AgentFile `subagent_type` 枚举动态注入 Task 工具 schema（inputSchema 根据可用 agents 限制枚举值） | AUTO | 前置：metadata 含 2 个 AgentInfo；操作：`taskInputSchema(ctx)` 调用；预期：`subagent_type.enum` 仅含已注册的 agent 名称 | src/tools/AgentTool/AgentTool.tsx:200-225（prompt 动态生成） | ✅ 通过 |
| ORCH-33 | worktree 隔离：sparse-checkout 仅检出 `worktree.sparse_paths` 指定路径 | AUTO | 前置：settings 含 `worktree.sparse_paths:[src]`；操作：Task `{isolation:"worktree"}`；预期：worktree 中仅含 src/ 目录 | src/tools/AgentTool/AgentTool.tsx（`createAgentWorktree` 在 utils/worktree.ts） | ✅ 通过（`applyTaskWorktreeSettings` 处理 sparse-checkout） |
| ORCH-34 | worktree 隔离：`symlink_directories` 将主仓库目录符号链接到 worktree | AUTO | 前置：settings 含 `worktree.symlink_directories:[node_modules]`；操作：Task `{isolation:"worktree"}`；预期：worktree 中 node_modules 是指向主仓库的软链接 | src/tools/AgentTool/AgentTool.tsx（worktree.ts） | ✅ 通过 |
| ORCH-35 | 子 agent 读取 `CLAUDE.md` 层级（除 `omitClaudeMd:true` 的只读 agent）；`omitClaudeMd` 时跳过 claudeMd userContext 注入 | AUTO | 前置：项目含 `CLAUDE.md`；操作：Task 调用 general-purpose agent；预期：子 agent system prompt 含 CLAUDE.md 内容（Explore/Plan 类 agent 跳过） | src/tools/AgentTool/runAgent.ts:386-396（`shouldOmitClaudeMd` 逻辑） | ❌ 缺失（ccgo 子 agent runner 直接使用父 runner 的 SystemPrompt，无 claudeMd 注入/跳过逻辑；`omitClaudeMd` agentfile 字段不存在） |
| TEAM-01 | 本地 Team 真实模型轮次：`orchestration.TeamRunner.RunTeammate` 调用 `RunTurnFunc`（含 conversation.Runner）并将结果持久化到 sidechain | AUTO | 前置：TestTeamRunner 用 fake factory；操作：`RunTeammate(ctx, tm, history, prompt)`；预期：factory 被调用，Persist 被调用，返回非空 Summary | src/utils/swarm/inProcessRunner.ts:884（`runInProcessRunner`，真实模型轮次） | ⚠️ 已建未接（`TeamRunner` 本身实现完整且有测试 `runner_test.go`；但 `callTeamDispatch`/`callTeamCoordinate`/`callTeamSendMessage` 等工具均走 APPEND-ONLY 路径，不调用 `TeamRunner.RunTeammate`；在真实运行中，team member 不会被驱动进行新的模型回合） |
| TEAM-02 | 自动后台转换（auto-background）：agent 运行超过阈值时间（CC 默认 120s）自动转为后台任务，前台解锁 | MANUAL | 前置：CC 开启 auto-background；操作：Agent 长时间运行；预期：2min 后工具返回，背景中继续执行，通知到达后显示结果 | src/tools/AgentTool/AgentTool.tsx:72-77（`getAutoBackgroundMs()`），819-930（auto-background 逻辑） | N/A（自动后台转换依赖前台 TUI 注册路径，ccgo REPL 未实现完整前台 agent 注册框架；整体异步 agent 路径均待接线） |
| TEAM-03 | agent swarms 特性门控：`ENABLE_AGENT_SWARMS` 关闭时 `team_name`/`name` 参数被拒绝，报 "Agent Teams is not yet available" | AUTO | 前置：未启用 swarms；操作：Task `{team_name:"t1", name:"worker1"}`；预期：工具返回特性门控错误 | src/tools/AgentTool/AgentTool.tsx:262-263（`isAgentSwarmsEnabled()` 检查） | N/A（ccgo Task 工具不暴露 `team_name`/`name` 参数；Agent swarms 基于 tmux/iTerm2 的 in-process teammate 路径 OUT-of-scope） |
| TEAM-04 | in-process teammate 不可产生后台 agent（in-process teammate 不支持 `run_in_background=true`） | AUTO | 前置：是 in-process teammate 上下文；操作：Task `{run_in_background:true}`；预期：工具报错阻止 | src/tools/AgentTool/AgentTool.tsx:278-279 | N/A（in-process teammate / tmux 路径 OUT-of-scope） |

小计：共 **36 行**（含 TEAM 子章）。✅ 通过：24；⚠️ 已建未接：3（ORCH-23/24、TEAM-01）；❌ 缺失：3（ORCH-12/13/35）；N/A：4（ORCH-08 实为 ✅，TEAM-02/03/04）。（W-Batch-C: +2 ✅: ORCH-03/05）

> **说明：** ORCH-08 计入 ✅（remote isolation 已明确拒绝，符合 OUT-of-scope 的期望行为）；TEAM-02/03/04 标 N/A 因依赖 OUT-of-scope 的 swarms/tmux 后端。实际 gap 合计：⚠️ 3 + ❌ 3 = **6 项 worklist 条目**。
