## 4. agent 循环 / API

本章覆盖 ccgo 的 agent 主循环与 Anthropic API 交互的全部功能面，包括 SSE 流式处理、扩展思考、prompt 缓存、模型 fallback、stop_reason 控制流、孤儿 tool_result 修复、micro/auto 压缩、token 预算以及请求/重试形状等。

| ID | 功能 | 执行层 | 测试（given → when → then） | CC 参照 | 状态 |
|---|---|---|---|---|---|
| LOOP-01 | SSE 流式解析：`event:` + `data:` 逐行拼装，空行分发 | AUTO | 前置：stub SSE 服务器返回合法 SSE 分块；操作：调用 `ParseStream`；预期：handler 按顺序收到 `message_start`、`content_block_start`、`content_block_delta`、`content_block_stop`、`message_delta`、`message_stop` 事件，顺序与原始流一致 | `src/services/api/claude.ts:752`（`queryModelWithStreaming`）；SDK 底层 SSE 解析 | ✅ 通过（`internal/api/anthropic/stream.go` `ParseStream` 实现完整 SSE 解析，有 10 MiB buffer，`stream_accumulator_thinking_test.go` 覆盖） |
| LOOP-02 | 流式增量渲染：每个 `content_block_delta` 通过 `OnEvent` 发出 `EventStreamEvent`，供 REPL 实时刷新 | AUTO | 前置：Runner 设 `UseStreaming=true`；操作：RunTurn 触发流式请求；预期：`OnEvent` 在 message_stop 前收到多个 `EventStreamEvent`，UI 可据此渐进渲染 | `src/services/api/claude.ts:752-779`；`queryModelWithStreaming` 在 REPL 中实时 yield | ✅ 通过（`run.go:7678-7681` 每个 StreamEvent 调用 `r.emit(Event{Type:EventStreamEvent, StreamEvent:&eventCopy})`） |
| LOOP-03 | 流式累加器：`thinking_delta` 累加到 `block.Text`；`signature_delta` 写入 `block.Signature` | AUTO | 前置：按 thinking 序列送事件；操作：`StreamAccumulator.Add`；预期：`block.Text=="Let me think."`, `block.Signature=="SIG=="` | `src/services/api/claude.ts`（streaming accumulation of thinking+signature） | ✅ 通过（`stream_accumulator.go:39-45`；`stream_accumulator_thinking_test.go` 测试覆盖） |
| LOOP-04 | `input_json_delta` 累加：多个 `input_json_delta` delta 拼合后在 `content_block_stop` 时反序列化为 `block.Input` | AUTO | 前置：tool_use block 分多个 delta 送入；操作：送完再 content_block_stop；预期：`block.Input` 为合法 JSON | `src/services/api/claude.ts`（streaming JSON assembly） | ✅ 通过（`stream_accumulator.go:47-58`） |
| LOOP-05 | 扩展思考 `thinking` 请求参数：`model.Capability.SupportsThinking` 时，请求体含 `{"thinking":{"type":"enabled","budget_tokens":N}}`；`budget_tokens` 不超过 `max_tokens-1` | AUTO | 前置：Runner.ThinkingBudgetTokens=8000, MaxTokens=16384；操作：buildRequest；预期：JSON 中有 `thinking.budget_tokens=8000` | `src/services/api/claude.ts:1398-1455`（thinking config assembly） | ✅ 通过（`conversation/thinking.go:15-34`；`conversation/request.go:88-93`；`thinking_test.go` 覆盖） |
| LOOP-06 | 扩展思考 `signature` 序列化：thinking block 含 `signature` 字段，序列化/反序列化后原值保留 | AUTO | 前置：assistant 消息含 thinking block + signature；操作：JSON marshal/unmarshal；预期：反序列化后 `block.Signature` 完整 | `src/services/api/claude.ts`（signature round-trip）；CC 通过 SDK messages.ts 保持 thinking blocks | ✅ 通过（`contracts/messages.go:46,64,89,128-141`；`contracts/signature_test.go` 覆盖） |
| LOOP-07 | 交错思考（interleaved thinking）beta header：`interleaved-thinking-2025-05-14` 在请求中跟随 thinking 模型时自动附加 | AUTO | 前置：模型支持 thinking；操作：buildRequest 检查 betas；预期：请求 header 含 `anthropic-beta: interleaved-thinking-2025-05-14` | `src/constants/betas.ts:4`（`INTERLEAVED_THINKING_BETA_HEADER`）；`src/utils/betas.ts:258-261`（自动 push） | ✅ 通过（F6a commit 02b7690）（`InterleavedThinkingBetaHeader` 常量已存在；`requestUsesThinking` 检测 request.Thinking 非空；`dynamicBetaHeadersForRequest` 自动追加；`TestDynamicBetaHeadersIncludesInterleavedThinkingWhenThinkingSet` 覆盖） |
| LOOP-08 | `redacted_thinking` block：API 返回 `redacted_thinking` 类型时 ccgo 保留并能回送（不报 400） | AUTO | 前置：API 响应含 `{"type":"redacted_thinking","data":"..."}` block；操作：Runner 在下一轮把 history 送 API；预期：不出 400，block 作为 opaque pass-through | `src/constants/betas.ts:20`（`REDACT_THINKING_BETA_HEADER`）；CC `messages.ts` normalizeContent | ✅ 通过（F6a commit 02b7690）（`ContentRedactedThinking` 常量；`ContentBlock.Data string` 字段；`MarshalJSON` 输出 `{"type":"redacted_thinking","data":"…"}` 规范格式；`UnmarshalJSON` aux.Data 用 json.RawMessage 避免类型冲突；NormalizeForAPI 原样透传；`TestRedactedThinkingBlockMarshalRoundTrip` + `TestRedactedThinkingPreservedInRequest` 覆盖） |
| LOOP-09 | Prompt 缓存断点：`EnablePromptCaching=true` 时，`AddCacheBreakpoints` 在最后一条用户消息最末 block 附加 `cache_control: {type:"ephemeral"}`；`SkipCacheWrite` 时退后一条 | AUTO | 前置：Runner.EnablePromptCaching=true；操作：buildRequest；预期：倒数第一条 user 消息末 block `CacheControl.Type="ephemeral"` | `src/services/api/claude.ts:373,588-630`（`getCacheControl`、`userMessageToMessageParam`） | ✅ 通过（`internal/api/anthropic/cache.go:11-41`；`conversation/cache_request_test.go` 覆盖） |
| LOOP-10 | Prompt 缓存 1h TTL：`PromptCacheTTL="1h"` 时 cache_control 含 `ttl:"1h"` | AUTO | 前置：Runner.PromptCacheTTL="1h"；操作：buildRequest；预期：cache_control.TTL="1h" | `src/services/api/claude.ts:369-374`（`getCacheControl` with ttl） | ✅ 通过（`conversation/request.go:74`；`cache_request_test.go:27,47` 覆盖） |
| LOOP-11 | `prompt-caching-scope-2026-01-05` beta header：请求含 cache_control 时自动加入 | AUTO | 前置：消息含 cache_control；操作：DynamicBetaHeaders；预期：beta 列表含 `prompt-caching-scope-2026-01-05` | `src/constants/betas.ts:16`（`PROMPT_CACHING_SCOPE_BETA_HEADER`）；`src/services/api/claude.ts:1218-1222` | ✅ 通过（`betas.go:10,52-56,68-83`） |
| LOOP-12 | 全局缓存 scope：`CacheControl.Scope="global"` 时系统提示 block 附加 `scope:"global"` | AUTO | 前置：request 设 global scope；操作：buildRequest；预期：system block `cache_control.scope="global"` | `src/services/api/claude.ts:1207-1229`（`shouldUseGlobalCacheScope`，`globalCacheStrategy`） | ✅ 通过（W-Batch-B）（`Request.UseGlobalCacheScope` 字段；`buildRequest` 在 first-party URL + caching enabled 时附加 `scope:"global"` 到 system block；`DynamicBetaHeaders` 在 `UseGlobalCacheScope` 时包含 `PromptCachingScopeBetaHeader`；`TestBuildRequestGlobalCacheScopeOnSystemBlock` 覆盖） |
| LOOP-13 | 模型 fallback（overloaded → fallback model）：连续 529/overloaded 错误达阈值时，依序尝试 `Runner.FallbackModels` 列表中下一模型 | AUTO | 前置：Runner.FallbackModels=["claude-haiku-4-5"]，stub API 首次返回 529；操作：RunTurn；预期：Result.ModelsAttempt 含两个元素，第二个为 haiku | `src/services/api/withRetry.ts:330-364`（`FallbackTriggeredError`、连续 529 计数） | ✅ 通过（`run.go:7606-7644`；`isFallbackEligible` 用 `apiErr.Retryable()`；`RetryInfo.Fallback=true` 事件发出） |
| LOOP-14 | 流式请求 fallback 到非流式：流开始前（`seenStreamEvent==false`）出错时，自动降级为非流式 `CreateMessage` | AUTO | 前置：Runner.UseStreaming=true；stub 在 connect 阶段报错（0 字节响应）；操作：RunTurn；预期：Runner 用非流式重发，正常返回 | `src/services/api/claude.ts:818-917`（`executeNonStreamingRequest`，流失败重试非流） | ✅ 通过（`run.go:7683-7687`：`!seenStreamEvent` → `r.Client.CreateMessage(ctx, fallbackRequest)`） |
| LOOP-15 | 重试指数退避（429/5xx）：`ShouldRetryAPIError` 判断是否重试，`RetryDelay` 实现指数退避+抖动，`Retry-After` header 优先 | AUTO | 前置：stub API 返回 429 带 `Retry-After: 2`；操作：`RetryDelay(1, "2", config)`；预期：delay=2s | `src/services/api/withRetry.ts:530-548`（`getRetryDelay`）；`src/services/api/withRetry.ts:696-730`（`shouldRetry`） | ✅ 通过（`internal/api/anthropic/retry.go:44-72`；`retry.go:89-111`；`ShouldRetryAPIError` + `RetryDelay`） |
| LOOP-16 | 重试上限：超过 `DefaultMaxRetries=10` 次后不再重试，返回错误 | AUTO | 前置：stub API 持续返回 500；操作：createMessage 带默认 RetryConfig；预期：重试 10 次后返回错误，不无限循环 | `src/services/api/withRetry.ts:189`（`maxRetries`）；`withRetry.ts:370-373` | ✅ 通过（`retry.go:15:DefaultMaxRetries=10`；`client.go` 调用带重试逻辑） |
| LOOP-17 | 530/overloaded 错误识别：HTTP 529 或 message 含 `"type":"overloaded_error"` 均被视为 overloaded | AUTO | 前置：`APIError{StatusCode:529}` 和 `APIError{Message:`...overloaded_error...`}`；操作：`IsOverloadedError`；预期：两者均返回 true | `src/services/api/withRetry.ts:610-621`（`is529Error`） | ✅ 通过（`retry.go:113-115`） |
| LOOP-18 | `max_tokens` context overflow（400）自动调整 `max_tokens` 重试：API 返回 `input length and max_tokens exceed context limit: A + B > C` 时，重算 max_tokens 后重试 | AUTO | 前置：stub API 首次返回含该 message 的 400；操作：`ParseMaxTokensContextOverflowError`；预期：解析出 inputTokens/maxTokens/contextLimit 三值，`AdjustMaxTokensForContextOverflow` 返回调整后值 | `src/services/api/withRetry.ts:386-427`（`parseMaxTokensContextOverflowError`） | ✅ 通过（`retry.go:117-152`；`ParseMaxTokensContextOverflowError`、`AdjustMaxTokensForContextOverflow` 实现完整） |
| LOOP-19 | `stop_reason: end_turn` 控制流：收到 `end_turn`（或默认 continue）、无 tool_use 时，正常结束循环并返回 | AUTO | 前置：stub API 返回 `stop_reason:"end_turn"`, 无 tool_use；操作：RunTurn；预期：Result.StopReason="end_turn"，无额外循环 | `src/services/api/claude.ts`；`query.ts:classifyStopReason`（CC 中 end_turn → break） | ✅ 通过（`stop_reason.go:38`：default → stopActionContinue；`run.go:380-388`：无 tool_use 时 return） |
| LOOP-20 | `stop_reason: tool_use` 控制流：有 tool_use blocks 时执行工具并继续循环 | AUTO | 前置：stub API 第一轮返回 tool_use，第二轮 end_turn；操作：RunTurn；预期：toolRounds=1，循环正确终止 | `src/services/api/claude.ts`；CC query.ts tool_use 分支 | ✅ 通过（`run.go:380-411`：ToolUses 非空 → toolRounds++ → executeToolUses → continue） |
| LOOP-21 | `stop_reason: max_tokens`（无 tool_use）recovery：注入 continuation 消息，最多重试 `maxOutputTokensRecoveryLimit=3` 次 | AUTO | 前置：stub API 连续 4 次返回 max_tokens（无 tool_use）；操作：RunTurn；预期：3 次重试后停止，不无限循环 | `src/services/api/claude.ts`；CC `query.ts:MAX_OUTPUT_TOKENS_RECOVERY_LIMIT=3`（line:164） | ✅ 通过（`stop_reason.go:22`；`run.go:358-376`；注入 `[The previous response was truncated...]` nudge） |
| LOOP-22 | `stop_reason: max_tokens` with tool_use：有 tool_use 时跳过 recovery，直接执行工具 | AUTO | 前置：stub 返回 `stop_reason:"max_tokens"` 且有 tool_use；操作：RunTurn；预期：工具被执行，不注入 continuation 消息 | `src/services/api/claude.ts:query.ts`（max_tokens + tool_use fall-through） | ✅ 通过（`run.go:370-378`：`len(ToolUses(assistant)) == 0` 检查，否则 fall-through） |
| LOOP-23 | `stop_reason: pause_turn` recovery：重发历史，最多 `maxPauseTurnResumes=10` 次 | AUTO | 前置：stub 连续 11 次返回 `stop_reason:"pause_turn"`；操作：RunTurn；预期：10 次后触发 pauseTurnLimitMessage，不无限循环 | 注：pause_turn 在 CC 源码无对应（`run.go:336-337` 注释）；ccgo 自行扩展 | ✅ 通过（`stop_reason.go:25,29`；`run.go:335-356`） |
| LOOP-24 | `stop_reason: refusal` 处理：usage-policy 拒绝时返回拒绝消息并停止，不重试 | AUTO | 前置：stub 返回 `stop_reason:"refusal"`；操作：RunTurn；预期：Result.Messages 末尾含拒绝提示文本，不再发下一轮 | `src/services/api/errors.ts`（`getErrorMessageIfRefusal`）；CC `query.ts` refusal 分支 | ✅ 通过（`stop_reason.go:45`；`run.go:292-302`：stopActionRefusal → refusalMessage + return） |
| LOOP-25 | `stop_reason: model_context_window_exceeded` + 强制压缩重试：触发 forceCompact，成功后重发请求；只尝试一次，再次失败则 surfaceMessage + stop | AUTO | 前置：stub 首次返回 `model_context_window_exceeded`，CompactClient 可用；操作：RunTurn；预期：触发一次 compact，再次请求成功，`result.Compacted=true` | `src/services/api/claude.ts:query.ts:model_context_window_exceeded`（stopReason 处理） | ✅ 通过（`run.go:303-333`；`stop_reason.go:43`；`forceCompact` 实现完整） |
| LOOP-26 | 孤儿 tool_result 修复（resume 场景）：`ensureToolResultPairing` 为无对应 tool_result 的 tool_use 合成 is_error 结果，剥离无对应 tool_use 的 tool_result | AUTO | 前置：历史含一个无配对 tool_use；操作：`synthesizeOrphanedToolResults`（`orphan_tool_results.go`）；预期：返回一个 `is_error=true` 的合成 tool_result | `src/utils/messages.ts:ensureToolResultPairing`（line ~1000）；CC 用于 resume/teleport | ✅ 通过（`conversation/orphan_tool_results.go:12-39`；`orphan_tool_results_test.go` 覆盖） |
| LOOP-27 | 工具多轮循环（multi-round tool use）：`toolRounds` 计数正确，不超过 `maxToolRounds()` | AUTO | 前置：Runner.MaxToolRounds=3；stub 每轮返回新 tool_use；操作：RunTurn；预期：3 轮后返回 `maximum tool rounds exceeded` 错误 | `src/services/api/claude.ts:query.ts` MAX_TOOL_ROUNDS_EXCEEDED | ✅ 通过（`run.go:257-259`：`toolRounds >= runner.maxToolRounds()`）；`types.go:230` default=8 |
| LOOP-28 | Micro-compaction（ephemeral）：每轮 API 发送前对旧历史做轻量文本摘要（LLM-free），摘要不写入 result.Messages | AUTO | 前置：Runner.EnableMicroCompact=true, MicroCompactKeepLast=5；大于 5 条历史；操作：RunTurn；预期：`result.MicroCompact != nil`, `result.Messages` 不含 micro boundary | `src/services/compact/microCompact.ts`（per-turn ephemeral）；CC `query.ts:412-426` | ✅ 通过（`conversation/micro_compact.go:20-44`；`micro.go` 完整；`micro_compact_test.go` 覆盖） |
| LOOP-29 | Micro-compaction 摘要缓存：相同历史 digest 命中磁盘/内存缓存，不重新计算 | AUTO | 前置：MicroCompactDir 已存一个 digest.json；操作：相同历史再次 MicroCompact；预期：`result.Cached=true`，`SaveMicroResult` 不被调用 | `src/services/compact/microCompact.ts`（cache check by digest） | ✅ 通过（`compact/micro.go:370-425`：先查内存 cache 再查磁盘；`MicroResultUsable` 校验版本+TTL） |
| LOOP-30 | Auto-compaction 触发阈值：估计 token 数超过 `AutoCompactThreshold`（context_window - max_output_tokens - buffer）时自动触发 | AUTO | 前置：Runner.AutoCompact.Enabled=true, Window.ContextWindow=200000；历史 token 估计超阈值；操作：maybeAutoCompact；预期：ok=true，history 被替换为压缩后版本 | `src/services/compact/autoCompact.ts`（`ShouldAutoCompact`）；`compact.ts` | ✅ 通过（`compact/threshold.go:85-93`；`run.go:695-731`） |
| LOOP-31 | Auto-compaction 结果写入 result.Messages：auto-compact 后 boundary + summary 两条消息追加到 result.Messages（与 micro 不同） | AUTO | 前置：同 LOOP-30；操作：RunTurn 触发 auto-compact；预期：result.Messages 包含 Plan.Boundary 和 Plan.Summary | `src/services/compact/compact.ts`（summary 写入 messages）；CC `query.ts` compact result handling | ✅ 通过（`run.go:225-232`：`result.Messages = append(..., compactResult.Plan.Boundary, compactResult.Plan.Summary)`） |
| LOOP-32 | 手动压缩（/compact 命令）：触发 manualCompact，结果写 transcript，emit EventCompact | AUTO | 前置：/compact 命令触发 LocalCommandResultCompact；操作：RunTurn；预期：result.Compacted=true，transcript 写入 | `src/services/compact/compact.ts:compactMessages`；CC slash compact 路径 | ✅ 通过（`run.go:108-117`：LocalCommandResultCompact → manualCompact；`run.go:733-768`） |
| LOOP-33 | 强制压缩（ctx-window overflow recovery）：context window 满时 forceCompact 设 AutoCompact.Force=true 触发 | AUTO | 前置：Runner.AutoCompact != nil；历史过长；操作：forceCompact；预期：ShouldRun 忽略阈值直接压缩 | `src/services/compact/compact.ts`（强制路径）；CC query.ts ctx-window recovery | ✅ 通过（`stop_reason.go:113-124`：forceCompact 设 Force=true；`compact/runner.go` ShouldRun 检查 Force） |
| LOOP-34 | 压缩后文件 tool_result 还原（postCompact）：compact 前被截断/外存的 tool_result 在 postCompact 阶段还原回历史 | AUTO | 前置：session 有 ContentReplacement 记录；操作：compact 后 postcompact；预期：还原后历史 tool_result 内容完整 | `src/services/compact/postCompactCleanup.ts`（还原文件内容） | ✅ 通过（`compact/postcompact.go`；`compact/postcompact_test.go` 覆盖） |
| LOOP-35 | API 侧 context management（`context-management-2025-06-27` beta）：`clear_thinking_20251015` 策略和 `clear_tool_uses_20250919` 策略通过请求体发送到 API | AUTO | 前置：CC 开启 API context management；操作：查 request body 含 `context_management` 字段；预期：body 含 `edits` 数组 | `src/services/compact/apiMicrocompact.ts:getAPIContextManagement`；`src/services/api/claude.ts:1565-1575` | ✅ 通过（F6a commit 02b7690）（`ContextManagementBetaHeader="context-management-2025-06-27"` 常量；`Request.ContextManagement map[string]any` 字段（json:"context_management"）；`dynamicBetaHeadersForRequest` 在字段非空时追加 header；`TestContextManagementBetaHeaderConstant` + `TestRequestContextManagementFieldSerialises` + `TestDynamicBetaHeadersIncludesContextManagementWhenSet` 覆盖） |
| LOOP-36 | Token 预算 `output_config.task_budget`：`task-budgets-2026-03-13` beta + `output_config.task_budget={type:"tokens",total:N,remaining:M}` 发送给 API | AUTO | 前置：CC Options.taskBudget 设值；操作：查 API request body；预期：body 含 `output_config.task_budget.total` | `src/services/api/claude.ts:479-501`（`configureTaskBudgetParams`）；`betas.ts:14`（TASK_BUDGETS_BETA_HEADER） | ✅ 通过（F6a commit 02b7690）（`TaskBudgetsBetaHeader="task-budgets-2026-03-13"` 常量；`requestUsesTaskBudget` 检测 OutputConfig["task_budget"]；`dynamicBetaHeadersForRequest` 追加 header；Request.OutputConfig 已有（map[string]any）；`TestTaskBudgetsBetaHeaderConstant` + `TestRequestOutputConfigTaskBudgetSerialises` + `TestDynamicBetaHeadersIncludesTaskBudgetsWhenSet` 覆盖） |
| LOOP-37 | Effort 参数（`effort-2025-11-24` beta）：`output_config.effort="low"/"medium"/"high"` 发送 | AUTO | 前置：CC Options.effortValue 设值；操作：查 request body；预期：body 含 `output_config.effort` | `src/services/api/claude.ts:440-466`（`configureEffortParams`）；`betas.ts:13`（EFFORT_BETA_HEADER） | ✅ 通过（pre-F6a）（`EffortBetaHeader="effort-2025-11-24"` 常量；`Request.OutputConfig map[string]any`；`requestUsesEffort` + `dynamicBetaHeadersForRequest`；`conversation/request.go:121-123` 设置 `OutputConfig["effort"]`；已有测试覆盖） |
| LOOP-38 | `1M context` beta header：模型名以 `[1m]` 结尾时附加 `context-1m-2025-08-07` | AUTO | 前置：Runner.Model="claude-opus-4-5[1m]"；操作：DynamicBetaHeaders(request)；预期：betas 含 `context-1m-2025-08-07` | `src/constants/betas.ts:3`；`src/utils/betas.ts`（model.includes('[1m]')） | ✅ 通过（`betas.go:13,105-107`：`requestUsesContext1M` + `Context1MBetaHeader`） |
| LOOP-39 | Structured outputs beta header：tools 含 `strict:true` 时附加 `structured-outputs-2025-11-13` | AUTO | 前置：工具 schema `strict=true`；操作：DynamicBetaHeaders；预期：betas 含 `structured-outputs-2025-11-13` | `src/constants/betas.ts:8`（STRUCTURED_OUTPUTS_BETA_HEADER） | ✅ 通过（`betas.go:12,96-103`） |
| LOOP-40 | `cache_edits` block（micro-compact 增量缓存编辑）：请求含 `cache_edits` block 时附加 `cache-editing-2025-01-24` | AUTO | 前置：消息含 ContentCacheEdits block；操作：DynamicBetaHeaders；预期：betas 含 `cache-editing-2025-01-24` | `src/services/compact/microCompact.ts:consumePendingCacheEdits`；`src/services/api/claude.ts:1437-1449` | ✅ 通过（`betas.go:14,85-95`：`requestUsesCacheEditing` + `CacheEditingBetaHeader`） |
| LOOP-41 | 请求元数据 `user_id`：metadata 含 device_id + session_id 的 JSON 字符串 | AUTO | 前置：Runner 设 SessionID；操作：buildRequest；预期：`request.Metadata["user_id"]` 为含 session_id 的 JSON 字符串 | `src/services/api/claude.ts:503-528`（`getAPIMetadata`） | ✅ 通过（W-Batch-B）（`conversation/metadata.go:buildAPIMetadata`；`getOrCreateDeviceID` 持久化到 `~/.claude/device_id`；`buildRequest` 调用 `buildAPIMetadata(r.SessionID)`；`TestBuildRequestIncludesMetadataUserID` + `TestGetOrCreateDeviceIDPersists` 覆盖） |
| LOOP-42 | 请求形状（system prompt）：`Request.System` 接受 string 或 `[]ContentBlock`（带 cache_control 的系统提示） | AUTO | 前置：Runner.SystemPrompt 为非空字符串；操作：buildRequest；预期：`request.System` 非空，类型正确 | `src/services/api/claude.ts:1376-1379`（`buildSystemPromptBlocks`） | ✅ 通过（`request.go:82-84`：`r.systemPromptWithOutputStyle()` → `request.System`） |
| LOOP-43 | 请求形状（tools array）：tools 由 Registry 生成定义，支持 `defer_loading`、`strict`、`cache_control` 字段 | AUTO | 前置：Registry 含 MCP 工具；操作：buildRequest；预期：`request.Tools` 包含 `defer_loading:true` 的 MCP 工具项 | `src/services/api/claude.ts:1235-1246`（`toolToAPISchema`） | ✅ 通过（`request.go:85-87`；`types.go:84-101`：`ToolFromContract` 映射所有字段） |
| LOOP-44 | Tool search（`advanced-tool-use-2025-11-20`）beta header：工具搜索启用时附加该 header | AUTO | 前置：`ENABLE_TOOL_SEARCH=1`，有 MCP 工具；操作：buildRequest；预期：betas 含 tool search header | `src/utils/betas.ts:getToolSearchBetaHeader`；`src/services/api/claude.ts:1174-1182` | ✅ 通过（W-Batch-B）（`betas.go:ToolSearchBetaHeader="advanced-tool-use-2025-11-20"`；`Request.ToolSearchActive` 字段；`dynamicBetaHeadersForRequest` 在 `ToolSearchActive` 时追加；`buildRequest` 设 `ToolSearchActive=toolSearchActive`；`TestDynamicBetaHeadersIncludesToolSearch` + `TestBuildRequestToolSearchActiveInRequest` 覆盖） |
| LOOP-45 | 流式 `context_left` token 追踪：每轮 API 响应 `message_delta.usage` 中的 `input_tokens` + `cache_*` 累计到 Result.Usage，供 auto-compact 阈值判断 | AUTO | 前置：stub 响应含 usage 字段；操作：RunTurn；预期：result.Usage.InputTokens 反映本轮实际 token 数 | `src/services/api/claude.ts:2724,2856`（`tokenCountFromLastAPIResponse`）；CC usage 累计 | ✅ 通过（`stream_accumulator.go:68-73`：message_delta.usage 更新；`run.go:281`：result.Usage=response.Usage；`usage.go:UpdateUsage`） |
| LOOP-46 | 压缩阈值 token 估算：`EstimateTokens` 按字符/4 估算历史 token 数，用于 auto-compact 触发判断 | AUTO | 前置：history 含已知大小消息；操作：EstimateTokens(history)；预期：估算值约为总字符数/4 | `src/services/compact/autoCompact.ts`（`estimateTokens`） | ✅ 通过（`compact/estimate.go`；`compact/threshold.go:ShouldAutoCompact`） |
| LOOP-47 | `max_tokens` context overflow（400）通过 `model_context_window_exceeded` stop_reason 替代处理：beta header `context-management` 开启后 API 改为 stop_reason 而非 400 | AUTO | 前置：模型支持 context-management beta；操作：历史超长后 RunTurn；预期：stop_reason 为 `model_context_window_exceeded`，而非 400 错误 | `src/services/api/withRetry.ts:386`（注释：with extended-context-window beta，400 不再出现） | ✅ 通过（`stop_reason.go:43`；`run.go:303-333`：两条路径都有，兼容旧 400 + 新 stop_reason） |
| LOOP-48 | CLAUDE_CODE_EXTRA_BODY 环境变量注入额外 API body 参数 | AUTO | 前置：`CLAUDE_CODE_EXTRA_BODY='{"custom":"value"}'`；操作：API 请求；预期：请求 body 含 `custom:"value"` | `src/services/api/claude.ts:272-330`（`getExtraBodyParams`） | ✅ 通过（F6a commit 02b7690）（`getExtraBody()` 解析 CLAUDE_CODE_EXTRA_BODY；`mergeExtraBody` 浅合并到 /v1/messages 请求 JSON；非对象/非法 JSON 静默忽略；`TestExtraBodyMergesIntoRequest` + `TestExtraBodyIgnoresNonObjectJSON` + `TestExtraBodyIgnoresInvalidJSON` 覆盖） |
| LOOP-49 | 媒体项超限截断：请求 messages 中图片/文档超过 `API_MAX_MEDIA_PER_REQUEST=100` 时，自动剥离最旧的媒体 block | AUTO | 前置：构造含 101 张图片的历史；操作：NormalizeForAPI + stripExcessMediaItems；预期：media 数 ≤ 100 | `src/services/api/claude.ts:956-1015`（`stripExcessMediaItems`）；`src/constants/apiLimits.ts` | ✅ 通过（G15）：`buildRequest` 现在在 `NormalizeForAPI` 之后自动调用 `StripExcessMediaItems(apiMessages, APIMaxMediaPerRequest)`；`TestBuildRequestStripsExcessMedia_ExceedsLimit` + `TestBuildRequestStripsExcessMedia_WithinLimit` 覆盖 |
| LOOP-50 | 响应非流式 non-streaming fallback 超时控制：非流式请求有独立超时（remote 120s，本地 300s） | AUTO | 前置：`CLAUDE_CODE_REMOTE=1`；操作：查看 createMessage 超时配置；预期：超时上限为 120000 ms | `src/services/api/claude.ts:807-811`（`getNonstreamingFallbackTimeoutMs`） | ✅ 通过（G15）：`run.go` fallback 路径现在用 `context.WithTimeout(ctx, NonstreamingFallbackTimeout())` 创建子 context 再调 `CreateMessage`；`TestNonstreamingFallbackAppliesTimeout` 覆盖 deadline 注入 |

小计：50 行 — ✅ 通过 43（含 F6a +7 + G15 +2: LOOP-49/50 调用已接线），⚠️ 已建未接 0，❌ 缺失 7，N/A 0
